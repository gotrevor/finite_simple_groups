# Handoff: v4.31 bump landed — next is discharging the open axioms

**Date**: 2026-06-21 19:18 UTC · **Branch**: `cfsg-sporadic-order-pin`

## 🎯 What we're doing
The assigned task was the **mathlib v4.29.1 → v4.31.0 forward-port** (host ran `lean-bump`; I did the
source fix-grind). That is **DONE, green, axiom-verified, committed**. With the bump out of the way the
standing goal reverts to the repo's real target: discharging the cited `axiom`s of the CFSG / Feit–Thompson
spine into real proofs (39 remain).

## ✅ State (all observed this session, not assumed)
- `lake build FiniteSimpleGroups FeitThompson` → **Build completed successfully (8622 jobs)**, exit 0,
  at `leanprover/lean4:v4.31.0`. Confirmed both by direct build and by the `.githooks` pre-commit hook
  (which runs `taskset -c 0-5 lake build`).
- **3 bump commits + 1 bonus**, all on `cfsg-sporadic-order-pin` (HEAD `ba5263e`):
  - `ee11288` forward-port (7 broken modules + 7 cascade files),
  - `5cf00d9` deprecation sweep (zero warnings remain),
  - `ba5263e` discharged `alternatingGroup_isSimple` axiom → real theorem (mathlib's
    `alternatingGroup.isSimpleGroup` is now in the pin). Axiom count **40 → 39**.
- Faithfulness gate: `#print axioms` on the refactored / discharged results
  (`SLn.transvecSL_closure_eq_top`, `commutator_SLn_eq_top`, `PSLn_nontrivial`,
  `trace_mulLeft_pi_matrix`, `exists_trivial_factor`, `fittingSubgroup_isNilpotent`,
  `alternatingGroup_isSimple`) → `[propext, Classical.choice, Quot.sound]` only, **no `sorryAx`**.
- `.lake/.bump-inflight.json` marker removed. `HANDOFF.md` top refreshed to "BUMP COMPLETE".
- Working tree clean except untracked `CLAUDE.local.md` (local-only, do NOT commit).

## 🧠 Context to carry forward
The v4.31 break families (all in the now-committed diff, useful when the next bump or a forward-port hits them):
1. **`IsMulCommutative`→`CommGroup/CommMonoid/CommMagma` went scoped** → `open scoped IsMulCommutative`.
   This is *not* in the published cookbook (it's an instance-resolution break). Saved as a memory note.
2. **`convert … using N` over-splits** (cookbook A/K/P) into spurious type/heq/metavar goals. In SLnPerfect
   the clean replacement was either (a) `exact Subgroup.mul_mem …` when the goal subject is *defeq* to the
   product, or (b) a 3-line helper `mem_closure_of_val_eq` that transports membership across equal matrix
   values using **bound** SL variables — because an inline `(⟨M,h⟩ : SpecialLinearGroup …)` elaborates at
   the *raw subtype* and poisons `*`/the equation type. (Memory note saved.)
3. **`simpa`/`simp only` final checks went syntactic / pre-β-reduced** (cookbook F/I) → `simp only …; exact`
   or just delete the no-op line.
4. Misc: `Nat.card` vs `Fintype.card` bridges, `Nat.card_sigma` now needs `[Fintype α]`, `add_apply`
   ambiguity (`_root_` vs `Matrix`), deprecated set-diff renames.

I deliberately did **not** start a deep axiom this lap: I'd just spent the budget on the bump, and starting
a multi-step FT proof at the tail risks leaving a half-`sorry` state that's *worse* than the current honest
axiom (the build is currently 100% `sorry`-free; converting an axiom to a `sorry`-holding theorem would
regress the self-stop gate). The remaining axioms are all genuinely deep — I surveyed them (see below).

## 🎬 Next actions
1. **Crack `commutator_lt_of_minnormal`** in `FeitThompson/BGsection1/L1_2.lean` — the most self-contained
   open axiom (`{G}[Group G]`, no Fintype). Statement: for `M` minimal-normal,
   `⁅M, FittingSubgroup G⁆ < M`. **Attack path:** ⁅M,F(G)⁆ is normal-in-G (commutator of two normals;
   `fittingSubgroup_normal` is axiom-free) and ≤ M (M normal). By `MinNormal` it's `⊥` or `M`. Rule out
   `= M`: if `⁅M,F⁆ = M` then `M ≤ F(G)` (since `⁅M,F⁆ ≤ M ⊓ F`); but `F(G)` is nilpotent
   (`fittingSubgroup_isNilpotent`, axiom-free) and in a nilpotent `H` a subgroup `K` with `⁅K,H⁆ = K`
   must be `⊥` (lower-central-series descent: `K ≤ ⁅K,H⁆ ≤ γ₂ ≤ … ≤ γ_c = ⊥`). Find/port the mathlib
   nilpotency-descent lemma (`lowerCentralSeries` API) — that's the crux of the proof. Check this repo's
   exact `MinNormal` / `FittingSubgroup` defs (`FiniteSimpleGroups/FittingSubgroup.lean`,
   `MinimalNormal.lean`) before writing.
2. If that resists, the other FT §1 axioms (`P1_3` chief-stabilizer pair, `P1_4`, `P1_6`, `P1_8`,
   `P1_10 comm_norm_cent_subset_cent`, `T1_11`/`C1_12` coprime-action "assembly") are the B&G §1 content —
   harder, paper-driven. The carrier-simplicity axioms (`LieType.PSU/POmega`, all of `Exceptional.lean`,
   `Sporadics` Co1-3) and the CFSG-structure axioms (`ProofTree`, `ProofStrategy`) are multi-*year* — read,
   decompose, formalize one prerequisite; don't expect a lap-discharge.
3. **Trevor's parked thread:** `wip/ushortroot-atom` (+95 lines `UnitarySimple.lean`, short-root atom
   reduction toward unitary simplicity) needs **forward-porting to v4.31** as its own commit. The earlier
   handoff deferred this to "Trevor / a later lap" — it's his in-flight work; coordinate rather than merge
   it unilaterally.

## ⚠️ Gotchas
- **Use `taskset -c 0-5 lake build …`** for full-target builds — a plain parallel `lake build` spuriously
  hits "Too many open files" (system file-table exhaustion mmapping the mathlib olean closure; `ulimit` is
  huge and not the cap; `LAKE_NUM_PROC` is ignored; lake has no `-j`). One-module-at-a-time also works. The
  pre-commit hook already does the taskset thing, so commits gate fine.
- Don't commit `CLAUDE.local.md` (it's the gitignored-by-intent local memory pointer; `git add -A` grabs it
  — `git restore --staged CLAUDE.local.md` first, as I did).
- Converting an `axiom` to a `theorem … := by sorry` would break the `sorry`-free invariant. Only convert an
  axiom when you can fully close it (like Alternating); otherwise leave the honest axiom.

## 📁 Key files
- `FeitThompson/BGsection1/L1_2.lean` — `commutator_lt_of_minnormal` axiom (next-action #1 target).
- `FiniteSimpleGroups/FittingSubgroup.lean` — `fittingSubgroup_isNilpotent` / `_normal` (both axiom-free; the
  load-bearing inputs for #1).
- `~/src/mathlib-bump-cookbook/v4.29.1-to-v4.31.0.md` — the bump cookbook (entries A–R), read-only in box.
- `~/personal/claude/knowledge/core/projects/lean-journey/reference/` — 132-note corpus; `grep -rl` it before
  re-deriving any friction.
- `HANDOFF.md` (master) — top section refreshed to "BUMP COMPLETE"; older stacked handoffs below it.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the context
above, then go to Next actions #1 and start cracking `commutator_lt_of_minnormal` (read its repo defs first,
then build the nilpotent-descent lemma). Build with `taskset -c 0-5 lake build …`.**
