# Handoff: Fitting work pushed to origin + repo housekeeping done. Next lens: `E(G)`

**Date**: 2026-05-30 (22:24 UTC) · **Branch**: `main` (the only branch now) · **Tip**: `16aafe3`

## 🎯 What we're doing

The CFSG Fitting subgroup is **complete and axiom-free** (done in the prior session,
see [`HANDOFF-2026-05-30-2146.md`](HANDOFF-2026-05-30-2146.md) for the math). This
session was pure plumbing: land that work on GitHub and tidy the repo. The actual
next *math* brick is unchanged — `E(G)` / generalized Fitting `F*(G) = E(G)·F(G)`.

## 🧠 Context to carry forward

- **The local-merge → push path, and why no PR.** The prior session fast-forwarded
  `cfsg-fitting-nilpotent` into local `main` (Trevor asked). This session Trevor said
  "push main!" and asked whether a PR was still worth it. **Answer: no** — a PR after
  a ff-merge into an already-pushed main would have an empty diff, since origin/main
  now contains every commit. We pushed `main` directly. This *bypassed the repo's
  standing "admin-merge on green CI via PR" convention* — acceptable here because the
  build was locally verified green and Trevor chose it, but note CI never ran on
  `a40c485..16aafe3`. If you want CI coverage on future bricks, branch + PR *before*
  merging to main rather than ff-ing locally first.
- **The repo is now clean of cruft.** It used to carry 15+ stale local branches and a
  second worktree (`~/src/finite_simple_groups-ft`, the box-side worktree where the FT
  + CFSG work happened). All gone now (details in State). Only `main` remains.
- **Two-track structure still applies** (from the master HANDOFF): `FeitThompson/`
  (FT-port, frozen at 11 cited axioms — LEAVE ALONE) and `FiniteSimpleGroups/` (CFSG,
  active). The worktree removal didn't touch either track's *files* — they live on
  `main`, which absorbed everything.

## ✅ State (all verified this session, from real output)

- `git push origin main` → `e1a7f88..16aafe3  main -> main` ✅ origin now has the
  axiom-free Fitting work.
- Worktree `finite_simple_groups-ft` removed (`git worktree remove`); was clean (no
  uncommitted changes, no stashes) and its branch was identical to main's tip.
- 16 merged local branches deleted (`cfsg-fitting-nilpotent` + 15 others, all
  confirmed `git branch --merged main`, zero unmerged). `git branch` → only `main`.
- Repo build state inherited from prior session (NOT re-run this session): Fitting
  was EXIT 0, 8248 jobs, 0 sorries, `#print axioms` clean. I did not rebuild — no
  code changed this session.

## 🎬 Next actions

1. **Start the `E(G)` brick** — the generalized Fitting subgroup `F*(G) = E(G)·F(G)`
   (Solomon p.343). Reuse the noncommPiCoprod coprime-direct-product pattern from
   `FittingSubgroup.lean` (recipe in `docs/fitting-roadmap.md` and the 2146 handoff's
   "nilpotency proof shape" bullet). Do NOT re-open Fitting.
2. Branch first if you want CI on it (see context bullet on the bypassed PR convention).

## ⚠️ Gotchas

- The master `HANDOFF.md` top block previously said branch `cfsg-fitting-nilpotent`,
  "not pushed, 7 local commits awaiting host-side push." I **refreshed it this session**
  to reflect the push + branch deletion — but double-check it reads sanely; the branch
  it named no longer exists.
- `origin/main` and local `main` are now in sync at `16aafe3`. No host-side push debt
  remains (the long-standing "push from host" todo is resolved).

## 📁 Key files

- `FiniteSimpleGroups/FittingSubgroup.lean` — the completed axiom-free Fitting work
- `docs/fitting-roadmap.md` — step 4 DONE; reusable nilpotency recipe + Lean gotchas
- `HANDOFF-2026-05-30-2146.md` — the math-state handoff this one sits on top of
- `HANDOFF.md` — master/post-merge pointer (refreshed this session)

---
**→ Next session: this is your starting point. Don't summarize this doc back to
Trevor or wait for instructions, and don't offer other projects from the KB — this
doc IS the chosen thread. The plumbing is done; absorb the context, then start the
`E(G)` / generalized Fitting brick (Next action #1), branching first if you want CI.**
