# Handoff: Burnside `pᵃqᵇ` — ingredient 3 keystone DISCHARGED, endgame infrastructure built

**Date**: 2026-06-03 04:26 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `82dc31a`

## 🎯 What we're doing

Discharging the one sharp axiom `isSimpleGroup_centralizer_index_not_primePow` (`Burnside.lean`) —
Burnside's prime-power class-size lemma — into a real proof, via 6 character-theory ingredients.
**This lap discharged the last-missing ingredient 3 (column orthogonality) keystone and built nearly
all the remaining endgame bricks.** All work is in `FiniteSimpleGroups/CharacterTheory.lean`.

## 🧠 Context to carry forward

- **The lap's big win**: ingredient 3 (Route B, regular character) is no longer a gap. The keystone
  `exists_wedderburn_character_decomp` (axiom-clean) gives `χ_reg(g) = ∑ᵢ dᵢ·trace(Rᵢ g)` via
  Artin–Wedderburn `ℂ[G] ≃ₐ ∏ᵢ Mₐᵢ(ℂ)`, where `Rᵢ : G →* Mₐᵢ(ℂ)` is the `i`-th projection of
  `single g 1`. Mechanism: `χ_reg(g) = trace(mulLeft (single g 1))` → push through the Wedderburn
  iso `e` (`LinearMap.trace_conj'` + `conj_mulLeft`) → split over the product + matrix-algebra trace
  formula (`trace_mulLeft_pi_matrix`, Aristotle-discharged). With `character_leftRegular_eq`
  (`χ_reg(g)=|G|[g=1]`) this is `∑ᵢ dᵢ·χᵢ(g)=0` for `g≠1`.
- **Architecture is now fully scoped.** The endgame reduces to `burnside_final_contradiction` (built,
  axiom-clean): from `∑ᵢ aᵢ=0` with `a_{i₀}=1` (unique trivial factor) and `aᵢ=p·θᵢ` (`θᵢ∈ℤ̄`) for
  `i≠i₀`, derive `False` (`Θ=-1/p∈ℤ̄` contradicts `not_isIntegral_neg_inv_prime`). Instantiate
  `aᵢ=dᵢ·χᵢ(g)`. **Three residual gaps feed its hypotheses** — see `PENDING_WORK.md §A` (freshly
  rewritten) for the full map. They are: (1) irreducibility of each `Rᵢ`, (2) the scalar bridge
  `‖χ(g)‖=χ(1)⇒ρg scalar`, (3) trivial multiplicity `T=1`.
- **Why Route B and not abstract Irr(G)**: mathlib has NO multiplicity-decomposition / `#Irr=#classes`
  / packaged `Irr(G)`. The Wedderburn-factor approach indexes irreducibles concretely by `Fin n` and
  makes `χ_reg=∑dᵢχᵢ` a *trace computation*, not a decomposition theorem. This avoids the abstract
  completeness theory entirely.
- **T=1 insight (gap 3)**: for nonabelian simple `G`, `[G,G]=⊤` (proved this lap,
  `commutator_eq_top_of_isSimpleGroup_of_nonabelian`), so `G^ab=1`, so the trivial rep is the ONLY
  linear character ⇒ exactly one `dᵢ=1` factor. Still needs "#{i:dᵢ=1} = |G^ab|" — a research
  question filed in `ON-LINE-REQUEST.md` (the clean mathlib route to count 1-dim factors).
- **`Representation.asModule` friction** (blocks the irreducibility criterion locally): the
  `ℂ[G]`-module lives on the synonym `ρ.asModule`, the `End ℂ V`-module on `V`; transferring between
  them via `isSimpleModule_of_ringHom_surjective` needs `inferInstanceAs`/explicit `M` plumbing that
  resisted a quick local proof. I handed the whole criterion to Aristotle (`critjob`) to dodge it.

## ✅ State (all verified green this lap)

14 commits (`60900d7..82dc31a`), every one `lake build` green (8297 jobs, pre-commit-gated) and
new theorems `#print axioms`-clean (`[propext, Classical.choice, Quot.sound]`). New in
`CharacterTheory.lean`:
- `exists_wedderburn_character_decomp` (THE keystone) + `trace_mulLeft_pi_matrix` (Aristotle
  `eed8a149`, ported+verified), `conj_mulLeft`, `character_ofMulAction_eq_trace_mulLeft`.
- `repOfMatrixHom` + `repOfMatrixHom_character` (Rᵢ as `Representation`, char=trace),
  `trace_matrixHom_isIntegral` (trace∈ℤ̄), `norm_trace_matrixHom_le` (‖trace‖≤dᵢ).
- `isIntegral_of_coprime_smul` (Bézout: `mβ,nβ∈ℤ̄`, `gcd=1 ⇒ β∈ℤ̄`).
- `injective_of_isSimpleGroup_of_exists_ne` (faithfulness of nontrivial Rᵢ).
- `isSimpleModule_of_ringHom_surjective` (simplicity transfer) + `isSimpleModule_natural_matrix`
  (Aristotle `dc41262f`, ported+verified).
- `commutator_eq_top_of_isSimpleGroup_of_nonabelian` (perfect group).
- `burnside_final_contradiction` (endgame arithmetic core).

`burnside_simple` still cites the axiom — the endgame is NOT yet wired in (needs the 3 gaps).

## 🎬 Next actions

1. **Poll Aristotle** (`aristotle list` — one-shot, NOT `show`/TUI). THREE Burnside jobs in flight:
   - `e66a25d1` `scalar_job` — scalar bridge `matrix_scalar_of_pow_eq_one_of_norm_trace_eq` (gap 2).
   - `09bdf796` `endjob` — `isSimpleModule_End_of_nontrivial` (natural End-module simple; gap 1 input).
   - `fe2dc954` `critjob` — `isIrreducible_of_asAlgebraHom_surjective` (the criterion; gap 1).
   For each done: download (`--destination /tmp/x.tar.gz` is a TARBALL, then `tar -xzf`), **verify in
   our v4.29.1 kernel + `#print axioms` clean** before trusting, port into `CharacterTheory.lean`
   (replacing inlined axioms with the real ported lemmas), then **immediately submit the next brick**.
   Port instructions in `ARISTOTLE-JOB-burnside-vanishing.md` (Jobs 2–6, esp. the criterion wiring).
2. **Finish gap 1 (irreducibility of Rᵢ)**: once the criterion lands, the remaining sub-task is
   *surjectivity of `(repOfMatrixHom Rᵢ).asAlgebraHom`* = `toLinAlgEquiv' ∘ πᵢ ∘ e` surjective
   (algebra-hom ext on `single g 1` generators; `e` and `πᵢ` are inside the `exists_wedderburn_…`
   proof, so this needs exposing them — refactor the existence theorem to also yield surjectivity).
3. **Gap 3 (T=1)**: act on `ON-LINE-FINDINGS-*` if a host answered the `ON-LINE-REQUEST.md` items;
   else pursue the `[G,G]=⊤ ⇒ unique dᵢ=1 factor` route.
4. **Wire the endgame**: assemble `isSimpleGroup_centralizer_index_not_primePow` from
   `exists_wedderburn_character_decomp` + per-factor case split + `burnside_final_contradiction`.

## ⚠️ Gotchas

- **`rw [MonoidAlgebra.single_mul_single]` / `rw [LinearMap.mulLeft_apply]` can FAIL to match
  visually-identical terms** when the goal's `*`/`mulLeft` was introduced by `show` against the
  `MonoidAlgebra` synonym (instance-path mismatch). Fixes that worked: `erw`, or a `show ... =
  MonoidAlgebra.single _ _ * MonoidAlgebra.single _ _` with canonical mul, or prove the fact as a
  standalone lemma in isolation (it matches there) and `rw` that. Cost me several iterations.
- **`Algebra.trace` requires `CommRing S`** — useless for `ℂ[G]`/matrices (noncommutative). Use
  `LinearMap.trace` of `LinearMap.mulLeft` instead (what the keystone does).
- **OOM-thrash**: `lake build`/`lake env lean` intermittently `Cannot allocate memory` on an unrelated
  mathlib module — just retry. Aristotle pins v4.28, we pin v4.29.1: always re-verify returned proofs.
- The mysterious `1537a23 "chore: archive superseded HANDOFF docs"` in the log is not mine (a hook?);
  repo is clean and green regardless.

## 📁 Key files

- `FiniteSimpleGroups/CharacterTheory.lean` — all bricks; top docstring = 6-ingredient board (updated).
- `FiniteSimpleGroups/Burnside.lean` — the sharp axiom + machine-checked `burnside_simple`.
- `PENDING_WORK.md §A` — **freshly rewritten**: keystone-done + the 3 residual gaps + attack paths.
- `ARISTOTLE-JOB-burnside-vanishing.md` — Jobs 1–6 tracker (UUIDs, port + criterion-wiring instructions).
- `ON-LINE-REQUEST.md` — 2 research asks (T=1 / 1-dim-factor count; ring-hom-surjective simplicity transfer).
- `ON-LINE-FINDINGS-2026-06-03-burnside-paqb.md` — host's Route-B guidance (still the map).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context, then start at Next action #1: `aristotle list`; for any of `e66a25d1`/`09bdf796`/`fe2dc954`
that's IDLE/done, download + verify + port (then submit the next), else continue gap 1/3 locally.**
