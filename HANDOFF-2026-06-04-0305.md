# Handoff: PSL(2,q) DONE + soundness fix + PSL(n≥3) thread opened

**Date**: 2026-06-04 03:05 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `37acabb`

## 🎯 What we're doing

Discharging the repo's tractable family-simplicity axioms. **This lap closed the
PSL(2,q) thread** (the prior handoff's pending item) and **opened the PSL(n,q) n≥3
thread** (`PSL_isSimpleGroup_rank_ge_three`). 6 commits, all green via the pre-commit
full `lake build`. Everything new is `#print axioms`-verified.

## ✅ State (all committed, all green builds observed)

1. **PSL(2,q) simple — DONE, axiom-clean** (`37aec52`). `SL2.PSL2_isSimpleGroup (q)
   [Fact prime q] (hq : 4 ≤ q)` proven via Iwasawa; `[propext, Classical.choice,
   Quot.sound]`. (Last lap had written the port but not seen the build go green; this
   lap confirmed it.)
2. **`axiom PSL_isSimpleGroup` discharged at n=2 + SOUNDNESS FIX** (`77442fa`). The old
   monolithic axiom was **mathematically false** for composite `q` (PSL n q =
   `ProjectiveSpecialLinearGroup (Fin n) (ZMod q)`; `ZMod q` is a field only for prime
   `q`; PSL(2,ZMod 6) is not simple). Split into: `LieType.axiom
   PSL_isSimpleGroup_rank_ge_three (n q) [Fact prime q] (3≤n)` (deep, now sound) +
   `SL2.theorem PSL_isSimpleGroup` (dispatches n=2→PSL2_isSimpleGroup, n≥3→axiom). It had
   **zero term-level consumers** (only docstrings), so safe.
3. **|PSL(2,q)| = q(q²−1)/2** (`51b9f1b`). `SL2.card_PSL2`/`card_center_SL2`, axiom-clean.
   Wired the orphaned `SL2Card.lean` into the build (imported by SL2.lean).
4. **PSL(n,q) n≥3 thread** (`9ef5b3e`, `2e32a54`, `37acabb`) in new `SLnPerfect.lean`:
   - `steinberg_comm` — Steinberg relation `⁅t_{ik}(a),t_{kj}(b)⁆=t_{ij}(ab)`, axiom-clean.
   - `transvecSL_mem_commutator` — for `3≤|n|`, each transvection is a commutator (the
     genuinely-new n≥3 content, no field-size restriction). Axiom-clean.
   - `commutator_SLn_eq_top` / `commutator_PSLn_eq_top` — SL/PSL(n,F) perfect for `3≤|n|`,
     **modulo one disclosed axiom `transvecSL_closure_eq_top`** (transvections generate SL_n).
   - `PSLn_nontrivial` — PSL(n,F) nontrivial for `2≤|n|`, axiom-clean.

## 🧠 Context to carry forward

- **Active thread = PSL(n,q) n≥3 via Iwasawa**, mirroring the n=2 proof on `ℙ^{n-1}`.
  Live tracker: **`PENDING_WORK.md §E`** (authoritative; read it). Obligations: perfect ✅,
  nontrivial ✅, then MulAction / faithful / quasi-preprimitive / IwasawaStructure — all
  needing the `SL(n,q) ↷ ℙ^{n-1}` action.
- **One Aristotle job IN FLIGHT**: `a4b84e9f-26f0-41d2-8208-2483d423ce9b` (`slngen`) proving
  `transvecSL_closure_eq_top` (transvections generate SL_n — the Whitehead det-1-diagonal
  step). See `ARISTOTLE-JOB-transvec-generate.md`. **Harvest it first next lap** (verify in
  our kernel + `#print axioms` clean, then drop the `axiom` in SLnPerfect → `commutator_SLn/
  PSLn_eq_top` become fully axiom-clean). Statement byte-identical to the repo's `transvecSL`,
  so it ports directly. HARD target — Aristotle may not crack it; if so decompose to the
  bounded sub-brick "det-1 diagonal = product of transvections".
- **mathlib already has the SL center characterization** — `Matrix.SpecialLinearGroup.mem_center_iff`
  (center = scalar matrices, n-th roots of unity) + `center_equiv_rootsOfUnity'`. Used in
  `PSLn_nontrivial`. DO NOT re-derive (one rule). Cite these for the faithfulness/order obligations.
- **The action obligation needs a DELIBERATE REFACTOR, not a quick add.** mathlib gives
  `MulAction (SL n F) (Projectivization F (n→F))` *once* you supply the linear `SMul/MulAction/
  DistribMulAction/SMulCommClass (SL n F) (n→F)` via `mulVec`. But `SL2.lean` already declares
  those for `Fin 2` — declaring general-`n` versions creates instance diamonds at `Fin 2` (the
  defeq might save proofs, but it's genuinely risky). Clean fix: extract the general `mulVec`
  action instances + `smul_vec_def` into a shared `SLnAction.lean`, have `SL2.lean` import &
  reuse them (delete its locals; watch the `smul_vec_def` name references at SL2 lines ~402,
  444, 615, 620), then build the ℙ^{n-1} descent (`center_smul_eq_n` via `mem_center_iff` +
  scalar≠0 fixes lines; `pslnAction` via QuotientGroup.lift+compHom, mirroring `psl1Action`).
  I deferred this to avoid risking the verified PSL(2,q) build late-lap.
- The Aristotle Steinberg job (`4827df6e`) was submitted then **beaten locally** — ignore it.

## 🎬 Next actions

1. `aristotle list` → check `a4b84e9f`. If done: `aristotle download a4b84e9f-... --destination
   /tmp/slngen-sol`, VERIFY in our kernel (`lake env lean`, `#print axioms` clean), port onto
   `SLnPerfect.transvecSL_closure_eq_top` (drop the `axiom`), confirm `commutator_SLn_eq_top`
   is now fully axiom-clean, commit. **Then immediately submit the next brick** to keep one in
   flight (candidate: the ℙ^{n-1} 2-transitivity lemma, or |SL(n,q)| order formula).
2. Do the **`SLnAction.lean` refactor** (see Context) to unblock the action/faithful/
   quasi-preprimitive obligations. Develop in scratch first; build SL2 to confirm the refactor
   didn't break the PSL(2,q) proof BEFORE committing.
3. Continue §E obligations on `ℙ^{n-1}` toward assembling `PSL2_isSimpleGroup`-analog for n≥3.

## ⚠️ Gotchas

- `lake build` intermittently dies with `Cannot allocate memory` on an unrelated `.olean.server`
  — **spurious, just retry** (hit it once this lap on commit; retry succeeded).
- The pre-commit hook runs a full `lake build` (taskset -c 0-5); a successful commit = a green
  full build. A manual `lake build` of default targets can exceed the 590s timeout (FeitThompson);
  build the specific leaf (`lake build FiniteSimpleGroups.SLnPerfect`) instead.
- `rw [← lemma]` whose RHS is a literal (e.g. `2`) rewrites EVERY `2` in the goal (`Fin 2`,
  `PSL 2 q`) — a disaster I hit. Use forward `have`s instead.

## 📁 Key files

- `FiniteSimpleGroups/SLnPerfect.lean` — the new n≥3 thread (steinberg, perfect, nontrivial).
- `FiniteSimpleGroups/SL2.lean` — completed PSL(2,q) thread + the unified `PSL_isSimpleGroup`.
- `FiniteSimpleGroups/LieType.lean` — the residual `axiom PSL_isSimpleGroup_rank_ge_three`.
- `PENDING_WORK.md §E` — authoritative obligation tracker for the active thread.
- `ARISTOTLE-JOB-transvec-generate.md` — the in-flight job's spec + harvest steps.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait
for instructions, and don't offer other projects from the KB — this doc IS the chosen thread.
Absorb the context, then start at Next action #1: `aristotle list` to check `a4b84e9f`, harvest
if ready, and keep driving PENDING_WORK §E (PSL(n,q) n≥3).**
