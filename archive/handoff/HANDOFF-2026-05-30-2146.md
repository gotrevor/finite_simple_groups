# Handoff: CFSG Fitting subgroup — COMPLETE. Next lens: generalized Fitting `F*(G)`

**Date**: 2026-05-30 (21:46 UTC) · **Branch**: `cfsg-fitting-nilpotent`
(off `main` @ `e1a7f88`, **not pushed** — no GitHub egress in the box) · **Tip**: `65beecb`

## 🎯 What just got done (this session)

`FiniteSimpleGroups/FittingSubgroup.lean` is now **axiom-free**. Both halves of
Fitting's Theorem are proved; the two custom `axiom`s the prior session left are
**discharged**. Verified, not assumed:

- `lake build FiniteSimpleGroups.FittingSubgroup` → **EXIT 0, 8248 jobs, 0 sorries**
  (read from `/tmp/fit_build.log`).
- `#print axioms fittingSubgroup_isNilpotent` and `… fittingSubgroup_normal` →
  `[propext, Classical.choice, Quot.sound]` only (custom axioms gone).
- `grep -nE "^axiom|sorry" FittingSubgroup.lean` → none.

5 commits this session, all green-verified before commit, on `cfsg-fitting-nilpotent`:
`a40c485` (sub-goal 1) · `84514b4` (F=⨆O_p over primes) · `7cbdfc4`
(pCore_eq_bot_of_not_dvd) · `dd7cf2f` (isNilpotent axiom discharged) · `65beecb`
(normal axiom discharged + file axiom-free). Working tree clean.

## 🧠 Context to carry forward

- **Two tracks, one worktree.** `FeitThompson/` (FT-port, frozen at 11 cited axioms
  — LEAVE ALONE) and `FiniteSimpleGroups/` (CFSG, active). Don't disturb FT.
- **The big new lemmas** (all in `FittingSubgroup.lean`): `iSup_prime_sylow_eq_top`
  (Sylows generate any finite group — reusable!), `fittingSubgroup_le_iSup_pCore`,
  `iSup_pCore_le_fittingSubgroup`, **`fittingSubgroup_eq_iSup_pCore`** (`F(G) = ⨆_p
  O_p(G)`, join over primes), `pCore_eq_bot_of_not_dvd`, **`iSup_pCore_isNilpotent`**,
  and the two former-axioms `fittingSubgroup_isNilpotent` / `fittingSubgroup_normal`.
- **Index decision — load this before touching anything pCore.** The join ranges
  over **primes** (`⨆ (p : ℕ) (_ : p.Prime), pCore G p`), NOT all `p : ℕ`. For
  *composite* `p`, `IsPGroup p` is a `π`-group condition and `O_p(G)` need not be
  nilpotent (`O_6(S₃) = S₃`). The prior roadmap's "⨆_p" was silently wrong; I caught
  and fixed it.
- **Two Lean gotchas hit this session (both now in `docs/fitting-roadmap.md`):**
  1. `Group.IsNilpotent (⨆ …)` fails with `synthesize SupSet (Type)` — the `Type`
     expectation pushes into the `⨆` body. Write `Group.IsNilpotent ↥(⨆ …)`.
  2. `Prime p` is *definitionally an `And`*, so `hp.prime.pow_dvd_…` dot-resolves to
     `And.…`. Use the `Nat`-namespaced lemma directly:
     `Nat.Prime.pow_dvd_iff_le_factorization hpp _` (takes `Nat.Prime p`, no coercion).
- **The nilpotency proof shape** (reusable for `E(G)` later): normal subgroups with
  pairwise coprime orders ⇒ `commute_of_normal_of_disjoint` + `IsPGroup.disjoint_of_ne`
  ⇒ `independent_of_coprime_order` ⇒ `Subgroup.noncommPiCoprod` over
  `(Nat.card G).primeFactors` is injective with range `⨆`, so `MonoidHom.ofInjective`
  + `MulEquiv.subgroupCongr` gives `⨆ ≃* ∏`; then `isNilpotent_pi` +
  `nilpotent_of_mulEquiv`. This mirrors `Sylow.directProductOfNormal`.

## 🎬 Next actions (pick the thread)

The Fitting brick is done. Solomon (Bulletin AMS 38, 2001, p.343) hangs the whole
CFSG architecture on the **generalized Fitting subgroup `F*(G) = E(G)·F(G)`**. `F(G)`
is now built; the natural next brick is **`E(G)`, the layer** (the product of the
components — subnormal quasisimple subgroups). Options, roughly increasing effort:

1. **`E(G)` / components.** Define `Component`/quasisimple, `E(G)` as the join of
   components, prove they commute pairwise and `E(G)` is normal. Then define
   `F*(G) = E(G) ⊔ F(G)` and target its basic properties (normal; contains `F(G)`;
   self-centralizing `C_G(F*(G)) ≤ F*(G)` is the deep one — likely an axiom at first).
   Check mathlib for `Quasisimple` / `IsComponent` first (probably absent → hand-roll).
2. **Mine the new `iSup_prime_sylow_eq_top`** — it's a clean general fact ("Sylows
   generate any finite group") that mathlib may lack; could be a standalone PR.
3. **Strengthen `FittingSubgroup`** — e.g. `F(G)` is the largest normal nilpotent
   subgroup, `O_p(G)` = the Sylow `p` of `F(G)`, characteristic. Cheap consolidation.

Recommend (1): it's the next load-bearing piece of the F*(G) lens and reuses this
session's direct-product machinery.

## ⚠️ Environment / process

- **Push is host-side.** No `gh`/`ssh`/GitHub egress in the box. Leave work on the
  named branch; Trevor pushes + opens the PR from the host. (This branch has 7
  unpushed commits incl. the prior session's 2 doc commits.)
- **Full `lake build` OOMs** (low RAM, ~8280 oleans). Always
  `lake build FiniteSimpleGroups.FittingSubgroup` (~10-17s).
- **Verify before claiming green/hashes** — build to a log, `Read` it. Relay was
  STABLE this whole session (no scrambling), unlike the prior one.
- **Pre-commit hook blocks commits to `main`** — fine, we're on a branch.

## 📁 Key files

- `FiniteSimpleGroups/FittingSubgroup.lean` — DONE, axiom-free (~290 lines).
- `docs/fitting-roadmap.md` — step 4 marked DONE with the full proof recipe + both
  gotchas. Read its "DONE — step 4" section for the brick list.
- `HANDOFF.md` — master baseline; I refreshed its banner to "Fitting complete."

---
**→ Next session: the Fitting subgroup is complete and axiom-free. Don't re-open it.
Start a new brick — recommended: `E(G)` / the generalized Fitting `F*(G)` (Next
action #1), reusing the noncommPiCoprod direct-product pattern from this session.**
