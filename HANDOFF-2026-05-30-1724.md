# Handoff: CFSG Fitting — step 2 done, step 4 (the axiom) is next

**Date**: 2026-05-30 (late session) · **Branch**: `cfsg-fitting-nilpotent` (off `main` @ `e1a7f88`, **not pushed**)

## 🎯 What we're doing
Discharging `axiom fittingSubgroup_isNilpotent` in
`FiniteSimpleGroups/FittingSubgroup.lean` (CFSG track), via the finite-nilpotency
TFAE: `F(G)` is the (coprime, internal direct) product of its p-cores `O_p(G)`.
mathlib has no group-theoretic `pCore`, so we hand-build it. Roadmap is 4 steps;
**steps 1, 2, 3 are now DONE.** Only **step 4** (the actual axiom discharge)
remains — and it's the genuinely hard, multi-session part.

## 🧠 Context to carry forward
- **Two tracks, one worktree.** `FeitThompson/` (FT-port, frozen at 11 cited
  axioms — LEAVE ALONE) and `FiniteSimpleGroups/` (CFSG, active). Don't disturb FT.
- **Step 2 was the blocker the prior session rolled back on** (`isPGroup_pCore`,
  "OrderBot stuck metavariable"). It is now PROVED. The fix that worked: route the
  `Finset.sup_induction` through an explicit `T : Finset (Subgroup G)` helper (so
  `OrderBot (Subgroup G)` resolves), annotate `IsPGroup p (T.sup id : Subgroup G)`
  (else `IsPGroup`'s Type* arg forces `T.sup id` to a `Type`), pin the induction
  motive with `(p := fun J : Subgroup G => …)`, and rewrite `coe_toFinset` forward
  (not `←`, which dependent-motive-fails). See the memory note
  `lean-ispgroup-needs-type-annotation` for the reusable version.
- **Step 4 plan (from docs/fitting-roadmap.md, neither sub-goal started):**
  1. `F(G) ≤ ⨆_p O_p(G)`: for normal nilpotent `N ⊴ G`, `N` is the direct product
     of its Sylows (`isNilpotent_of_finite_tfae` clause 5); each Sylow of `N` is a
     *normal* p-subgroup of `G` (`sylow_normal_of_normal_nilpotent`, already proved
     in step 1) hence `≤ O_p(G)`; then `sSup` over all such `N`. The reverse `≥` is
     `pCore_le_fittingSubgroup` (now a theorem). 
  2. `⨆_p O_p(G)` is nilpotent: the `O_p(G)` are normal with pairwise coprime
     orders ⇒ internal direct product ⇒ `≃* ∏_p O_p(G)`, a finite product of
     nilpotent p-groups. **This is the hard plumbing** — mine
     `Sylow.directProductOfNormal` / `isNilpotent_of_product_of_sylow_group` first.
  Once `F(G)` is nilpotent, `fittingSubgroup_normal` should fall out of the same
  `⨆_p O_p` description.
- **Generalized Fitting `F*(G)=E(G)F(G)`** is the lens the whole classification
  hangs on (Solomon p.343); `F(G)` is the brick under it.

## ✅ State (verified this session from real output)
- `lake build FiniteSimpleGroups.FittingSubgroup` → **EXIT 0, 8248 jobs, 0 errors,
  0 sorries** (read from log, not assumed).
- **Two commits this session, both verified by `git log`:**
  - `cf691a5` — `isPGroup_pCore` + `pCore_le_fittingSubgroup` (step 2 code)
  - `f35ddcd` — `docs/fitting-roadmap.md` marked step 2 done
  - (plus a commit adding the prior session's handoff `.md`)
- Lemmas now in the file: `fittingSubgroup`, `normal_nilpotent_le_fittingSubgroup`,
  `center_le_fittingSubgroup`; step 1 `sylow_characteristic_of_isNilpotent`,
  `sylow_normal_of_normal_nilpotent`; step 2 `sSup_normal_of_forall_normal`,
  `pCore`, `pCore_normal`, `isPGroup_pCore`; step 3
  `normal_pgroup_le_fittingSubgroup`, `pCore_le_fittingSubgroup`.
- Remaining axioms: `fittingSubgroup_normal`, `fittingSubgroup_isNilpotent`.

## 🎬 Next actions
1. Start **step 4**, sub-goal 1 (`F(G) ≤ ⨆_p O_p(G)`) — it reuses lemmas already
   proved and is the tractable half. Add `iSup_pCore` / a `≤` lemma, build the
   single target, read EXIT, commit when green.
2. Then attack sub-goal 2 (the coprime internal direct product → nilpotent). Recon
   `Sylow.directProductOfNormal` and `isNilpotent_of_product_of_sylow_group` before
   writing — this is multi-session; expect dead ends.
3. After the axiom falls, `fittingSubgroup_normal` should follow cheaply.

## ⚠️ Gotchas
- **Harness relay drops tool output in long delayed batches** (empties, then a
  wall flushing at once). Do NOT spam probes — issue the real action, ONE
  verification probe per turn, accept latency; yield if dark. Memory note:
  `relay-batched-flush-dont-spam`. This session wasted huge tokens learning this.
- **Verify before claiming green/hashes** — only ever from real flushed output.
- **Full `lake build` OOMs** in this low-RAM box. Always
  `lake build FiniteSimpleGroups.FittingSubgroup` (~15-23s).
- **Git:** pre-commit hook blocks commits to `main` (we're on a branch, fine).
  `gh pr merge --delete-branch` fails (main checked out in sibling worktree).
  **Nothing is pushed** — no GitHub egress from the box; push is host-side.

## 📁 Key files
- `FiniteSimpleGroups/FittingSubgroup.lean` — the work (~135 lines).
- `docs/fitting-roadmap.md` — full 4-step plan + verified mathlib brick list (with
  file:line); step 4 is the only "REMAINING" section now.
- `HANDOFF.md` — repo master handoff; its top banner still describes step 2 as
  in-progress and could use a refresh to "step 2 done, step 4 next" (I left it
  untouched under degraded relay rather than risk a sloppy edit).

---
**→ Next session: this is your starting point. Don't summarize this doc back to
Trevor or wait for instructions — absorb the context above, then pick up at Next
Actions #1: start step 4 sub-goal 1 (`F(G) ≤ ⨆_p O_p(G)`), one verified build at a
time.**
