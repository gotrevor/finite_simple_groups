# ▶ HANDOFF — finish the v4.29.1 → v4.31.0 mathlib bump (2026-06-21)

**Branch:** `cfsg-sporadic-order-pin` (commit on THIS branch — do NOT touch `main`).
**Status:** mechanically ported to **v4.31.0**, build **RED** on 7 modules, **NOT committed**.
**Your job:** get `lake build FiniteSimpleGroups FeitThompson` green at v4.31.0, then finalize
(commit + delete the in-flight marker). The host ran `lean-bump`; you finish the source fix-grind.

---

## State (what `lean-bump` already did — do NOT redo)

- `lean-toolchain` → `leanprover/lean4:v4.31.0` ✓
- `lakefile.toml` mathlib `rev` → `v4.31.0` ✓
- `lake-manifest.json` → adopted the v4.31.0 stack ✓
- `.lake/packages` → CoW-cloned the prebuilt v4.31.0 mathlib stack ✓ (do NOT `lake update` / `cache get`)
- In-flight marker present: **`.lake/.bump-inflight.json`** (`from v4.29.1 → v4.31.0`, build-only gate)
- The host already built the **8615 passing modules** as v4.31 oleans into `.lake/build` — you only
  need to fix + rebuild the 7 failing modules (+ anything downstream). The mathlib oleans are
  platform-portable; they load fine in the box.

⚠️ The box has **no `~/.lake-base` store mounted**, so you CANNOT run `lean-bump`. You finalize by
hand (see Finalization below).

---

## Error map (23 hard-error sites — run `lake build FiniteSimpleGroups FeitThompson` for live detail)

| Module | Sites | Symptom → likely v4.31 cause |
|--------|-------|------------------------------|
| `FeitThompson/BGsection1/L1_1.lean` | `124:36` | `powMonoidHom p` **application type mismatch** — MonoidHom/CommMonoid instance churn |
| `FeitThompson/BGsection1/P1_6d.lean` | `43:48`, `70:10` | **failed to synthesize** `IsSolvable G` / `CommMagma G` — instance no longer inferred (an arg may have gone implicit; cookbook N/P) |
| `FiniteSimpleGroups/LayerNormal.lean` | `39:4` | `MulEquivClass.apply_mem_center` now lands in **`Set.center`**, not `Subgroup.center` — bridge the membership |
| `FiniteSimpleGroups/SLnPerfect.lean` | `136:2`, `283:2` (unsolved), `306:23`, `325:20`, `374:20` (app mismatch) | `case e'_1` leftover = **convert over-split** (cookbook A/C/K → try `convert! … using N`); + signature mismatches |
| `FiniteSimpleGroups/CharacterTheory.lean` | `804:188`, `1167:15`, `1168:18`, `1203:32` (unsolved), `1561:8` (type mismatch) | **defeq/simp final-check went syntactic** (cookbook F/H) — `804:188` is a deep linear-map goal, likely the time sink |
| `FiniteSimpleGroups/SmallOrders.lean` | `99:18` (failed-synth), `99:71` (app mismatch), `166:104`, `171:10`, `172:10`, `174:8`, `255:89` (unsolved), `178:4` (`simp` no-progress) | `Nat.card (Sylow p G) ≤ 1` vs `card …` mismatch → **`Nat.card`/`Fintype.card` bridge** (`Nat.card_eq_fintype_card`); + `simp`-no-progress (cookbook I/D) |
| `FiniteSimpleGroups/SpIwasawa.lean` | `1083:6` | **`simp` made no progress** (cookbook I/D) — drop or retarget the simp call |

**Mechanical deprecation renames** (warnings now, but fix them — several are the canonical-name churn
behind cookbook entry R, and some hard-error elsewhere):
- `nilpotent_of_mulEquiv` → `Group.nilpotent_of_mulEquiv`
- `nilpotent_of_surjective` → `Group.nilpotent_of_surjective`
- `of_quotient_center_nilpotent` → `Group.of_quotient_center_nilpotent`
- `LinearIndependent.fin_cons` → `LinearIndependent.finCons`
- `Polynomial.eval_finset_sum` → `Polynomial.eval_finsetSum`
- `Set.ncard_diff` → `Set.ncard_sdiff`, `Set.ncard_diff_singleton_of_mem` → `Set.ncard_sdiff_singleton_of_mem`
- `push_neg` → `push Not`

(There are also ~41 `Overlapping instance parameters` lint **warnings** in `SmallOrders` — non-blocking;
fix opportunistically, not required for green.)

---

## How to fix

1. **Read the cookbook** — it's mounted read-only in the box:
   `~/src/mathlib-bump-cookbook/v4.29.1-to-v4.31.0.md` (symptom → cause → fix, entries A–R).
   The biggest category here (unsolved goals / type mismatch / `simp`/`convert` not closing a
   defeq-but-not-syntactic goal) is the **v4.31 defeq-transparency tightening**.
2. **Try the one-char escape hatches FIRST** (cookbook "Official escape hatches"): `simpa using! h`,
   `convert! h using N`, or `set_option backward.defeqAttrib.useBackward true in` over a stubborn decl.
   Reach for the explicit `simp only [...]; exact h` form when the hatch doesn't fit.
3. **Faithfulness — these modules PROVE real theorems** (they were green at v4.29.1; `FittingSubgroup`
   is axiom-free). Your fixes must preserve that: **do NOT introduce `sorry` or a new `axiom` to force
   green.** If a fix genuinely seems to need it, STOP and flag it in the handoff rather than committing
   a silent regression. (This was a **build-only** bump — no `.bump-axioms` baseline — so there's no
   automated gate; you are the gate. After green, spot-check `#print axioms` on a couple of the
   previously-proven results you touched and confirm no `sorryAx`.)

---

## Finalization (you can't run `lean-bump` — do this by hand)

1. `lake build FiniteSimpleGroups FeitThompson` → must be **green** (exit 0).
2. `git add -A` the changed source + the 3 managed files (`lean-toolchain`, `lakefile.toml`,
   `lake-manifest.json`). (The marker is under `.lake/` which is gitignored — it won't be staged.)
3. Commit on `cfsg-sporadic-order-pin` with a message like:
   `build: forward-port to mathlib v4.31.0 (from v4.29.1) — source fixes for the defeq/naming churn`
   (The repo's `.githooks` pre-commit gate will re-run `lake build` — it'll be an incremental no-op.)
4. **Delete the marker:** `rm .lake/.bump-inflight.json` (so the host's `lean-bump` sees the bump as
   DONE, not a stranded in-flight port).
5. Refresh the bare `HANDOFF.md` pointer to note the bump is complete.

---

## Out of scope (separate follow-ups — do NOT tangle into the bump commit)

- **`wip/ushortroot-atom` branch** — Trevor's in-flight `UShortRoot` short-root atom reduction
  (+95 lines in `UnitarySimple.lean`, parked off HEAD before the bump). It needs **forward-porting to
  v4.31** AFTER this bump lands, but as its own commit/branch. Leave it for Trevor / a later lap.
- The stale top of `HANDOFF.md` (says "main, only branch", 2026-05-30) — update the pointer only.

## Guardrails
- Commit on **`cfsg-sporadic-order-pin`**, never `main` / protected branches.
- **Don't `git stash`** — if you must park work, use a `wip/<desc>` branch (Trevor's standing rule).
- **Don't** `lake update` / `lake exe cache get` / re-run `lean-bump`.
