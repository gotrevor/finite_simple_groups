# Handoff: PSp(2n,q) simplicity REDUCED to 3 disclosed axioms ◐

**Date**: 2026-06-04 ~09:30 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `08297fb`

## 🎯 What we're doing

Discharging the deep CFSG family axiom `PSp_isSimpleGroup` (LieType.lean) via the **symplectic
Iwasawa criterion**, mirroring the now-closed PSL arc. This lap took PSp from "one membership
brick" to **`PSpn_isSimpleGroup_of_iwasawa` proven modulo exactly THREE disclosed geometric
axioms** — the same shape the SLn arc was in just before its final discharge.

## 🧠 Context to carry forward

- **The reduction is the win.** `SpN.PSpn_isSimpleGroup_of_iwasawa [Nonempty l] : IsSimpleGroup
  (symplecticGroup l F ⧸ center)` has `#print axioms` = `[propext, Classical.choice, Quot.sound,
  sp_transvec_closure_eq_top, commutator_PSp_eq_top, pspQuasiPreprimitive]`. Every *other* Iwasawa
  obligation (MulAction, FaithfulSMul, Nontrivial, is_comm, is_conj, is_generator-modulo-generation)
  is machine-checked and axiom-clean. So the entire remaining PSp problem is those 3 classical
  theorems — nothing else.
- **Architecture = SLn mirror, file-for-file.** I built it by transcribing SLnAction/SLnSimple/
  SLnIwasawa and adapting. The faithfulness "fixes every line ⇒ scalar" argument transcribes
  VERBATIM (it's generic in the linear action); only the endgame differs (Sp has no mathlib
  `mem_center_iff`, so I used `Matrix.scalar_commute` + the conjugation lemma for the converse).
  See memory `[[math-fixes-every-line-scalar-action-generic]]`.
- **The genuinely-symplectic content** is: `spForm_self` (ω alternating, char-2-safe via block
  structure), `spTransvection_conj` (proved locally — superseded a flaky Aristotle job), and
  `sp_center_fixes_line` (central ⇒ fixes lines, via conj + a rank-one extraction). The rest is
  plumbing.
- **Aristotle**: the `spconj` job is now IDLE/superseded (I proved it locally). At lap end a
  *foreign* session's job (`aristotle-singbound`) was RUNNING — do NOT submit (would preempt it;
  corpus rule). The generation axiom is the analogue of an Aristotle-solved SL brick BUT has no
  mathlib symplectic infra → corpus says likely walls. Submit it only when the shared key is idle.
- **Decision log**: I deliberately did NOT try to discharge the LieType `PSp_isSimpleGroup` axiom
  itself (specializing to `Fin n, ZMod q` + wiring) this lap — the 3 sub-axioms still gate it and
  the field-size/`n≥2` hypotheses need care. Left as a clean later step once the 3 are real.

## ✅ State (12 commits this lap, every one a green full build = 8307 jobs)

New files, all axiom-clean except the 3 disclosed axioms:
- `SpTransvection.lean` — transvection def, `spForm_self`, `_mem`, `_det`, `_mul`, `_conj`
  (is_conj core), `_mulVec` (geometric action `τ(w)=w+c·ω(w,v)·v`), `_smul_vec` (line reparam),
  `spForm_skew` (ω(u,w)=-ω(w,u)); group-level `spTransvecSp`/`spTransvecHom`/`spTransvecGroup`
  (abelian) / `spTransvecGroup_conj`.
- `SpAction.lean` — `mulVec` action of Sp on `(l⊕l)→F` + projective action on ℙ²ⁿ⁻¹.
- `SpSimple.lean` — faithfulness both directions (`eq_scalar_of_fixes`, `sp_mem_center_of_smul_eq`,
  `sp_center_fixes_line`), `pspPermHom`/`pspAction`/`pspFaithful`, `PSp_nontrivial`,
  `spTransvecGroup_smul`/`_rep` (line-invariance).
- `SpIwasawa.lean` — `Tline`, `Tline_iSup`, `pspIwasawaStructure`, the 3 axioms,
  `PSpn_isSimpleGroup_of_iwasawa`, and `spTransvection_maps_of_form_ne` (first generation brick).
All wired into `FiniteSimpleGroups.lean`. `PENDING_WORK.md §F` rewritten with full status + paths.

## 🎬 Next actions

1. **Start the generation axiom `sp_transvec_closure_eq_top`** via `sp_transitive_on_vectors`
   (Sp transitive on nonzero vectors). The non-orthogonal case is DONE
   (`spTransvection_maps_of_form_ne`). Do the **orthogonal-case bridge**: for `u,w≠0` with
   `ω(u,w)=0`, find `z` with `ω(u,z)≠0 ∧ ω(z,w)≠0`, then compose two transvections. The `z`-existence
   needs "two hyperplanes don't cover a vector space" — check mathlib (`Subspace`/covering) first;
   `spForm_skew` + non-degeneracy of `ω` (J invertible) are the inputs.
2. Then the Eichler/Witt induction: transvections transitive on hyperbolic pairs ⇒ generate Sp.
   This is the multi-lap core; decompose narrowly.
3. In parallel-ish: `commutator_PSp_eq_top` reduces (given generation) to "each transvection is a
   commutator in Sp(2n), n≥2" — a symplectic Steinberg relation (analogue of
   `SLn.transvecSL_mem_commutator`; type Cₙ).
4. `pspQuasiPreprimitive`: transitivity-on-points comes free from action 1; primitivity needs the
   maximal-parabolic / isotropic-line-stabilizer argument (NOT 2-transitivity — Sp preserves ω).

## ⚠️ Gotchas

- Full `lake build` can **OOM transiently** ("Cannot allocate memory" loading a Mathlib olean)
  under the 6-core taskset; the "ring failed" lines that accompany it are collateral, not real.
  Just **retry the commit** — it passed on retry this lap. (corpus: `lean-box-oom-...`)
- Iterate with `lake build FiniteSimpleGroups.SpIwasawa` (the leaf, ~10-20s); the pre-commit hook
  runs the full taskset build and IS the green gate.
- When building an `IwasawaStructure` record whose fields `letI := pspAction`, the record `def`
  MUST carry `[Nonempty l]` or you get a MISLEADING `show ... not defeq` on the action (memory
  `[[lean-letI-action-def-needs-instance-binder]]`).
- `mul_smul` is ambiguous under `open Matrix`; use `smul_smul` (memory entry flushed).

## 📁 Key files
- `FiniteSimpleGroups/SpIwasawa.lean` — the assembly + 3 axioms + first generation brick. START HERE.
- `FiniteSimpleGroups/SpTransvection.lean` — all transvection algebra (`_mulVec`, `_conj`, `spForm_*`).
- `FiniteSimpleGroups/SpSimple.lean` — faithfulness; template for any further center/scalar work.
- `FiniteSimpleGroups/SLnIwasawa.lean` — the DONOR to mirror for the remaining generation/Tline work.
- `PENDING_WORK.md` §F — authoritative status + attack paths for the 3 axioms.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb
the context, then start at Next action #1: build `sp_transitive_on_vectors` (orthogonal-case
bridge) toward discharging `sp_transvec_closure_eq_top`. The non-orthogonal half is already done.**
