# Handoff: PSU(n,q) simplicity — `hgen` generation-induction infrastructure built

**Date**: 2026-06-04 17:59 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `4d06400`

## 🎯 What we're doing
Discharging the deep CFSG axiom `PSU_isSimpleGroup` by proving `PSUConcrete n p` simple via Iwasawa.
The reduction is COMPLETE down to a **single atom**: `hgen` — *the isotropic unitary transvections
generate `SU_n(F_{p²})`* (n≥3). Capstone `PSU_isSimpleGroup_modulo_generation` (`UnitarySimple.lean
§Iwasawa`) is axiom-clean modulo `hgen` as a hypothesis. This lap built the scaffolding for the
`hgen` Dieudonné/Eichler **dimension induction** (the unitary analogue of the symplectic
`genAux_le`, which is the EXACT template — `SpIwasawa.lean:680-733` proves Sp generation fully).

## 🧠 Context to carry forward (read this — it's the map)
- **The template is symplectic `genAux_le`** (`SpIwasawa.lean`): induct on `Sᶜ.card`, peel standard
  hyperbolic pairs indexed by `Finset S`, `FixS S g` = fixes S-pairs, complement = coordinate
  `offS`. The unitary version follows the SAME shape but is HARDER for two reasons:
  1. **Isotropy.** Symplectically every vector is a valid transvection centre; unitarily centres
     must be isotropic, so the within-complement transitivity uses **Eichler moves** (transvection
     products), not single transvections. We already have these Eichler engines (see State).
  2. **The standard basis is anisotropic** for the identity Hermitian form, so the hyperbolic basis
     is NOT the coordinate basis. BUT the explicit hyperbolic pair in coordinates `{2i,2i+1}` SPANS
     the same coordinate subspace, so `offS` (= coordinate-vanishing on `∪{2i,2i+1}`) is still the
     right clean predicate. `exists_coord_hyperbolic_pair` is the basis building block.
- **★ THE KEY OBSTRUCTION (flushed to KB `[[unitary-su-generation-scalar-det-obstruction]]`):** a
  line move only gives `e ↦ c·e'` with `c` in a 1-dim `F_q`-coset (generally NOT `F_q*`). Fixing `c`
  needs `F_{q²}*` scaling. But **"scale `e` by `λ∉F_q` fixing `⟨e,f⟩^⊥` pointwise is IMPOSSIBLE in
  SU"** (det = λ/star λ ≠ 1). So det-compensation must live in the un-fixed complement → the
  induction must fix only the SMALL `S`, never the whole perp (exactly as `offS_transvecGen_maps_pair`
  does). My `exists_su_maps_nonorth_fixing_perp` (fixes WHOLE perp) is therefore too strong for the
  pair-mapping step — only usable for `F_q`-valued within-complement moves.
- **★ SU(2)-of-a-hyperbolic-plane ≅ SL_2(F_q)** (flushed to `[[unitary-weyl-swap-fq-torus]]`). The
  within-plane torus reachable by transvections is exactly `F_q*` (built it: `exists_su_weyl_swap`,
  `exists_su_hyperbolic_scale`). The deepest base case connects to the repo's SL_2 generation thread
  (`SL2.lean`, `transvecSL`). This is the cleanest framing for the genuinely-hard small case.
- **hT1/hT2/hPP are ALL discharged** — do NOT reopen them. The "needs Witt's extension theorem"
  diagnosis from the old `ugen`/`t1` Aristotle runs was WRONG for those (explicit Eichler matrix
  `uEichler`). Generation `hgen` is the only real remaining wall and IS genuinely deep (multi-lap).

## ✅ State (8 commits this lap, every one a verified green `lake build` = 8312 jobs)
All new lemmas `#print axioms`-clean (`[propext, Classical.choice, Quot.sound]`), verified in-kernel.
New infrastructure (`UnitarySimple.lean` general section + §Concrete; `UnitaryTransvection.lean`):
- **Perp block:** `uForm_nondegenerate`, `uPerpComp`/`uPerpComp_mem_perp`/`uPerpComp_add_span`,
  `uPerp_form_ne_of_mem_perp`, `exists_uPerp_form_both_ne`.
- **Pair bricks:** `u_fixing_preserves_perp`, `uTransvection_fixes_pair` (UnitaryTransvection.lean).
- **Eichler transitivity engines:** `exists_su_maps_nonorth_fixing_perp` (isotropic `v↦c·w`, fixes
  `⟨v,w⟩^⊥`), `exists_su_fixes_maps_isotropic_mate_fixing_perp` (EXACT mate `y↦y'`, fixes `⟨x,y,y'⟩^⊥`).
- **Weyl + torus:** `exists_su_weyl_swap`, `exists_su_hyperbolic_scale` (F_q* torus).
- **Det base case:** `u_eq_one_of_fixes_all_but_one` (+ `u_preserves_form'`) — Aristotle-solved
  (project `c861683e`), re-verified in-kernel, ported.
- **Basis seed:** `exists_coord_hyperbolic_pair`.
- Pre-existing & still valid: `exists_su_fixes_maps_nonorth`, `exists_su_fixes_maps_isotropic_mate`
  (hT1 engine), `exists_su_maps_isotropic` (line transitivity), `exists_hyperbolic_partner`.

**Aristotle:** `c861683e` (base case) DONE+ported. `90aa26a2` (exact-transitivity assembly) DONE +
verified in-kernel (depends only on the 3 inlined axioms `exists_move`/`exists_partner`/`exists_scale`)
— confirms the scalar-cancellation assembly is correct, but it's SU-level resting on the `exists_scale`
axiom, so NOT directly usable for generation (need a T-level / transvection-product version). Do NOT
port it. The real blocker it exposes is **`exists_scale` = F_{q²}* line-stabiliser scaling AS A
TRANSVECTION PRODUCT** (n≥3 essential, third-dimension det balance) — that's the next deep target.

## 🎬 Next actions
1. **Poll `90aa26a2`** (`aristotle tasks 90aa26a2-55c1-4a6f-9ff9-febd52d2b98c`). If done, download
   (`aristotle download <id> --destination /tmp/x.tgz` → it's a **gzip tarball**, `tar xf`), verify
   in-kernel + `#print axioms`. Port only if cheap (see caveat above). Then **submit the next brick**
   to keep Aristotle busy — good candidate: `SU(2)`-of-hyperbolic-plane Bruhat decomposition (every
   plane-SU element = product of `τ_{e,·}, τ_{f,·}`, using the Weyl + torus + unipotents we have).
2. **Build the genAux scaffolding** (the real multi-lap core), mirroring `SpIwasawa.lean:460-733`:
   define `offSU S` (coordinate-vanishing on `∪_{i∈S}{2i,2i+1}`) + closure lemmas; the hyperbolic
   basis from `exists_coord_hyperbolic_pair`; `offS`-transitivity via the Eichler engines + Weyl/torus
   (fixing only S — see KEY OBSTRUCTION); the `genAux`-style induction; assemble to `hgen`.
3. The hard sub-piece is the **F_q-scalar handling within a plane via SL_2(F_q)** (base case). Reuse
   `exists_su_hyperbolic_scale` (F_q* torus) + `exists_su_weyl_swap`. For the cross-plane scalar `c`,
   absorb it by movement in the complement (NOT by fixing the perp).

## ⚠️ Gotchas
- `field_simp` often CLOSES a field-arithmetic goal fully → a trailing `; ring` then errors "no
  goals". Drop the `ring`.
- `rw` can leave a syntactically-`X=X` goal UNCLOSED when the two sides differ by a coercion
  instance (e.g. `Submonoid.coe_mul` mul vs `Matrix.instMul`). Use `simp only [...]` instead of `rw`.
- `aristotle download` writes a **gzip tarball AT the `--destination` path** (pass a FILENAME, not a
  dir; then `tar xf`). Already in the KB corpus (`reference_aristotle_cli.md`) — grep it first.
- Transient box OOM during pre-commit `lake build` is memory pressure, not a real failure — re-run.

## 📁 Key files
- `FiniteSimpleGroups/UnitarySimple.lean` — START HERE. General section (perp block, Weyl, torus,
  base case, transitivity engines) + §Concrete (geometry) + §Iwasawa (capstone
  `PSU_isSimpleGroup_modulo_generation`).
- `FiniteSimpleGroups/SpIwasawa.lean:460-733` — the EXACT template for the genAux generation induction.
- `FiniteSimpleGroups/UnitaryTransvection.lean` — `uTransvection`/`uEichler`/`uTransvecSU` + pair bricks.
- `PENDING_WORK.md §G` — authoritative PSU status; the `★★★ UPDATE 2026-06-04` block has the full plan.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (PSU
generation `hgen`). Absorb the Context (esp. the KEY OBSTRUCTION and the SU(2)≅SL_2 framing), then
pick up at Next action #1: poll `90aa26a2`, then start the genAux scaffolding (#2) — that IS the
remaining work.**
