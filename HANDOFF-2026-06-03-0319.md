# Handoff: Burnside `pᵃqᵇ` — ingredients 2 & 6 cracked, Lemma A in, ingredient 3 (column orthogonality) scoped

**Date**: 2026-06-03 03:19 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `170d11e`

## 🎯 What we're doing

Discharging the one sharp axiom `isSimpleGroup_centralizer_index_not_primePow` (`Burnside.lean`) —
*Burnside's prime-power class-size lemma* — into a real proof. It rests on 6 classical character-theory
ingredients. **After this lap, only ingredient 3 (column orthogonality) is missing**; everything else
is machine-checked and `#print axioms`-clean. All work is in `FiniteSimpleGroups/CharacterTheory.lean`.

## 🧠 Context to carry forward

- **The big unlock this lap**: ingredient 2 (central-character integrality, `[G:C_G(g)]·χ(g)/χ(1) ∈ ℤ̄`)
  was thought to need class-algebra machinery mathlib lacks. It does NOT. **Every element of `ℤ[G]` is
  integral over `ℤ`** (module-finite over `ℤ`), so the class sum is integral for free — no structure
  constants. Centrality of the class sum is only needed for Schur (to make `ρ(z_C)` a scalar). This
  collapsed it to ~40 lines (`classSize_char_isIntegral` / `centralizerIndex_char_isIntegral`). See
  the memory note `[[group-algebra-elements-integral-over-Z]]`. NB `ℂ[G]` is noncommutative so
  `integralClosure ℤ ℂ[G]` won't typecheck — push from `ℤ[G]` via `MonoidAlgebra.mapAlgHom`.
- **Schur in mathlib** = `Representation.IsIrreducible.algebraMap_intertwiningMap_bijective_of_isAlgClosed`
  + `IntertwiningMap.algebraMap_apply` (`= c • 1`). Fully-qualify `Representation.IntertwiningMap`
  (`open Representation` doesn't bring it into scope inside a namespace). `Basis` is now `Module.Basis`.
- **The host answered the open-web ask** (read `ON-LINE-FINDINGS-2026-06-03-burnside-paqb.md` — it's
  gold): ingredient 3 is the sole gap; **mathlib HAS the Wedderburn backbone**
  (`IsSemisimpleRing.exists_algEquiv_pi_matrix_of_isAlgClosed`: `ℂ[G] ≃ₐ ∏ Mₐᵢ(ℂ)`); **Route B
  (regular character) is recommended**; Coq mathcomp `integral_char.v` is the decl-for-decl port
  blueprint (`second_orthogonality_relation`, `NirrE : #Irr = #classes`, `primes_class_simple_gt1`
  = our axiom). No Lean/Isabelle equivalent exists — this repo would be the first Burnside `pᵃqᵇ` in Lean.
- **Route B plan for ingredient 3**: `χ_reg = ∑_{χ∈Irr} χ(1)·χ`, then `χ_reg(g)=|G|·[g=1]` ⇒
  `∑_χ χ(1)χ(g)=0` for `g≠1`. Sub-lemma 1 (`χ_reg(g)=|G|·[g=1]`) and the inner-product computation are
  DONE. The hard remaining piece is **the finite family `Irr(G)`** — index a complete, duplicate-free
  set of irreducible characters as functions `G→ℂ` (use the Wedderburn `∏Mᵢ` factors as the index;
  `sum_sq_dim_eq_card` already extracts `n=#Irr` and `∑dᵢ²=|G|`), plus the semisimple decomposition
  "character of a rep = ∑(multiplicity)·(irred char)" with multiplicity `=⟨χ_reg,χ⟩=χ(1)`. This is a
  genuine fresh-lap-sized build but now well-scoped, not from-scratch.

## ✅ State (all verified green this session)

12 commits this lap (`9e00afc..170d11e`), every one `lake build` green (8297 jobs, gated by pre-commit)
and `#print axioms` = `[propext, Classical.choice, Quot.sound]`. Ingredient status:
- (1) `Representation.character_isIntegral` ✅ (prior lap)
- (2) `centralizerIndex_char_isIntegral` ✅ (this lap) + `classSum`/`classSum_isIntegral`/
  `classSum_central`/`trace_asAlgebraHom_classSum`/`exists_scalar_isIntegral_of_central`/
  `filter_isConj_card_eq_index` (class size = centralizer index)
- (3) column orthogonality ⛔ — Route-B bricks built: `character_leftRegular_eq` (`χ_reg(g)=|G|[g=1]`),
  `sum_character_leftRegular_mul`, `sum_sq_dim_eq_card` (Wedderburn `∑dᵢ²=|G|`). Missing: `Irr(G)` family + decomposition.
- (4) Kronecker ✅ mathlib · (5) `not_isIntegral_neg_inv_prime` ✅
- (6) `not_isScalar_of_isSimpleGroup_of_nonabelian` ✅ (this lap — final contradiction)
- **Vanishing lemma** `burnside_vanishing_core` ✅ (Aristotle `8451c8e2`, verified + ported this lap)
- **Scalar step** `eq_of_norm_sum_eq_card` (analytic equality case) ✅; the full matrix bridge is OUT at Aristotle.

## 🎬 Next actions

1. **Poll Aristotle** (`aristotle list` — one-shot, NOT `aristotle show`/the TUI). Job
   `e66a25d1-6646-4527-a08c-8e98ad175446` (`scalar_job`) is grinding the scalar bridge
   `matrix_scalar_of_pow_eq_one_of_norm_trace_eq` (`M^n=1`, `‖tr M‖=d ⇒ M=ζ•1`). If IDLE/done:
   download, **verify in our v4.29.1 kernel + `#print axioms` clean** (replace the inlined
   `axiom eq_of_norm_sum_eq_card` with the real in-repo lemma), port, then wrap to the Representation
   level (`‖ρ.character g‖=finrank ⇒ ∃c, ρ g = algebraMap ℂ (End ℂ V) c`). Full instructions in
   `ARISTOTLE-JOB-burnside-vanishing.md` §Job 2. **Then immediately submit the next brick.**
2. **Build ingredient 3 via Route B** (`PENDING_WORK.md §A`): the `Irr(G)` finite family from the
   Wedderburn factors + the regular-rep decomposition `χ_reg = ∑_χ χ(1)·χ`. This is THE remaining gap.
3. Once 3 + the scalar bridge land, **assemble the sharp axiom** (the `-1/p` argument — chain spelled
   out in `ON-LINE-FINDINGS-2026-06-03-burnside-paqb.md` "Final assembly" and `PENDING_WORK §A`).

## ⚠️ Gotchas

- **OOM-thrash**: `lake build` / `lake env lean` intermittently errors `Cannot allocate memory` on an
  unrelated mathlib module. Just retry (I looped 2–3×). Not a real failure.
- **`rw` auto-closes via rfl**: a trailing `ring`/`pow_two` after a `rw` chain that already closed the
  goal errors `No goals to be solved` — drop it (e.g. `hbound_sum` needed no `ring`).
- Aristotle uses toolchain v4.28; we pin v4.29.1. Always re-verify returned proofs (Lemma A ported
  with zero drift, but check). Submit a minimal project dir (copy `lean-toolchain` + `lakefile.toml` +
  `lake-manifest.json` + a `Target.lean`); it `lake exe cache get`s mathlib — the toolchain/`.lake`
  warnings on submit are expected and harmless.

## 📁 Key files

- `FiniteSimpleGroups/CharacterTheory.lean` — all the char-theory bricks; top docstring = 6-ingredient board.
- `FiniteSimpleGroups/Burnside.lean` — the sharp axiom + machine-checked `burnside_simple`.
- `ON-LINE-FINDINGS-2026-06-03-burnside-paqb.md` — host's answer: Route B, mathlib Wedderburn, mathcomp blueprint. **Read this.**
- `PENDING_WORK.md §A` — axiom inventory + ingredient-3 Route A/B and the final assembly chain.
- `ARISTOTLE-JOB-burnside-vanishing.md` — Lemma A (done) + scalar-bridge job `e66a25d1` (in flight) port instructions.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context, then start at Next action #1: poll Aristotle (`aristotle list`); if `e66a25d1` is done,
download + verify + port the scalar bridge; otherwise build ingredient 3 Route B per `PENDING_WORK §A`.**
