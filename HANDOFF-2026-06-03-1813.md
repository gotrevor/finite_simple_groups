# Handoff: Burnside `pᵃqᵇ` — COMPLETE (fully machine-checked, zero custom axioms)

**Date**: 2026-06-03 18:13 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `5852d99`

## 🎯 What we're doing

The lap's thread was discharging the sharp axiom `isSimpleGroup_centralizer_index_not_primePow`
(Burnside's prime-power class-size lemma) into a real proof. **This is now fully done.** The entire
Burnside `pᵃqᵇ` character-theoretic core is machine-checked:

- `ProofStrategy.Burnside_paqb` (the genuine theorem: `Nat.card G = p^a·q^b ⇒ IsSolvable G`)
- `Burnside.burnside_simple`, `Burnside.isSimpleGroup_centralizer_index_not_primePow`

all have `#print axioms` = `[propext, Classical.choice, Quot.sound]` — **no custom axioms** (verified
this session via `lake env lean` on a scratch `#print axioms` file). First Burnside `pᵃqᵇ` in Lean.

## 🧠 Context to carry forward

This lap closed all three former "gaps" of the endgame. Mental model of how the proof fits, in
`FiniteSimpleGroups/CharacterTheory.lean` (one file, `import Mathlib`):

- **Route B (regular character)**: `exists_wedderburn_character_decomp` gives, via Artin–Wedderburn
  `ℂ[G] ≃ₐ ∏ᵢ Mₐᵢ(ℂ)`, `χ_reg(g) = ∑ᵢ dᵢ·trace(Rᵢ g)` where `Rᵢ g := (e (single g 1))ᵢ`. It now
  ALSO outputs the surjective algebra homs `Ψᵢ = πᵢ∘e` and the **unique trivial factor index `i₀`**.
- **Gap 1** (each `Rᵢ` irreducible): `isIrreducible_of_surjective_algHom` — transfer simplicity from
  the natural matrix module along surjective `Ψᵢ` via `LinearMap.isSimpleModule_iff_of_bijective`.
  Solved locally; Aristotle jobs `fe2dc954`/`09bdf796` were SUPERSEDED (don't port them).
- **Gap 2** (scalar bridge): `matrix_scalar_of_pow_eq_one_of_norm_trace_eq` (ported from Aristotle
  `e66a25d1`) wrapped as `matrix_trace_zero_or_scalar` (`trace=0 ∨ M scalar`).
- **Gap 3** (`T=1`, the `+1`): existence `exists_trivial_factor` (augmentation/idempotent, ported
  from Aristotle `a3e3d823`) + **uniqueness `trivial_factor_unique` proved locally** by the
  averaging-idempotent argument: `w = |G|⁻¹∑g` is idempotent, each Wedderburn component `(e w)ₖ` is
  an idempotent matrix with nonneg-integer trace `rₖ` (`matrix_idempotent_trace_natCast` via
  `IsProj.trace`), the weighted traces sum to 1 (= `|G|⁻¹∑_g χ_reg(g)`), yet each trivial factor
  contributes `dₖ·rₖ = dₖ² ≥ 1`, so two would force the sum ≥ 2. (Aristotle "gap3u" `3f459e3a` was
  submitted as a backup but is now SUPERSEDED.)
- **Endgame** `burnside_class_size`: `0 = χ_reg(g)` for `g≠1`; the unique trivial factor gives `+1`,
  every other factor a multiple of `p` (`wedderburn_factor_char_vanish` for `p∤dᵢ`; `p·(dᵢ/p)·χᵢ(g)`
  for `p|dᵢ`) ⇒ `0 = 1 + pΘ` ⇒ `Θ = -1/p ∈ ℤ̄`, contradicting `not_isIntegral_neg_inv_prime`.

**Why this is genuinely done, not just "a milestone"**: I inventoried every remaining open `axiom`
in the repo (see `PENDING_WORK §B`). They are ALL deep/intended-permanent — the CFSG milestones
(`B_theorem`, `aschbacher_dichotomy`, `Feit_Thompson_odd_order`, …; decades-long), family simplicity
(`PSL/PSU/PSp/POmega`, the 10 exceptionals, `Co1/2/3`; mathlib's job, do-not-derive on shared tree),
and `alternatingGroup_isSimple` (bucket C, delete-when-PR-#36524-passes). There is no *tractable*
next axiom. This is not fixation on a blocked thread — the thread is finished.

## ✅ State

13 commits this lap (`96a3c43..5852d99`), every one `lake build` green (8297 jobs, pre-commit-gated).
All new theorems `#print axioms`-clean. Working tree clean. The 5 harvested `ON-LINE-FINDINGS-*.md`
are archived under `archive/findings/`; no `ON-LINE-REQUEST.md` (nothing open). The host's mid-lap
findings validated exactly the routes used (gap-1 `isSimpleModule_iff_of_bijective`, gap-3
augmentation "Option D").

## 🎬 Next actions

The chosen thread (Burnside) is complete. So:
1. **Confirm green + axiom-clean once** (sanity after fresh boot): `lake build` (expect 8297 jobs),
   then `#print axioms FiniteSimpleGroups.Burnside_paqb` (expect only propext/Classical.choice/
   Quot.sound).
2. **There is no tractable open axiom left.** Do NOT start on `PENDING_WORK §B` items (CFSG
   milestones / family simplicity) — those are decades-long or mathlib-port-only and explicitly
   out of scope for a lap. If Trevor wants more, the honest options are: (a) a `/wrap` to mine this
   lap's Burnside formalization into the KB (recommended — it's rich), (b) tidy/`/simplify` the new
   ~500 lines of `CharacterTheory.lean` (low value, build is clean), or (c) a genuinely new thread
   Trevor names.
3. If picking up anyway, the one optional cleanup with real merit: the host findings note gap-1
   could use the mathlib instance `IsSimpleModule (Module.End ℂ V) V` directly instead of the
   Aristotle-ported `isSimpleModule_natural_matrix` — a small, safe simplification, not required.

## ⚠️ Gotchas

- **Don't re-port superseded Aristotle jobs** `fe2dc954`/`09bdf796`/`3f459e3a` — they're solved
  locally and better. Only `8451c8e2`/`eed8a149`/`dc41262f`/`e66a25d1`/`a3e3d823` were used.
- **OOM-thrash**: `lake build`/`lake env lean` intermittently `Cannot allocate memory` on an
  unrelated mathlib olean — just retry.
- Aristotle pins v4.28, we pin v4.29.1 — always re-verify returned proofs in our kernel + `#print
  axioms` before trusting (did this for every port).
- `set_option ... in` must go BEFORE a docstring, not between it and the theorem (parse error).

## 📁 Key files

- `FiniteSimpleGroups/CharacterTheory.lean` — ALL the character theory (~1500 lines); top docstring
  = the 6-ingredient board (now all ✅). Capstone `burnside_class_size`.
- `FiniteSimpleGroups/Burnside.lean` — `isSimpleGroup_centralizer_index_not_primePow` (now a theorem
  = `burnside_class_size`), `burnside_simple`.
- `FiniteSimpleGroups/ProofStrategy.lean` — `Burnside_paqb` (top-level theorem).
- `PENDING_WORK.md` — §A Burnside COMPLETE; §B the do-not-attempt deep axioms.
- `ARISTOTLE-JOB-burnside-vanishing.md` — job ledger (which ported, which superseded).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread, and it is
DONE. Boot, run Next action #1 (confirm `lake build` green + `#print axioms Burnside_paqb` clean),
and report that the Burnside `pᵃqᵇ` thread is fully machine-checked with zero custom axioms. Then —
and only then — surface the Next-action-#2 options (a `/wrap` is the strong recommendation) for
Trevor to choose. Do NOT silently start cracking a `PENDING_WORK §B` deep axiom.**
