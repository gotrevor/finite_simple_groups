# Handoff: `<60` simple-group classification CLOSED (sorry-free) + pre-commit OOM fixed

**Date**: 2026-06-01 00:30 UTC · **Branch**: `cfsg-wielandt` (HEAD `cb42a73`)

## 🎯 What we're doing
Finishing the order-`<60` classification in `FiniteSimpleGroups/SmallOrders.lean`:
**every finite simple group of order < 60 has prime order** (equivalently, A₅ at
60 is the smallest non-abelian one). This was bucket-D's only open item in the
CFSG scaffold. As of this session it is **fully proved, sorry-free**.

## 🧠 Context to carry forward
- This continues the lt60/o30 Aristotle work. Aristotle (`lt60` job) produced the
  Sylow-counting *toolkit* + `no_simple_group_order_56` and left order-30 + the
  main theorem as sorries. I assembled order-30 by hand from its proven counting
  lemma (parent commit `22aa823`), then **hand-wrote the remaining 14 mixed orders
  + 25 prime-power/pq wrappers + the interval_cases dispatch** this session.
- **Two proof patterns** cover every mixed order, both reusing the toolkit:
  (a) "a Sylow count is forced to 1" → `omega` over `n_p ∣ |G| ∧ n_p ≡ 1 [MOD p]`,
  then `false_of_card_sylow_eq_one_of_not_isPGroup` (orders 18,20,28,40,42,44,45,
  50,52,54); (b) the faithful-action embedding `|G| ∣ n_p!` is violated for the
  surviving `n_p>1` (orders 12,24,36,48, via `card_dvd_factorial_of_card_sylow_gt_one`).
  Orders 30,56 use element-counting (already in `22aa823`).
- **The one real bug** (now a KB insight): in the final `interval_cases n <;> first | … `,
  having `norm_num` (the prime closer) FIRST silently broke every composite case —
  `norm_num` reduces `Prime <composite>` to `⊢ False` and "succeeds", so `first`
  never reaches the matching lemma. Fix: primality `norm_num` goes **last**.
- **Aristotle's o30 job did finish sorry-free (320 lines)** but it's moot — my
  hand-assembly landed first and is integrated. Don't bother porting o30.

## ✅ State (verified this session via tagged `PROBE_*` greps)
- HEAD `cb42a73`. **4 commits ahead of `f7478b1`**, all unpushed (box has no
  egress — host pushes):
  - `22aa823` order-30/56 element-counting (sorry-free)
  - `8fc2958` ⚠️ BROKEN pre-commit hook (`lake build -j 6`; Lake 5.0.0 has no `-j`)
  - `cb42a73` hook fix (taskset) + the main-theorem close — **wait, see gotcha**
- `prime_card_of_simpleGroup_card_lt_sixty` builds **sorry-free**; full `lake build`
  green (`PROBE_FULL exit=0`); `#print axioms` = propext/Classical.choice/Quot.sound
  + native_decide, no sorryAx, no custom axioms.
- Pre-commit OOM **fixed**: hook now wraps build in `taskset -c 0-5` (cap via
  `PRECOMMIT_LAKE_JOBS`, default 6); verified the gate ran taskset + passed
  (`PROBE_HF gate_ok=1, oom=0`).

## 🎬 Next actions
1. **Answer Trevor's pending question** (this is where the conversation paused):
   should the broken intermediate commit `8fc2958` (the `-j 6` hook that errors)
   be **squashed** into `cb42a73` before he pushes from the host? My recommendation
   was squash (two halves of one fix; the middle is non-functional), but it's his
   call and needs a host-side interactive rebase (the box can't rebase-push).
2. If he wants it: give him the exact `git rebase -i f7478b1` / squash recipe to
   run on the host. Don't attempt push/rebase-push from the box.
3. Optional: propagate the **taskset** hook fix to the sibling Lean repos
   (bounded_gaps, sum-product, …) that share the same in-repo `.githooks` pattern —
   only if OOM recurs there; ask before touching other repos.

## ⚠️ Gotchas
- **`git status` shows the main-theorem close as part of `cb42a73`** (I committed
  the hook fix + the SmallOrders close together because the staged whitespace
  newline forced the gate). So the sorry-free theorem and the hook fix are in the
  SAME commit `cb42a73`, NOT separate. If squashing, fold `8fc2958` into `cb42a73`.
- **Relay was badly scrambling output this whole session** — batched/out-of-order
  flushes twice made an *aborted* commit look landed. Trust only freshly-tagged
  `PROBE_xxx >>> … <<<` scalar greps, never raw build/commit echoes. (KB:
  [[lean-box-oom-and-scrambled-relay-false-green]].)
- Lake 5.0.0 has **no** `-j`/`--jobs` flag and no jobs env var. Cap parallelism
  with `taskset`, not a lake flag.
- `.githooks/` is now tracked (was untracked before this session).

## 📁 Key files
- `FiniteSimpleGroups/SmallOrders.lean` — the whole `<60` proof; theorem at the
  bottom (`prime_card_of_simpleGroup_card_lt_sixty`).
- `.githooks/pre-commit` — taskset-capped build gate; `PRECOMMIT_LAKE_JOBS` override.
- `CFSG_MAP.md` — bucket classification; this closes bucket D's `<60` item.
- `HANDOFF-2026-05-31-1745.md` — prior handoff (lt60/o30 state).

---
**→ Next session: this is your starting point. Don't summarize this doc back to
Trevor or wait for instructions, and don't offer other projects from the KB — this
doc IS the chosen thread. The work itself is DONE and verified; the live item is
Next action #1: Trevor's squash-or-leave decision on commit `8fc2958` before he
pushes. Lead with that.**
