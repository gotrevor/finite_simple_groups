# Handoff: PSp(2n,q) simplicity — 3 axioms → 2 cores; generation toolkit COMPLETE

**Date**: 2026-06-04 ~11:30 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `517fcff`

## 🎯 What we're doing

Discharging the deep CFSG-family axiom `PSp_isSimpleGroup` (LieType.lean) via the symplectic
Iwasawa criterion. The route is `SpN.PSpn_isSimpleGroup_of_iwasawa` — a fully-assembled Iwasawa
proof of `IsSimpleGroup (symplecticGroup l F ⧸ center)` modulo disclosed geometric axioms. **This
lap took it from 3 disclosed axioms to 2 genuine irreducible cores**, and built the *entire*
geometric toolkit for one of them.

## 🧠 Context to carry forward

- **The 16-commit arc this lap.** Started: 3 axioms `[sp_transvec_closure_eq_top,
  commutator_PSp_eq_top, pspQuasiPreprimitive]`. Now `#print axioms PSpn_isSimpleGroup_of_iwasawa`
  = `[propext, Classical.choice, Quot.sound, psp_isTrivialBlock_of_isBlock, sp_stab_hyperbolic_le]`.
  VERIFIED via `lake env lean` this session.
- **Perfectness DISCHARGED** (now a theorem). The deep "PSp perfect" axiom was replaced by the
  elementary `sp_scaling_exists` (∀v≠0,λ≠0 ∃g∈Sp: g·v=λv), which was THEN ALSO discharged
  (`spDiag λ = fromBlocks (λ•1) 0 0 (λ⁻¹•1)` is symplectic; conjugate by a transvection product).
  Engine: `spTransvecSp_commutator` `[g,τ_{v,a}]=τ_{v,(λ²-1)a}`. Carries the **honest field-size
  hypothesis** `hlam : ∃λ,λ≠0∧λ²≠1` (|F|≥4) which **fixes a latent soundness bug** — the old
  axiom unsoundly claimed `PSp(4,2)≅S₆` perfect. NB the `λ²≠1` route MISSES `q=3` (where PSp(4,3)
  IS simple) — that case needs a separate perfectness argument someday.
- **Generation NARROWED + toolkit COMPLETE.** `sp_transvec_closure_eq_top` is now a theorem
  resting only on the core axiom `sp_stab_hyperbolic_le` (a symplectic `g` fixing a hyperbolic
  pair `(e,f)` pointwise ∈ ⟨transvections⟩). ALL the transitivity machinery is machine-checked
  (on-vectors, on-hyperbolic-pairs, `sp_preserves_form`). **AND** — the key insight — the whole
  complement infrastructure for the core's dimension induction is built WITHOUT an abstract
  submodule layer, using the explicit coordinate projection `perpComp e f x = x + ω(f,x)·e −
  ω(e,x)·f`. See `[[symplectic-explicit-complement-projection]]`.
- **Quasi-preprimitivity NARROWED.** `pspQuasiPreprimitive` is now a theorem (via mathlib's
  `IsPreprimitive.isQuasiPreprimitive`) resting only on `psp_isTrivialBlock_of_isBlock` (every
  block trivial = primitivity). Pretransitivity is machine-checked (`psp_isPretransitive`). Also
  provided `pspPreprimitive_iff_isCoatom_stabilizer` — the standard maximal-parabolic target.
- **Both core axioms are SOUND** (checked): `sp_stab_hyperbolic_le` is Witt's theorem; PSp is
  primitive on ℙ²ⁿ⁻¹ for ALL F (incl. PSp(4,2) — primitivity needs no field-size exclusion).
- **Decision log**: did NOT wire `PSpn_isSimpleGroup_of_iwasawa` into LieType's `PSp_isSimpleGroup`
  — premature: ZMod q is a field only for q prime, `hlam` needs q≥4, and the cores are still open.
  Left for after the cores are discharged. mathlib has NO abstract symplectic/Witt infra (checked
  `LinearAlgebra/`), so the cores genuinely need new development.

## ✅ State (16 commits, each a green full build = 8307 jobs; last `lake build` green THIS session)

The complement toolkit for `sp_stab_hyperbolic_le`, ALL axiom-clean (in `SpIwasawa.lean`):
`perpComp`/`perpComp_mem_perp`/`perpComp_add_span` (the ⊕ decomposition), `spForm_nondegenerate`
+ `perp_form_ne_of_mem_perp` (global+relative non-degeneracy), `exists_perp_form_both_ne` +
`exists_perp_transvecGen_maps` (transitivity WITHIN ⟨e,f⟩⊥ by pair-fixing transvections),
`sp_fixing_preserves_perp` + `spTransvection_fixes_pair` (preserve/extend), `sp_eq_one_of_fixes_perp`
(terminal case: g id on complement ⟹ g=1). PENDING_WORK §F is fully rewritten & current.

## 🎬 Next actions

1. **Poll Aristotle**: `aristotle tasks 8522edf3-fdae-4924-9549-2eb53e1a8f50` (was IN_PROGRESS 35min
   at lap end — bare-statement stabilizer-core stub at `/tmp/aristotle-spstab/SpStab.lean`). If
   COMPLETED → download, verify in-kernel + `#print axioms`, port. If FAILED/still walling →
   **resubmit a RICHER stub** with the complement toolkit lemmas above inlined as axioms, asking
   only for the induction.
2. **The ONE remaining gap for generation**: the well-founded induction wiring the toolkit
   together — iterate `exists_perp_transvecGen_maps` to reduce `g|_⟨e,f⟩⊥` to identity, terminating
   via `sp_eq_one_of_fixes_perp`. Needs a measure (strong induction on `Fintype.card l`, OR a rank
   measure on the unfixed subspace) + likely a generalization of `perpComp` to the complement of
   *several* hyperbolic pairs. This is the genuine multi-lap formalization; geometric content done.
3. **Primitivity core** `psp_isTrivialBlock_of_isBlock`: prove the point-stabilizer is maximal
   (`pspPreprimitive_iff_isCoatom_stabilizer` reduces to that) — the maximal-parabolic argument.

## ⚠️ Gotchas

- Full `lake build` can OOM transiently under the 6-core taskset; just retry the commit (corpus
  `lean-box-oom-...`). Iterate on the leaf with `lake build FiniteSimpleGroups.SpIwasawa` (~5-20s).
- Under `open Matrix`, `mul_smul` is ambiguous → use `SemigroupAction.mul_smul`, or better
  `inv_smul_smul`/`smul_comm` (see `[[lean-action-mul-smul-ambiguous]]`).
- `omit [Fintype l] in` must go BEFORE the docstring, not between docstring and `theorem`.
- Don't preempt a foreign/running Aristotle job (rate-limit: one at a time).

## 📁 Key files
- `FiniteSimpleGroups/SpIwasawa.lean` — THE file: all transitivity, perfectness chain, spDiag,
  generation reduction + complete complement toolkit, both core axioms, assembly. START HERE.
- `FiniteSimpleGroups/SpTransvection.lean` — transvection algebra, `sp_preserves_form`,
  `spTransvecSp_commutator`, `spTransvecSp_inv`.
- `FiniteSimpleGroups/SpSimple.lean` — faithfulness / `pspAction` / `pspPermHom`.
- `PENDING_WORK.md` §F — authoritative status + attack paths for the 2 cores.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb
the context, then start at Next action #1: poll the Aristotle job, then attack the generation-core
induction (the geometric toolkit is all built — you're wiring the well-founded recursion) or the
primitivity maximal-parabolic core.**
