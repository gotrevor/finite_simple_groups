# Handoff: PSU(n,q) simplicity — `hgen` collapsed to two transvection-product atoms

**Date**: 2026-06-04 19:27 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `969ee14`

## 🎯 What we're doing
Discharging the deep CFSG axiom `PSU_isSimpleGroup` (`LieType.lean:166`) by proving `PSUConcrete n p`
simple via Iwasawa. Everything reduced to **`hgen`** (the isotropic unitary transvections generate
`SU_n(F_{p²})`, n≥3). This lap **built the entire `hgen` Dieudonné dimension-induction machinery** and
collapsed the deep content to **exactly two transvection-product facts**.

## 🧠 Context to carry forward (the map)
- **`hgen` is now `hgen_of_scaleKill_mate : hgen ⟸ UScaleKill + UExactMateTrans + (2≠0)`.** Top-level:
  **`PSU_isSimpleGroup_modulo_scaleKill_mate`** (`UnitarySimple.lean §Iwasawa`, axiom-clean). The
  whole genAux induction, base case, within-`offSU` connectivity, and line-to-scalar transitivity are
  **machine-checked** — only the two atoms remain:
  1. **`UScaleKill`** (THE deep wall) — a `uTransvecGen`/`FixSU C` element scaling an isotropic
     `e' ↦ c·e'` for ARBITRARY `c ∈ F_{q²}*`. This is the `F_{q²}*` line-stabiliser torus **as a
     transvection product** = the **SU₃ generation base case** (`diag(μ, star μ/μ, star μ⁻¹)`: a
     hyperbolic plane + an anisotropic axis whose norm-1 entry absorbs the determinant; n≥3 essential).
     The F_q* sub-case is already built (`exists_su_hyperbolic_scale_gen`); the genuine wall is c∉F_q.
     ⚠️ The **SU-level** `exists_scale` (any SU element, NOT a transvection product) is the easy one —
     R·P construction, **submitted to Aristotle `9105747d` and still RUNNING (>1hr)**. It does NOT
     discharge `UScaleKill` (membership in `uTransvecGen` is the whole point — circular otherwise), but
     port it as a bonus theorem (exact transitivity) when it returns + verify in-kernel.
  2. **`UExactMateTrans`** (torus-free, but non-local) — mate `f↦f'` fixing `e` (scalar auto-pinned to
     1 by `⟨e,·⟩=1`). Reduces to **`uEichler ∈ uTransvecGen`**. I proved the μ-peeling
     `uEichler_eq_mul_transvection : E_{x,h,μ} = E_{x,h,0}·τ_{x,−μ}`, leaving the pure `E_{x,h,0}`
     (h isotropic). **KEY FINDING:** `E_{x,h,0}` is NOT a product of `span{x,h}`-transvections (that
     span is totally isotropic ⟹ they commute & act diagonally) — it needs an **ambient hyperbolic
     partner** `k` of `x`. So this is a genuine Dieudonné lemma, not a local computation.
- **Both walls need the literature** — see `ON-LINE-REQUEST.md` (sharpened this lap to two precise
  questions: the explicit transvection factorization of the F_{q²}* torus / SU₃ generation, and the
  unitary Eichler as a transvection product using a partner). A networked session must fulfill these;
  attempting them blind = guessing transvection products + scalars (high error risk). Check for
  `ON-LINE-FINDINGS-*.md` at lap start and act on it before re-grinding.
- The symplectic `genAux_le` (`SpIwasawa.lean:681`) was the exact template; the unitary version differs
  by (i) isotropy (centres must be isotropic ⟹ Eichler moves not single transvections) and (ii) the
  anisotropic standard basis (peel coordinate hyperbolic pairs `hpA/hpB` on `{2i,2i+1}`, not basis vecs).

## ✅ State (15 commits this lap, every one a verified green `lake build` = 8312 jobs; all axiom-clean)
New in `UnitarySimple.lean` (general `offSU/FixSU` block lives in §Concrete; reductions in §Iwasawa):
- **Bookkeeping:** `offSU`/`FixSU` + closure (`offSU_add/sub/smul/single`, `FixSU_mul`),
  `coord_eq_form`, `offSU_preserved`, `FixSU_of_fixes_perp`, `FixSU_univ_eq_one`.
- **`uTransvecGen`** (= `Subgroup.closure {transvections}`, so `hgen ⟺ uTransvecGen = ⊤`),
  `uTransvecSU_mem_gen`; membership+`FixSU`-aware engines `offSU_maps_nonorth_gen`,
  `exists_su_weyl_swap_gen`, `exists_su_hyperbolic_scale_gen` (F_q* torus).
- **Coordinate pair:** `hpA`/`hpB` + iso/hyperbolic/offSU + span recovery `fixes_coords_of_fixes_hpAB`.
- **genAux:** `uGenAux_base`, `uGenAux` (peels a coord pair per step), `UExactPairTrans` (hyp),
  `uTransvecGen_eq_top_of_hpair`, `hgen_of_hpair`.
- **Connectivity (offSU-relative):** `exists_hyperbolic_partner_offSU`,
  `common_nonorth_isotropic_perp_offSU`, `exists_common_nonorth_isotropic_offSU`,
  `offSU_maps_isotropic_gen` (line→scalar).
- **Reductions:** `UExactLineTrans`/`UExactMateTrans` + `UExactPairTrans_of_line_mate`;
  `UScaleKill` + `UExactLineTrans_of_scaleKill`; `hgen_of_line_mate`, `hgen_of_scaleKill_mate`;
  `PSU_isSimpleGroup_modulo_line_mate`, `PSU_isSimpleGroup_modulo_scaleKill_mate`.
- In `UnitaryTransvection.lean`: `uEichler_eq_mul_transvection`.

## 🎬 Next actions
1. **Poll `aristotle list`** (fast one-shot). If `9105747d` (uscale_brick, SU-level `exists_scale`)
   is done: `aristotle download 9105747d --destination /tmp/x.tgz` (gzip tarball → `tar xf`), verify
   in-kernel + `#print axioms`, port the SU-level `exists_scale` + `exists_su_maps_isotropic_exact`
   (exact transitivity) as bonus theorems on the real defs. Then **submit the next PSU brick** to keep
   Aristotle busy — best candidate: the **pure Eichler factorization** `E_{x,h,0}` as a transvection
   product using a partner `k` (toward `UExactMateTrans`), inlining the needed defs.
2. **Check `ON-LINE-FINDINGS-*.md`** at repo root; if a networked session answered the request, port
   the factorizations (that's what unblocks both atoms). `git mv` harvested findings to `archive/findings/`.
3. The two open atoms are deep; advance whichever the literature unblocks first. `UExactMateTrans`
   (Eichler) is likely the easier (no torus); `UScaleKill` (SU₃ torus) is the genuine multi-lap core.

## ⚠️ Gotchas
- **Section `variable (p)` ⟹ `p` is an explicit leading arg of every decl using it.** Cross-calls
  inside §Concrete MUST pass it (`offSU_add p ...`); forgetting it gives bogus "numerals are data"
  errors. (Flushed to memory.)
- `dotProduct_star_swap a b : star b ⬝ᵥ a = star (star a ⬝ᵥ b)` — output-first arg order; easy to get
  backwards (cost me one rebuild on `offSU_maps_isotropic_gen`).
- `UnitaryTransvection.lean:335` emits an `info: Try this: abel_nf` note on `uEichler_eq_mul_transvection`
  — harmless (an info suggestion, not an error/warning); build is green.
- `(2 : UnitaryField p) ≠ 0` from `p≥5`: derive via `CharP.cast_eq_zero_iff (UnitaryField p) p` +
  `Nat.le_of_dvd` (pattern in `PSU_isSimpleGroup_modulo_line_mate`).

## 📁 Key files
- `FiniteSimpleGroups/UnitarySimple.lean` — START HERE. §Concrete has the `offSU/FixSU`/genAux/
  connectivity machinery (~lines 1217-1720); §Iwasawa has the capstones (`PSU_isSimpleGroup_modulo_*`).
- `FiniteSimpleGroups/SpIwasawa.lean:460-733` — the symplectic `genAux_le` template.
- `FiniteSimpleGroups/UnitaryTransvection.lean` — `uTransvection`/`uEichler` + the μ-peel lemma.
- `PENDING_WORK.md §G` — authoritative PSU status (the `★★★★ UPDATE` block has the full current plan).
- `ON-LINE-REQUEST.md` — the two sharpened literature questions that unblock the atoms.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (PSU `hgen`,
now down to `UScaleKill` + `UExactMateTrans`). Start at Next action #1: poll Aristotle (`9105747d`),
check for `ON-LINE-FINDINGS-*.md`, then advance whichever atom the literature unblocks.**
