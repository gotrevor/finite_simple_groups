# Handoff: CFSG component-commuting cluster made AXIOM-FREE (Aristotle join-free proof)

**Date**: 2026-06-02 17:25 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main)

## 🎯 What we're doing
Sharpening the `finite_simple_groups` CFSG **scaffold** (a learning artifact — read
`CFSG_MAP.md` first). This session's headline: the entire **component-commuting cluster
is now sorry-free AND axiom-free**, after Aristotle found a proof of Aschbacher 31.4 that
doesn't need the Wielandt subnormal-join. **Stay in this repo** — this session's name IS
`finite_simple_groups`; Trevor runs a fleet of one-repo-per-lane box agents.

## 🧠 Context to carry forward
- **The session arc (the mental model):** I marched an axiom boundary downward —
  `commute_of_ne` (axiom) → `normalizes_of_ne` (axiom) → `aschbacher_base` (the *normal*
  base case, with the subnormal-length induction proven) → **fully discharged**. The last
  step came from Aristotle: the base case turned out NOT to need `IsSubnormal.sup`. A
  **forward** induction along H's subnormal chain (`centralizing_by_subnormal`: at each
  step `M ◁ J`, `⁅L,H⊓J⁆ ≤ H⊓M`, propagated by three-subgroups + perfectness) replaces the
  classical normal-closure `⟨L^H⟩` argument.
- **The load-bearing lesson:** the repo (and I) asserted this *needed* the join. That was a
  **hypothesis wearing the costume of a fact.** Same failure mode as my earlier "exhausted
  the productive work" claim, which Trevor rightly called out — confident closure where the
  check was never run. On a CFSG scaffold "no more productive work" is essentially never
  true (the map calls scaffold architecture "unbounded"). Flushed to memory as
  `needs-deep-machinery-is-a-hypothesis`.
- **Aristotle worked as leverage, not handoff-of-open-problem:** I architected the bounded
  lemma, gave it a self-contained file (inline defs mirroring the repo + the join supplied
  as a *usable* axiom + goal=sorry), and — critically — **verified the returned proof in
  our kernel + `#print axioms` before trusting it** (defs/statement faithful, no `sorryAx`,
  no snuck-in axioms). Pattern in memory: `aristotle-leverage-self-contained-verify`.
- **Earlier wins this session (don't redo):** `lt60` was already closed (repo sorry-free);
  added 4 sporadic order cross-checks (incl. `co1Subquotient_order_dvd_co1` — note J₂ IS a
  Co₁ subquotient, the prior handoff's list omitted it); recorded the order-pin sweet-spot
  + Lie-type-pin resolution (opaque `classicalLieTypeCarrier` is safe to pin, deferred only
  for confabulation-risk on the formulas) in `CFSG_MAP.md`.
- **Two-debt vocabulary (Trevor likes it):** *proof debt* (axioms) vs *definitional debt*
  (`opaque` carriers). This session paid proof debt: −2 axioms (commute + layer_commutator).

## ✅ State (verified this session)
- Branch `cfsg-sporadic-order-pin`, HEAD `efca6be` (docs). **Last gated Lean build: `19c57e2`,
  full `lake build` green at 8291 jobs.** Repo sorry-free.
- `#print axioms` (observed): `IsComponent.subnormal_dichotomy`, `IsComponent.commute_of_ne`,
  `layer_commutator_fittingSubgroup_eq_bot` all depend on `[propext, Classical.choice,
  Quot.sound]` **only** — axiom-free.
- `#print axioms CFSG` unchanged: 7 milestone axioms + trio (the cluster is NOT in CFSG's
  trail — it builds toward Bender's cornerstone, a separate track).
- Top-level `axiom` declarations in tree: **34** (`rg -c '^axiom '`). Aristotle solution
  preserved verbatim at repo root: `aristotle-solution-components-commute.lean`.

## 🎬 Next actions
1. **Respond to Trevor's next message in-lane.** Session ended at a clean checkpoint; I had
   just offered him two next moves (below) and he ran `/handoff`.
2. **If "carry on" / dealer's choice — best target: `IsSubnormal.sup` is now a DEAD axiom.**
   Verified this session: no proof uses it (only docstrings mention it); the warm-up
   `sup_normal` IS still used by `GeneralizedFitting.lean`, so keep that. Options: delete the
   `IsSubnormal.sup` axiom from `Wielandt.lean` (and scrub the now-stale "needs the join"
   docstrings in `ComponentCommute.lean` / `AschbacherDichotomy.lean`), OR demote it to a
   stated-but-unused classical result with a note. Cleaning it is satisfying and honest.
3. **Then the next real local-theory target:** `genFittingSubgroup_self_centralizing`
   (Bender's cornerstone, `GeneralizedFitting.lean`) — genuinely deep, the honest axiom that
   the commute/layer theorems feed toward. Or breadth: deepen `ProofTree.lean` interfaces
   (low-risk architecture). Don't manufacture makework, but don't declare "done" either.

## ⚠️ Gotchas
- **Box OOM-thrashes under the concurrent fleet.** Pre-commit full `lake build` intermittently
  fails "Cannot allocate memory" on an *unrelated* mathlib file even when your module builds
  green. **Just retry the commit** — it converges. **Do NOT `pkill lean/lake`** (kills other
  sessions' builds). Memory: `lean-yolo-box-fleet-lane-discipline`.
- **Other sessions live on the shared tree.** MEMORY.md / KB get rewritten under you by a
  `/distill` worker. Don't wander into other repos (`bounded_gaps`, `sum-product`).
- **In the lean-yolo-box, not the host.** `git` local works (commit freely); no push/`gh`.
  **Never offer to push** (Trevor, 2026-06-02).
- **Lake globs all files** (builds unimported modules too), but the root `FiniteSimpleGroups.lean`
  is the re-export — I wired `AschbacherDichotomy` + `ComponentCommute` into it this session.
- **Master `HANDOFF.md` exists** — this is a dated session doc; don't overwrite the master.
- **KB inconsistency to reconcile (for `/distill`):** existing memory
  `aristotle-standalone-lemma-ports-clean` claims "ports v4.28→v4.29 zero edits, drift a
  non-issue" — my experience contradicts it (2 edits: `ReflTransGen.refl` is now `@[refl]` so
  `rw` auto-closes, and `h.le` dot-notation resolves on the displayed head type). New note
  `lean-v428-v429-port-gotchas` corrects it.

## 📁 Key files
- `FiniteSimpleGroups/AschbacherDichotomy.lean` — NEW. The axiom-free dichotomy (ported
  Aristotle proof); `centralizing_by_subnormal` is the join-free heart.
- `FiniteSimpleGroups/ComponentCommute.lean` — `commute_of_ne` + `layer_commutator_…` now
  theorems on the dichotomy; no axioms.
- `FiniteSimpleGroups/Wielandt.lean` — `IsSubnormal.sup` (now-dead axiom) + the still-used
  `sup_normal` warm-up.
- `aristotle-solution-components-commute.lean` (repo root) — Aristotle's verbatim solution.
- `ARISTOTLE-JOB-components-commute.md` — job record (project `adf60350`, SOLVED) + poll/port notes.
- `CFSG_MAP.md` — the 4-bucket discipline; read first. Counts section updated this session.

---
**→ Next session: this is your starting point. You are the `finite_simple_groups` session —
STAY in this repo (fleet, one repo per lane). Don't summarize this doc back to Trevor, don't
wait for instructions, and don't offer other KB projects. Absorb the context above (esp. the
"needs the deep machinery is a hypothesis" lesson), then respond to Trevor's next message
in-lane — or, if he says carry on, start with Next action #2 (kill/demote the now-dead
`IsSubnormal.sup` axiom and scrub the stale "needs the join" docstrings).**

🧵 ➡️ 🧵
