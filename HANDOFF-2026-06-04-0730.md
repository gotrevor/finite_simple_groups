# Handoff: PSL(n,q) n≥3 DISCHARGED — deep axiom → Iwasawa proof, modulo 2 narrow geom axioms

**Date**: 2026-06-04 07:30 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `60d0b29`

## 🎯 What we're doing

Discharging the repo's tractable family-simplicity axioms. **This lap CLOSED the entire
`PSL(n,q)` n≥3 thread**: the deep `axiom PSL_isSimpleGroup_rank_ge_three` is **deleted** and
the n≥3 case is now a fully machine-checked Iwasawa proof. 11 commits, every one green via the
pre-commit full `lake build`.

`#print axioms FiniteSimpleGroups.PSL_isSimpleGroup` is now
`[propext, Classical.choice, Quot.sound, SLn.exists_sl_maps_two_points,
SLn.transvecSL_closure_eq_top]` — i.e. the unified PSL-simplicity theorem (all n≥2) rests on
**exactly two concrete linear-algebra facts**, nothing deeper. Discharge both → fully axiom-clean.

## 🧠 Context to carry forward

- **The whole Iwasawa criterion is assembled for general `n : Type*`** (not just `Fin n`), `F`
  any field, then specialised at `(Fin n, ZMod q)` inside `SL2.PSL_isSimpleGroup`. Three new
  files: `SLnAction.lean` (linear `mulVec` action), `SLnSimple.lean` (center-acts-trivially,
  faithful, quasi-preprimitive), `SLnIwasawa.lean` (the direction-`v` transvection
  `IwasawaStructure` + final assembly `PSLn_isSimpleGroup_of_rank`).
- **Architecture decision (important):** the `PSL(n,F)` action `pslnAction` is a `def`, NOT a
  global `instance` — a global one would diamond with `SL2.psl1Action` at `(Fin 2, ZMod q)`.
  All PSL-level obligations (`pslnFaithful`, `pslnQuasiPreprimitive`, `pslnIwasawaStructure`)
  are likewise `def`s, and the final theorem supplies them via `letI`/`haveI`. This is why
  statements use `letI := pslnAction …` in their signatures. Keep this pattern; do NOT promote
  to global instances.
- **The two residual axioms are narrow and standard** (`SLnSimple`/`SLnPerfect`):
  1. `transvecSL_closure_eq_top` — transvections generate `SL(n,F)` (Whitehead/Gaussian). mathlib
     has `Matrix.diagonal_transvection_induction`; missing only "det-1 diagonal = product of
     transvections". **OUT at Aristotle, job `a4b84e9f` (`slngen`), still RUNNING at >1h** at lap end.
  2. `exists_sl_maps_two_points` — `SL(n,F)` 2-transitive on `ℙ^{n-1}` for `2 ≤ |n|`. I scoped the
     local construction: distinct points → l.i. reps (`Projectivization.independent_iff` /
     `mk_eq_mk_iff'`), `Basis.extend` to a basis, `Basis.toMatrix`/`det_ne_zero`, rescale the 2nd
     column by `1/det` (projectively invariant) to land in SL, compose `B·A⁻¹` through the
     reference frame. It's a real ~150-line basis-extension construction over an abstract index
     type — I deliberately did NOT start it late-lap; it's the **next Aristotle brick** (spec in
     `ARISTOTLE-JOB-transvec-generate.md`). `n=2` is the proved `SL2.sl2_two_trans`.
- **The direction-`v` transvection construction** (the genuinely new math, `SLnIwasawa`): the
  unipotent radical `{1 + v⊗φ : φ(v)=0}` of a point stabilizer, built from `Matrix.vecMulVec v φ`.
  Key mathlib tools: `vecMulVec_mul_vecMulVec` (gives the nilpotency cross-term),
  `mul_vecMulVec`/`vecMulVec_mul` (conjugation), `det_one_add_replicateCol_mul_replicateRow`
  (det=1+φ(v)). This is abelian, conjugation-equivariant, and generates (via axiom #1).
- **Two Lean gotchas flushed to memory** this lap (see staging buffer): SL coercion is `↑ₘ` not
  `Subtype.val` (use `congrArg Subtype.val (mul_inv_cancel g)`); `Pi.single i 1` needs `(…:n→F)`.

## ✅ State (all committed, all green full builds observed via pre-commit)

11 commits `9a73eda…60d0b29`. The chain: action refactor → MulAction → faithful → quasi-prep →
direction-`v` algebra → subgroup+conj → scaling-inv + `Tline` → **assembly** (`3aef5e3`,
`PSLn_isSimpleGroup_of_rank` axioms = the 2 geom axioms) → **discharge** (`fe1a8c6`, wired into
`SL2.PSL_isSimpleGroup`) → **axiom deleted** (`fc4e628`) → trackers (`60d0b29`). All new decls
`#print axioms`-verified clean-or-modulo-the-2-disclosed-axioms.

## 🎬 Next actions

1. **`aristotle list` → check `a4b84e9f` (`slngen`).** If done: download → VERIFY in our kernel
   (`lake env lean`, `#print axioms` clean) → port onto `SLnPerfect.transvecSL_closure_eq_top`
   (drop the `axiom`). That makes perfect/quasi-prep/the whole assembly lose one axiom.
   **Then immediately submit the next brick: `exists_sl_maps_two_points`** (full spec at the
   bottom of `ARISTOTLE-JOB-transvec-generate.md`).
2. If `slngen` failed/partial: decompose it to the bounded sub-brick "det-1 diagonal = product
   of `transvection`s" and resubmit; meanwhile attempt `exists_sl_maps_two_points` locally (the
   basis-extension construction — see Context; tools are all in mathlib).
3. Discharging BOTH axioms makes `FiniteSimpleGroups.PSL_isSimpleGroup` fully
   `[propext, Classical.choice, Quot.sound]` — the natural finish line for this thread.

## ⚠️ Gotchas

- `lake build` intermittently dies with `Cannot allocate memory` on an unrelated `.olean` —
  **spurious, just retry** (hit it once this lap).
- A full default `lake build` can exceed the 590s timeout (FeitThompson); build the specific
  leaf (`lake build FiniteSimpleGroups.SLnIwasawa`) when iterating. The pre-commit hook runs the
  full taskset build and is the real green gate — a successful commit = a green full build.
- `pslnAction` etc. are `def`s on purpose (diamond avoidance) — see Context. Don't "fix" them
  into instances.

## 📁 Key files

- `FiniteSimpleGroups/SLnIwasawa.lean` — direction-`v` transvection `IwasawaStructure` + the final
  `PSLn_isSimpleGroup_of_rank`. The capstone.
- `FiniteSimpleGroups/SLnSimple.lean` — MulAction/faithful/quasi-prep descent; holds
  `axiom exists_sl_maps_two_points` (residual #2).
- `FiniteSimpleGroups/SLnPerfect.lean` — perfectness; holds `axiom transvecSL_closure_eq_top`
  (residual #1, at Aristotle).
- `FiniteSimpleGroups/SL2.lean` — `PSL_isSimpleGroup` dispatcher (n=2 proved here, n≥3 → SLnIwasawa).
- `PENDING_WORK.md §E` — now marked DISCHARGED; authoritative status of the 2 residuals.
- `ARISTOTLE-JOB-transvec-generate.md` — `slngen` harvest steps + the `exists_sl_maps_two_points` brick spec.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait
for instructions, and don't offer other projects from the KB — this doc IS the chosen thread.
Absorb the context above, then start at Next action #1: `aristotle list` to check `a4b84e9f`,
harvest if ready, and drive the two residual geometric axioms to fully axiom-clean `PSL_isSimpleGroup`.**
