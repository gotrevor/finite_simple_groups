# Handoff: PSU(n,q) simplicity — Eichler atom cracked, only the SU₃ torus (`UScaleKill`) remains

**Date**: 2026-06-04 20:50 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `991d996`

## 🎯 What we're doing
Discharging the deep CFSG axiom `PSU_isSimpleGroup` (`LieType.lean:166`) by proving `PSUConcrete n p`
simple via Iwasawa. Everything is reduced to **`hgen`** (isotropic unitary transvections generate
`SU_n(F_{p²})`, n≥3), and `hgen` was reduced by the prior lap to **two transvection-product atoms**:
`hgen ⟸ UScaleKill + UExactMateTrans + (2≠0)` (top-level `PSU_isSimpleGroup_modulo_scaleKill_mate`,
`UnitarySimple.lean §Iwasawa`, axiom-clean). This lap **cracked the core of `UExactMateTrans`** and
sharply isolated the single remaining wall.

## 🧠 Context to carry forward (the map — read this)
- **`UExactMateTrans` (the Eichler mate step) is now machine-checked for the isotropic case.** The
  prior handoff believed the pure Eichler `E_{x,h,0}` "needs an ambient hyperbolic partner k" — **that
  was a fallacy** (corrected in memory `unitary-eichler-not-local-transvection-product`). The truth,
  now kernel-verified:
  **`E_{x,h,0} = τ_{h,c⁻¹}·τ_{x,−c}·τ_{x+c⁻¹h,c}`** for any trace-zero unit `c` — three transvections
  ALL centred in `span{x,h}`, NO partner. The off-axis centre `x+c⁻¹h` (still in span{x,h}) is what
  produces the antisymmetric cross terms; the old "they all act diagonally" argument only considered
  centres x,h separately. All three centres are pairwise/self-orthogonal isotropic ⟹ every vecMulVec
  cross-product vanishes ⟹ the triple product collapses.
- **Both atoms collapse to ONE wall = the SU₃ torus.** `UExactMateTrans` for **isotropic** `h=f'−f` is
  done (peel + membership below). For **anisotropic** `h`: n≥4 splits into two isotropic Eichlers via
  the composition law; **n=3 is a verified obstruction** (`x^⊥/⟨x⟩` is an anisotropic line ⟹ no
  isotropic split) and coincides exactly with `UScaleKill`. So the genuine remaining content is
  **`UScaleKill` = the SU₃ line-stabiliser torus as a product of unitary transvections** — the
  Dieudonné generation core. It cannot be decomposed into nondegenerate planes (only ⟨e,m⟩ is
  nondegenerate; the F_{q²}-scaling genuinely needs rank 3). This is a multi-lap target.
- **Aristotle confirmed the SU-level (easy) scaler** `g₀ = R·P` (`uscale_brick`): `P` = F_{q²}-scaling
  on the hyperbolic plane (det `c/star c`), `R` = norm-1 scaling on the anisotropic axis (det
  `star c/c`); individually ∉ SU, product ∈ SU. The OPEN problem is realising this `R·P` as SU
  transvections (short-root/Weyl construction). It does NOT discharge `UScaleKill` (membership in
  ⟨transvections⟩ is the whole point).

## ✅ State (5 commits this lap, all verified green `lake build` = 8312 jobs, all axiom-clean)
New in `UnitaryTransvection.lean` (namespace `FiniteSimpleGroups.PSU`):
- **`uEichler_isotropic_eq_transvection_prod`** — the 3-transvection factorization (above).
- **`uEichler_comp`** — Eichler composition law `E_{x,a,μ}·E_{x,b,ν}=E_{x,a+b,μ+ν+⟨a,b⟩}`.
New in `UnitarySimple.lean`:
- **`uEichler_zero_mem_uTransvecGen`** — `E_{x,h,0} ∈ uTransvecGen` (x,h isotropic, ⟨x,h⟩=0).
- **`uEichler_isotropic_mem_uTransvecGen`** — `E_{x,h,μ} ∈ uTransvecGen` for isotropic h (μ
  trace-zero) via the peel `E_{x,h,μ}=E_{x,h,0}·τ_{x,−μ}`.
- `#print axioms` clean on all four (propext/Classical.choice/Quot.sound only). Last green build
  observed at pre-commit of `da44869` (8312 jobs); HEAD `991d996` is doc-only.
- **Aristotle**: `su3_scaler` (`a0bb1361`) RUNNING on the deep `UScaleKill` core (poll it first thing).
  `ueichler_comp` (`fcb65b71`) IDLE/done but REDUNDANT (proved locally — ignore/discard).

## 🎬 Next actions
1. **Poll `aristotle list`** (fast one-shot). If `su3_scaler` (`a0bb1361`) is done: download, VERIFY
   in-kernel + `#print axioms`, and if it genuinely exhibits the SU₃ torus as a transvection product,
   port it onto the real defs to discharge `UScaleKill` (the prize). If it wrapped (assumed the result
   as an axiom — likely for an open problem), note it and re-architect a sharper sub-brick. Then submit
   the next bounded brick so Aristotle never idles.
2. **Check for `ON-LINE-FINDINGS-*.md`** at repo root; `ON-LINE-REQUEST.md` item #1 (sharpened, with
   the `R·P` structure + short-root hint) asks for the explicit SU₃ torus transvection word — that is
   what unblocks `UScaleKill`. `git mv` any harvested findings to `archive/findings/`.
3. **Optional quick win — finish `uEichler_split_mem_uTransvecGen`** (the n≥4 anisotropic reduction).
   The composition wiring is trivial (`uEichler_comp` + two `uEichler_isotropic_mem_uTransvecGen`);
   the ONLY blocker is the trace bookkeeping `(μ+ν+⟨h₁,h₂⟩)+star(…)=⟨h₁+h₂,h₁+h₂⟩`, which stalls
   ring/abel/ring_nf/linear_combination (see Gotchas). Fix per the recipe and it lands. This does NOT
   close any atom (n=3 still blocks) but completes the Eichler API.

## ⚠️ Gotchas
- **`ring`/`abel`/`ring_nf`/`linear_combination` stall on `a + b = b + a` when `a,b` are `dotProduct`
  atoms of different provenance** over a StarRing: `ring` pushes `star` through the dotProduct sum, so
  `star(⟨h₁,h₂⟩)` normalizes to the summand-swapped `⟨h₂,h₁⟩` as a *different* opaque atom that never
  unifies with a syntactically-written `⟨h₂,h₁⟩`. Also `simp [star_add, add_dotProduct, dotProduct_add]`
  on `star(u+v)⬝ᵥ(u+v)` emits cross terms in an unpredictable order. **Fix:** force ONE canonical
  surface form before ring/lc (rewrite all copies via `dotProduct_star_swap`), or finish a residual
  `a+b=b+a` by hand with explicit `add_comm a b`, or prove the `star(u+v)⬝ᵥ(u+v)` expansion as a named
  lemma so order is fixed. (Flushed to memory `lean-ring-pushes-star-through-dotproduct`.)
- **`match_scalars <;> ring` works, but `match_scalars <;> (first | ring | linear_combination …)`
  silently fails to dispatch the lc alternatives.** Use explicit `·` bullets per goal (dump goals with
  a bare `match_scalars` first). The factorization/composition proofs use this pattern.
- Section `variable (p)` ⟹ `p` is an explicit leading arg: cross-calls must pass it
  (`uEichler_zero_mem_uTransvecGen p …`, `uTransvecSU_mem_gen p …`).
- `UnitaryTransvection.lean:335` emits a harmless `info: Try this: abel_nf` — not an error.

## 📁 Key files
- `FiniteSimpleGroups/UnitaryTransvection.lean` — `uEichler`/`uTransvection` + this lap's
  factorization (`uEichler_isotropic_eq_transvection_prod`) and composition law (`uEichler_comp`).
- `FiniteSimpleGroups/UnitarySimple.lean` — `uTransvecGen`, the new membership lemmas (~line 1303+),
  the mate engine `exists_su_fixes_maps_isotropic_mate_fixing_perp` (`g=τ_{x,c}·E_{x,h,μ}`, line ~881),
  and the §Iwasawa capstones (`PSU_isSimpleGroup_modulo_scaleKill_mate`).
- `PENDING_WORK.md §G` — authoritative status; the `★★★★★ UPDATE 2026-06-04` block has the full
  refined map + the two tactic gotchas + the split-lemma recipe.
- `ON-LINE-REQUEST.md` — item #1 = the single literature ask (SU₃ torus transvection word).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (PSU `hgen`,
now down to the single SU₃ torus wall `UScaleKill`). Start at Next action #1: poll Aristotle
(`a0bb1361`), check for findings, then advance `UScaleKill` — the genuine multi-lap core.**
