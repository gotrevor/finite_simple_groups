# Handoff: PSU(n,q) thread — faithfulness AXIOM-FREE; scaling element + perfectness DONE mod generation

**Date**: 2026-06-04 ~22:30 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `fe4eda1`

## 🎯 What we're doing
Discharging the deep CFSG axiom `PSU_isSimpleGroup` by building `PSUConcrete n p = SU_n(F_{p²})⧸center`
and proving it simple via the **Iwasawa criterion** (`MulAction.IwasawaStructure.isSimpleGroup`), the
same machinery as PSL/PSp. PSp (`SpIwasawa`/`SpTransvection`/`SpSimple`/`SpSmallField`) is the
line-by-line template. The Iwasawa criterion needs five things on the action of `PSUConcrete` on the
**isotropic** projective points `IsoPoint p n` (NOT full ℙ — SU isn't transitive there):
`Nontrivial`, `FaithfulSMul`, `IsQuasiPreprimitive`, `IwasawaStructure` (abelian conj-equivariant
generating T-family), and `commutator = ⊤` (perfect).

## 🧠 Context to carry forward
- **This lap cleared the entire step-2 faithfulness AND the perfectness engine.** Faithfulness is now
  fully axiom-free (last geometry axiom `common_nonorth_isotropic_perp` discharged — see below), and
  perfectness is fully assembled **modulo one hypothesis: generation** (unitary transvections
  generate SU). Generation is the unitary Eichler/Witt theorem and is the SOLE remaining input for
  both `commutator(SU)=⊤` and the Iwasawa `is_generator`.
- **The `common_nonorth_isotropic_perp` discharge** (the geometry insight worth remembering): for the
  perpendicular connectivity case, take ANY hyperbolic partner `w₂` of `z₂`, project it into `H^⊥`
  (`w₂' = w₂ - ⟨w₁,w₂⟩z₁ - ⟨z₁,w₂⟩w₁`), then re-isotropize. The projected self-product
  `⟨w₂',w₂'⟩ = -(α·star β + β·star α)` is **automatically** of the form `-(x+star x)` (`x=α·star β`),
  so re-isotropization uses the EXPLICIT scalar `t=α·star β` — no trace-surjectivity lemma, no H^⊥
  subspace machinery. This avoided the whole orthogonal-complement development the previous handoff
  feared. Pattern: when you need an isotropic vector and have a near-isotropic one, check if its defect
  is already a trace before reaching for surjectivity.
- **The scaling element `uScale`** (the `spDiag` analogue): unitary maps preserve form-type, so you
  CANNOT scale a basis vector and conjugate (the PSL/PSp trick). Build it directly on the hyperbolic
  pair `(v,w)` from `exists_hyperbolic_partner`:
  `uScale v w λ = 1 + (λ-1)(v⊗star w) + ((star λ)⁻¹-1)(w⊗star v)`. Unitarity collapses cleanly via the
  rank-one relations `A²=A, B²=B, AB=BA=0` (closed by `match_scalars <;> field_simp <;> ring`); the
  determinant `λ·(star λ)⁻¹` is the Weinstein–Aronszajn identity `Matrix.det_one_add_mul_comm` (write
  the update as `U·V`, `U=[v|w]` is `N×2`, `V·U=diag(λ-1,(star λ)⁻¹-1)` is `2×2` via `det_fin_two`).
  `∈ SU` needs `det=1`, i.e. `star λ = λ` (fixed field), so `N(λ)=λ²`; for `N(λ)≠1` use the prime-field
  witness `λ=algebraMap 2` (`exists_fixedField_norm_ne_one`, needs `p≥5`).
- **Why I stopped where I did:** the two remaining pieces — the `IwasawaStructure` T-family assembly
  (`is_comm`/`is_conj`/`is_generator`) and `IsQuasiPreprimitive` — are each large fiddly developments
  (the `pspIwasawaStructure.is_conj` in SpIwasawa is ~25 dense lines; quasi-preprimitivity is the deep
  Witt-transitivity core). Starting one mid-budget risks a half-done mess. Generation is at Aristotle.
- **Aristotle is FREE again** (the foreign jobs that blocked all of last lap finished). I submitted the
  **generation** brick: project `a1c167e7-c1bb-417e-9dd2-15e82ddd1fc4`, stub `/tmp/aristotle-ugen/UGen.lean`
  (self-contained `import Mathlib` + `uTransvection` def + `uTransvection_mem_su` axiom, goal = unitary
  Witt generation). It was IN_PROGRESS (~20 min) at handoff. **First thing next lap: poll it.**

## ✅ State (11 commits this lap, every one a green full `lake build` = 8312 jobs, verified)
- `common_nonorth_isotropic_perp`: now a **THEOREM** (was the last faithfulness axiom). `#print axioms`
  of `su_center_le_scalar`/`su_mem_center_iff_scalar` = `[propext, Classical.choice, Quot.sound]`.
- **`UnitarySimple.lean §Faithful`** (NEW): `IsoPoint p n` (isotropic ℙ-points subtype), its
  `SMul`/`MulAction`, `su_center_le_isoKer`/`su_isoKer_le_center` (ker=center), `psuPermHom` +
  `psuPermHom_injective` (n≥3), `psuAction`, **`psuFaithful : FaithfulSMul (PSUConcrete n p) (IsoPoint p n)`**
  (the Iwasawa FaithfulSMul obligation), `nonempty_isoPoint` (n≥2). All axiom-clean.
- **`UnitaryTransvection.lean §Scaling`** (NEW): `uScale` + `uScale_mulVec`/`_self`/`_partner`,
  `uScale_mem` (∈U), `uScale_det`, `uScale_mem_su` (∈SU, fixed-field λ), `uTransvecSU_mem_commutator`
  (each transvection ∈ commutator(SU)), **`commutator_specialUnitaryGroup_eq_top`** (perfectness,
  field-generic, takes generation as hyp). All axiom-clean.
- **`UnitaryFoundation.lean`**: `exists_fixedField_norm_ne_one` (p≥5 perfectness scalar). Axiom-clean.
- **`UnitarySimple.lean`**: `commutator_SU_eq_top_of_generate` (n≥3, p≥5) — concrete perfectness
  modulo generation, feeds in all the above. Axiom-clean.
- `PENDING_WORK.md §G` fully updated.

## 🎬 Next actions
1. **Poll Aristotle**: `aristotle tasks a1c167e7-c1bb-417e-9dd2-15e82ddd1fc4` (one-shot). If COMPLETED:
   `aristotle download a1c167e7-...`, VERIFY in our kernel (`lake env lean` + `#print axioms`), port
   onto repo defs (the stub used the SAME `uTransvection`/`uTransvecSU` names so it ports name-for-name),
   then `commutator_SU_eq_top_of_generate` becomes unconditional → real `commutator(SU)=⊤`. **Then
   immediately submit the next Aristotle brick** (candidate: quasi-preprimitivity / Witt transitivity
   on isotropic points, or the general-λ scaling-element det adjustment). Never let Aristotle idle.
2. **Build the `IwasawaStructure` T-family** (mirror `pspIwasawaStructure`, SpIwasawa.lean ~1305):
   `Tline : IsoPoint p n → Subgroup (PSUConcrete n p)` = `uRootSubgroup` of the isotropic line,
   descended through the center via `QuotientGroup`. `is_comm` from `uRootSubgroup` being
   `IsMulCommutative`; `is_conj` mirror SpIwasawa's `is_conj` using `uTransvecSU_conj`; `is_generator`
   = generation (Step 3a). This + faithfulness + nontrivial + perfectness is 4/5 of Iwasawa.
3. **`IsQuasiPreprimitive`** of the `IsoPoint` action — the DEEP geometric core (unitary Witt
   transitivity + trivial blocks; mirror SpIwasawa `pspQuasiPreprimitive`/`psp_isTrivialBlock_of_isBlock`;
   `IsPreprimitive.isQuasiPreprimitive` is the mathlib bridge). Good Aristotle candidate.
4. **Assemble** `MulAction.IwasawaStructure.isSimpleGroup` ⟹ `IsSimpleGroup (PSUConcrete n p)`, then
   connect `LieType.PSU` (replace opaque carrier; general `q=p^m` needs `GaloisField p (2m)`).

## ⚠️ Gotchas
- **Transient box OOM during `lake build`** (`resource exhausted, code 12` + cascade of `bad import`
  errors) — it's memory pressure corrupting oleans mid-build, NOT a real failure. Just re-run `lake build`
  and retry the commit. Hit it once this lap.
- **`open scoped commutatorElement`** is REQUIRED to use `⁅g,h⁆` element-bracket (it's a *scoped*
  instance); `commutator G` (subgroup) works without it. Use `open scoped commutatorElement in` before
  the lemma.
- **Projectivization motive trap**: never `rw [su_smul_vec_def]` (or any vector rewrite) INSIDE a
  `Projectivization.mk` — the mk's nonzero-proof depends on the vector → "motive is not type correct".
  Reduce to `mk_eq_mk_iff'` (an existential over bare vectors) FIRST, then rewrite the vector.
- **`ext` on `Equiv.Perm (Subtype …)`** recurses through `Subtype.ext_iff`, adding a `Subtype.val`
  coercion that breaks `show g • x = x`. Use `apply Equiv.ext; intro x` (stops at the function level).
- `(g⁻¹ : specialUnitaryGroup).val = (g.val)⁻¹`: bridge via `Matrix.star_eq_inv` (g⁻¹ = star g) +
  `specialUnitaryGroup.coe_star` + `star_eq_conjTranspose` + `Matrix.inv_eq_right_inv` (unitarity).
- `star_mul` is ambiguous (Matrix vs scalar); use `star_mul'` for scalars in a comm field.
- `UnitaryField` lives in namespace `FiniteSimpleGroups.PSU` (so `open FiniteSimpleGroups.PSU` in probes).

## 📁 Key files
- `FiniteSimpleGroups/UnitarySimple.lean` — faithfulness crux + §Faithful (IsoPoint action, psuFaithful)
  + `commutator_SU_eq_top_of_generate`. START HERE.
- `FiniteSimpleGroups/UnitaryTransvection.lean` — §Scaling (uScale, perfectness engine).
- `FiniteSimpleGroups/UnitaryFoundation.lean` — UnitaryField + number-theory existence lemmas.
- `FiniteSimpleGroups/SpIwasawa.lean` — THE template for the remaining IwasawaStructure + primitivity.
- `PENDING_WORK.md §G` — authoritative PSU status.
- `/tmp/aristotle-ugen/UGen.lean` — the generation brick submitted to Aristotle.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (PSU). Absorb
the context, then pick up at Next action #1: poll Aristotle (`aristotle tasks
a1c167e7-c1bb-417e-9dd2-15e82ddd1fc4`); if it landed, verify+port generation and make
`commutator(SU)=⊤` unconditional, then build the IwasawaStructure T-family (Next action #2).**
