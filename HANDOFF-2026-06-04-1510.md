# Handoff: PSU(n,q) thread — Iwasawa structure ASSEMBLED + pretransitivity PROVEN; simplicity ⟸ {generation, block-triviality}

**Date**: 2026-06-04 15:10 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `399bfd3`

## 🎯 What we're doing
Discharging the deep CFSG axiom `PSU_isSimpleGroup` by building `PSUConcrete n p = SU_n(F_{p²})⧸center`
and proving it simple via the **Iwasawa criterion** (`MulAction.IwasawaStructure.isSimpleGroup`),
same machinery as PSL/PSp. The action is on the **isotropic** projective points `IsoPoint p n` (SU is
NOT transitive on full ℙ). PSp (`SpIwasawa.lean`) is the line-by-line template.

## 🧠 Context to carry forward
- **This lap built the ENTIRE Iwasawa assembly and PROVED pretransitivity.** Going in, the two feared
  pieces were the IwasawaStructure T-family and quasi-preprimitivity. Both are now largely done:
  - The IwasawaStructure (`psuIwasawaStructure`) is complete, axiom-clean, modulo a generation hyp.
  - Quasi-preprimitivity decomposes (mathlib `IsPreprimitive.isQuasiPreprimitive`) into pretransitivity
    (**PROVEN**) + block-triviality (non-perp half **built**, perp case remaining).
- **The pretransitivity breakthrough (remember this pattern):** I feared SU-transitivity on isotropic
  points needed Witt classification. It does NOT. The **non-orthogonal case is a product of TWO
  transvections**: for isotropic `v,w`, `⟨v,w⟩=β≠0`, `g = τ_{v,b}·τ_{w,a}` maps `v ↦ (a·star β)·w`,
  with `a` any nonzero trace-zero, `b=-(N(β)·a)⁻¹` AUTOMATICALLY trace-zero (N(β) is star-fixed).
  Then the **general case** routes through a common non-orthogonal isotropic `u` via the already-proven
  diameter-2 connectivity `exists_common_nonorth_isotropic`. See `exists_su_maps_nonorth` /
  `exists_su_maps_isotropic` / `psu_isPretransitive`. (Flushed to KB staging as
  `lean-isotropic-transitivity-two-transvections`.)
- **Why block-triviality is harder:** T1 (transitivity FIXING a line `[x]`) can't use the free
  two-transvection move (fixing x rules it out) — it genuinely needs the unitary **Eichler/Siegel
  transformation** (fix x, translate the hyperbolic partner by `h∈x^⊥`, where h is NOT isotropic so a
  plain transvection won't do). Same Eichler theory as the generation theorem. THAT is why both
  remaining atoms are at Aristotle.
- **The perp case of block-triviality** (distinct PERPENDICULAR isotropic block points) needs a SECOND
  Eichler lemma (perp-transitivity) + an isotropic separation lemma. **BUT for n=3 it's VACUOUS**: two
  distinct isotropic lines are never perpendicular in a nondeg dim-3 Hermitian space (Witt index ≤ 1).
  Proving that vacuity (clean ~50-line linear-algebra lemma, route in PENDING_WORK §G) discharges
  PSU(3,q) block-triviality from the non-perp half alone, needing only T1.
- **MulAut.conj_apply coercion gotcha** (cost me 2 build cycles, flushed to KB): `rw`/`simp` won't fire
  on `↑(MulAut.conj g) z`; it's defeq to `g*z*g⁻¹`, so close with `exact (…).symm`.

## ✅ State (7 commits this lap, every one a green full `lake build` = 8312 jobs, verified)
- **`UnitaryTransvection.lean`** (axiom-clean): `mem_uRootSubgroup`, `uRootSubgroup_congr`,
  `uRootSubgroup_conj` (the Iwasawa `is_conj` engine), `uRootSubgroup_smul`/`uRootSubgroup_rep`
  (line-invariance).
- **`UnitarySimple.lean §Concrete`** (axiom-clean): `exists_su_maps_nonorth` (two-transvection move),
  `exists_su_maps_isotropic` (SU transitive on isotropic lines).
- **`UnitarySimple.lean §Iwasawa`** (all axiom-clean):
  - `psu_mk_smul`, `Tline`, `Tline_mk`, `Tline_iSup`, `psuIwasawaStructure (hgen)`.
  - `psu_isPretransitive (n≥3)` — PROVEN.
  - `form_rep_mk_right_smul`/`_left_smul`, `block_mem_of_nonperp`, `block_univ_of_nonperp_pair` (the
    non-perp half of block-triviality, take T1 as hyp).
  - `commutator_PSU_eq_top_of_generate` (perfectness descended).
  - **`PSU_isSimpleGroup_of_generate_of_qpp (n≥3,p≥5)`** — Iwasawa criterion, takes generation + qpp.
  - **`PSU_isSimpleGroup_of_generate (n≥3,p≥5)`** — the headline: takes only generation `hgen` +
    block-triviality `hblk` (pretransitivity now internal). `#print axioms` = `[propext,
    Classical.choice, Quot.sound]`. **A real, machine-checked reduction of `PSU_isSimpleGroup` to two
    classical inputs.**
- **Aristotle: 2 jobs in flight** — (1) generation `a1c167e7-c1bb-417e-9dd2-15e82ddd1fc4` (>1h);
  (2) T1 `19b0b4a0-94ac-43c7-849b-e0fd7b9c255f` (stub `/tmp/aristotle-t1/T1.lean`).

## 🎬 Next actions
1. **Poll both Aristotle jobs** (`aristotle list`; `aristotle tasks <uuid>`). If generation
   (`a1c167e7`) is COMPLETED: download, VERIFY in our kernel (`lake env lean` + `#print axioms`), port
   onto repo defs (same `uTransvecSU` names → name-for-name), feed into `commutator_SU_eq_top_of_generate`
   and `Tline_iSup` ⟹ `psuIwasawaStructure` unconditional. If T1 (`19b0b4a0`) is COMPLETED: verify,
   port to IsoPoint level, plug into `block_mem_of_nonperp`/`block_univ_of_nonperp_pair`. **Then submit
   the next brick** (perp-transitivity, or the Eichler transformation) — never let Aristotle idle.
2. **Prove the n=3 perp-vacuity** (clean, no Eichler — route in PENDING_WORK §G): get `u` non-orth to
   both x,y; `{x,y,u}` is a basis of `F³`; the functional `⟨x,·⟩-(⟨x,u⟩/⟨y,u⟩)⟨y,·⟩` vanishes on it
   ⇒ is 0 ⇒ (nondeg) x∥y. mathlib: `LinearIndependent.span_eq_top_of_card_eq_finrank`,
   `LinearMap.eqOn_span`, `Module.finrank_pi`. This + non-perp half ⟹ `psu_isTrivialBlock_of_isBlock`
   for n=3 (mod T1) ⟹ PSU(3,q) simple mod generation+T1 only.
3. **Build the perp-case assembly** (general n≥4) mirroring SpIwasawa `psp_isTrivialBlock_of_isBlock`
   (lines 1207-1236), taking perp-transitivity + separation as hyps. Completes block-triviality reduction.
4. **Connect `LieType.PSU`** (final): replace `opaque PSU` with a `def` → `PSUConcrete` (mirror PSp's
   `def PSp` at `LieType.lean:104` + `SpLieType.lean`); general `q=p^m` needs `GaloisField p (2m)`.

## ⚠️ Gotchas
- `open scoped Pointwise` REQUIRED for `MulAut.conj g • Tline x` (the subgroup action) — already in
  §Iwasawa.
- `MulAut.conj_apply` won't `rw`/`simp` through the `↑(conj g)` coercion — use `exact` (defeq). (above)
- Transient box OOM during `lake build` (`resource exhausted, code 12` + bad-import cascade) is memory
  pressure, NOT a real failure — just re-run `lake build`.
- `uRootSubgroup v hv` is proof-irrelevant in `hv` (defeq for different isotropy proofs);
  `uRootSubgroup_congr` is `subst; rfl`.
- Section `Concrete` has `n` IMPLICIT, `p` explicit; section `Iwasawa`/`Faithful` have both explicit.
  Mind the call sites (`PSU_nontrivial p hn` vs `psuFaithful p n hn`).

## 📁 Key files
- `FiniteSimpleGroups/UnitarySimple.lean` — §Concrete (transitivity), §Iwasawa (assembly + block
  scaffold). **START HERE.**
- `FiniteSimpleGroups/UnitaryTransvection.lean` — `uRootSubgroup_*` group-level lemmas, §Scaling.
- `FiniteSimpleGroups/SpIwasawa.lean` — THE template (esp. `psp_isTrivialBlock_of_isBlock` perp case).
- `PENDING_WORK.md §G` — authoritative PSU status (updated this lap).
- `/tmp/aristotle-t1/T1.lean` — the T1 brick submitted to Aristotle.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (PSU). Absorb
the context, then pick up at Next action #1: poll both Aristotle jobs (`aristotle list`), verify+port
any that landed and immediately submit the next brick; while polling, prove the n=3 perp-vacuity
(action #2) — it's the cleanest unblocked win.**
