# Handoff: CFSG scaffold — lt60 landed (2 sorries), order-30 retry in flight

**Date**: 2026-05-31 17:45 UTC · **Branch**: `cfsg-wielandt` (HEAD `f7478b1`)

## 🎯 What we're doing
Building out the **CFSG scaffold** incrementally. Continues the 1700 handoff —
read `HANDOFF-2026-05-31-1700.md` for full context (the one rule + bucket map);
this is an incremental update after the `lt60` Aristotle job finished.

## 🧠 Context to carry forward
**The one rule (unchanged, internalize it):** only build work that is BOTH
(a) part of the CFSG scaffold AND (b) not formalized anywhere else. Deep axioms +
Coq-formalized §1 Bender–Glauberman stay axioms; pin-lag (Alternating) is
delete-on-bump, never rebuilt. The committed `CFSG_MAP.md` is the source of truth
(4 buckets: A done / B stays-axiom / C pin-lag / D real-work). Don't manufacture
busywork; a small bucket-D frontier is correct for a scaffold.

**`lt60` result (the order-<60 theorem, bucket D's only item):** the Aristotle job
finished IDLE with a **259-line artifact at 2 sorries**, saved to
`/tmp/lt60_final_snapshot.lean`. It built real Sylow infrastructure
(`prime_of_simple_isPGroup`, the unique-Sylow-normal contradiction, the faithful
action embedding `|G| ∣ n_p!`) and proved several mixed orders. The **2 remaining
sorries**: `no_simple_group_order_30` (line 235) and the main
`prime_card_of_simpleGroup_card_lt_sixty` (line 259, the `interval_cases`
assembly). Order 56 has machinery in place (I couldn't fully confirm it's
sorry-free — verify if it matters). **I did NOT roll this into the repo** — it has
holes; rolling a sorry-laden file in would be a regression.

**Decision this session:** rather than hand-grind order 30 under a flaky relay, I
gave Aristotle a *scoped* retry — a one-lemma `--project-dir` job (`o30`) with
just `no_simple_group_order_30` + a full proof sketch (element-counting:
n₅=6 ⟹ 24 elts order 5, n₃=10 ⟹ 20 elts order 3, 24+20=44>30). Prompt/sketch is
in `/tmp/ari/o30/O30.lean`. This is the right division of labor: the main job
proved everything *except* the two fiddly bits; isolate one and retry it clean.

**Process note (my mistake, don't repeat):** I submitted `o30` while `lt60` still
showed RUNNING — violating the one-job-at-a-time rule. It resolved OK (`lt60` went
IDLE on its own; the `cancel` API 404'd but the slot freed). Rule stands: wait for
the current job to leave RUNNING before submitting the next. Also note a
`slab-aristotle` job shows RUNNING in `list` — that's NOT ours (another
session/project); ignore it.

## ✅ State (verified this session)
- HEAD `f7478b1`, tree clean (untracked: `.githooks/`, handoff docs). **44 axioms**
  (the `feitThompson_dichotomy` discharge from the prior session is in).
- Repo's only real `sorry`: `SmallOrders:104` (the `lt60`/order-<60 target).
- `lt60` artifact: IDLE, 2 sorries, saved at `/tmp/lt60_final_snapshot.lean`.
- `o30` retry: RUNNING (`aristotle list | grep o30`, id `0f5a4a09-…`).

## 🎬 Next actions
1. **Check `o30`** (`aristotle list | grep o30`, id `0f5a4a09-e934-4541-8db2-46a808ef2656`).
   - **Done/IDLE** → download (`aristotle download 0f5a4a09-… --destination
     /tmp/o30.tar.gz && tar -xzf …`), check `O30.lean` for `sorry`. If clean: this
     gives `no_simple_group_order_30`. Then assemble: take
     `/tmp/lt60_final_snapshot.lean`, splice in the proven order-30 lemma,
     hand-prove the final `interval_cases` wiring (line 259 — the only remaining
     piece; it just dispatches each composite order to its lemma), roll
     v4.28.0→v4.29.1, drop into `SmallOrders.lean:104`, `lake build`,
     `#print axioms`, commit through the pre-commit gate. That clears bucket D.
   - **Still RUNNING** → leave it; it's the autonomous-loop heartbeat job.
   - **Returns a sorry again** → hand-prove order 30 (machinery confirmed present:
     `IsPGroup.disjoint_of_ne`, `orderOf_eq_prime`, element-counting). Report
     honestly; don't roll holes in.
2. **More bucket-A** (if Trevor wants architecture): scan ProofStrategy for axioms
   whose docstrings say "standard given X" where X is a deeper axiom we keep — the
   `feitThompson_dichotomy` template. Most milestones are genuinely deep; be
   skeptical.
3. **Push is host-only** (no box egress). `cfsg-wielandt` has unpushed commits;
   Trevor pushes from the host.

## ⚠️ Gotchas
- **Don't roll a sorry-laden Aristotle artifact into the repo.** `lt60` has 2
  holes; only splice in *proven* pieces.
- **Aristotle: one job at a time** (rate-limited). Wait for RUNNING→IDLE before
  submitting. `cancel` may 404 ("cannot be canceled") but jobs go IDLE on their
  own. `submit "<long text>"` crashes — use `--project-dir`. Poll with `list`,
  never `show` (hangs).
- **`lake build` skips unimported modules** (false green); the pre-commit hook
  runs the full build and blocks bad commits — trust it over your own log reads,
  especially under relay scrambling (which was active this session — output
  duplicated/empty intermittently; use scalar `SENT[k=v]END` probes).
- **`mulEquivOfPrimeCardEq` wants `Nat.card`, not `Fintype.card`.**

## 📁 Key files
- `HANDOFF-2026-05-31-1700.md` — prior handoff; fuller context.
- `CFSG_MAP.md` — the bucket classification + one rule. Read first.
- `/tmp/lt60_final_snapshot.lean` — the 259-line order-<60 artifact (2 sorries).
- `/tmp/ari/o30/O30.lean` — the scoped order-30 retry prompt/sketch.
- `FiniteSimpleGroups/SmallOrders.lean:104` — the target sorry to discharge.
- `FiniteSimpleGroups/ProofStrategy.lean` — spine; `feitThompson_dichotomy` (now
  a theorem) + `classification_via_program`.

---
**→ Next session: this is your starting point. Don't summarize this doc back to
Trevor or wait for instructions, and don't offer other projects from the KB —
this doc IS the chosen thread. Start at Next action #1: check the `o30` Aristotle
job and act on its state.**
