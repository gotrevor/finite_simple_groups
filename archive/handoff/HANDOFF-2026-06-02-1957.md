# Handoff: Bender's cornerstone discharged to a sharp soluble kernel (axiom-free induction)

**Date**: 2026-06-02 19:57 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main)

## 🎯 What this session did
Headline: **`genFittingSubgroup_self_centralizing` (Bender's cornerstone, `C_G(F*(G)) ≤
F*(G)`) is no longer an axiom** — it is a machine-checked theorem, by strong induction on
`|G|`, resting only on a strictly sharper *soluble* base-case axiom. Plus removed one dead
axiom. Stay in this repo (`finite_simple_groups`); read `CFSG_MAP.md` first.

## 🧠 The session arc (mental model)
Marched the Bender axiom boundary all the way down, building each rung axiom-free first:
1. **Removed dead axiom.** `IsSubnormal.sup` (Wielandt full join) was referenced only in
   docstrings since the dichotomy went join-free last session — deleted it, reframed
   `Wielandt.lean` around the proven warm-up `IsSubnormal.sup_normal`. (commit `7d2dc13`)
2. **Fitting monotonicity.** `fittingSubgroup_characteristic` (F(G) is characteristic — its
   defining normal-nilpotent set is automorphism-invariant) ⟹ `fittingSubgroup_map_subtype_le`
   (`F(N) ≤ F(G)` for `N ⊴ G`, via `normal_of_characteristic_of_normal`). (`5f0752e`)
3. **Layer monotonicity.** `isNormalStep/isSubnormal_map_subtype` (push subnormal chains
   through `N ↪ G` via `le_normalizer_map`) ⟹ `isComponent_map_subtype` (a component of
   `N ⊴ G` is a component of `G`) ⟹ `layer_map_subtype_le` (`E(N) ≤ E(G)`); combined into
   `genFittingSubgroup_map_subtype_le` (`F*(N) ≤ F*(G)`). (`0de6c13`)
4. **The induction.** `bender_aux`: strong induction on `Nat.card G`. `C = C_G(F*(G))` is
   normal; if `C = ⊤` the base case fires; else `|C| < |G|`, and since `C` centralizes
   `F*(G) ⊇ F*(C)`, every element of `C` centralizes `F*(C)` so `C_C(F*(C)) = ⊤`, IH gives
   `F*(C) = ⊤`, and monotonicity sends `C ≤ F*(G)`. (`2c8a9d5`)
5. **Kernel sharpening.** `layer_eq_bot_of_le_center` (a central layer is trivial: a central
   component would be perfect+abelian=trivial, contradicting nontriviality) lets the central
   base case shed the layer. The residual axiom is now purely soluble:
   `fittingSubgroup_eq_top_of_layer_eq_bot_of_le_center` — *no components + central `F(G)` ⟹
   `F(G) = ⊤`*. (`4b58cea`)

**The load-bearing lesson (again):** the repo asserted Bender "rests on `[E(G),F(G)]=1` and
the central-product structure — well beyond this scaffold." Same costume-as-fact pattern as
the Wielandt join. The induction + monotonicity were entirely in reach; only the soluble
central base case is genuinely irreducible. Don't trust a docstring's "this is too deep."

## 🔧 Second half of the lap (after the governor said keep going)
Built the **complete in-repo infrastructure to discharge the soluble kernel**, and submitted
both halves of the kernel to Aristotle (async). The kernel splits as:
- **(b) soluble climb** `soluble_fitting_kernel` — all minimal normals abelian + central F(G)
  ⟹ F(G)=⊤. **Submitted: Aristotle project `17c03da4`** (`/tmp/aristotle-bender/Target.lean`).
- **(a) layer-structure** — `layer G = ⊥` ⟹ every minimal normal subgroup abelian. **Submitted
  as `nonabelian_minNormal_has_simple_normal`: Aristotle project `be54b396`**
  (`/tmp/aristotle-minnormal/Target.lean`). Both were RUNNING at lap end.

In-repo, all **axiom-free and green**, so porting the two Aristotle results is near-`exact`:
- `isComponent_of_isSubnormal_of_isSimpleGroup`, `layer_ne_bot_of_isComponent` (LayerNormal) —
  a subnormal non-abelian simple subgroup is a component; a component forces `layer ≠ ⊥`.
- `layer_ne_bot_of_normal_simple_factor` (LayerNormal) — **the (a)-consumer**: if `M ⊴ G` has a
  normal simple non-abelian subgroup `S`, then `layer G ≠ ⊥` (S↪G is subnormal via `S◁M◁G` and
  quasisimple). Feed it `nonabelian_minNormal_has_simple_normal`'s output directly.
- `MinimalNormal.lean` (NEW) — the **independent in-repo path for (a)** (no Aristotle needed),
  four rungs built & axiom-free:
  1. `IsMinimalNormal` def.
  2. `eq_bot_or_eq_top_of_characteristic` — minimal normal ⟹ characteristically simple.
  3. `inf_eq_bot` / `commutator_eq_bot` — distinct minimal normals intersect trivially & commute.
  4. `exists_isMinimalNormal` — finite nontrivial ⟹ ∃ minimal normal subgroup.
  5. `IsMinimalNormal.exists_nonabelian_sub` — **(P2), DONE** char-simple non-abelian ⟹ ∃
     non-abelian minimal normal subgroup. Proved WITHOUT the socle/join-abelian step: `↥M` is
     perfect (commutator characteristic; `⊥` ⟹ abelian), so `F(↥M)` is trivial (`F=⊤` ⟹
     nilpotent ⟹ solvable, contra `Group.IsPerfect.not_isSolvable`), so any abelian minimal
     normal subgroup (normal nilpotent) would be `≤ F = ⊥` — impossible.
  6. **`center_eq_top_of_isMinimalNormal_of_layer_eq_bot` — (a) FULLY DONE in-repo** (`b3ae42c`):
     `layer G = ⊥ ⟹ every minimal normal subgroup abelian`, via `isSimpleGroup_of_top` +
     `layer_ne_bot_aux` (strong `|G|`-induction). **Wired into the Bender kernel** (`8042ae5`):
     `genFittingSubgroup_eq_top_of_centralizer_eq_top` discharges via (a) + the new pure-soluble
     axiom `fittingSubgroup_eq_top_of_minNormal_abelian`. **So Bender's cornerstone now rests on
     exactly ONE non-standard axiom — the soluble climb (b) — and nothing else.** The Aristotle
     (a) job `be54b396` was cancelled (redundant). `#print axioms genFittingSubgroup_self_centralizing`
     = `[propext, Classical.choice, fittingSubgroup_eq_top_of_minNormal_abelian, Quot.sound]`.

  **To make Bender's cornerstone fully axiom-free: only (b) remains** — port Aristotle `17c03da4`
  (`soluble_fitting_kernel`) into the axiom slot `fittingSubgroup_eq_top_of_minNormal_abelian`
  (same shape), verify in kernel, done. (b) is the genuinely deep soluble Fitting fact
  `C_G(F(G)) ≤ F(G)` for the abelian-socle case; if Aristotle stalls it can stay an honest axiom.

  ~~Only the |G|-induction remains for full in-repo (a)~~ (DONE — see rung 6):
  - `layer_ne_bot_aux`: `∀ n (N : Type u) [Group N] [Finite N], Nat.card N ≤ n →
    (∃ M, IsMinimalNormal M ∧ center ↥M ≠ ⊤) → layer N ≠ ⊥`, induction on `n` (cf. `bender_aux`).
    Succ case, given non-abelian minimal normal `M`: **by_cases `IsSimpleGroup ↥M`** — if simple,
    `M` is a subnormal simple non-abelian subgroup = component (`isComponent_of_isSubnormal_of_isSimpleGroup`
    on `hM.1.isSubnormal_top`, then `layer_ne_bot_of_isComponent`). Else `↥M` not simple ⟹ `M ≠ ⊤`
    (prove helper `IsMinimalNormal (⊤:Subgroup N) → IsSimpleGroup N` — minimality gives every
    normal `⊥`/`⊤`, `Nontrivial N` from `⊤ ≠ ⊥` — then `↥⊤ ≃* N` via `topEquiv`+`MulEquiv.isSimpleGroup`
    contradicts not-simple) ⟹ `Nat.card ↥M < Nat.card N` (via `Subgroup.eq_top_of_card_eq`, cf.
    `bender_aux`) ⟹ `≤ m`; `hM.exists_nonabelian_sub hMna` gives the witness for `ih ↥M`, yielding
    `layer ↥M ≠ ⊥`; push to `layer N ≠ ⊥` via `layer_map_subtype_le (N:=M)` + `Subgroup.map_injective`
    (`map_bot`). Then (a): `layer G = ⊥ → ∀ M, IsMinimalNormal M → center ↥M = ⊤` (contrapositive),
    and feed into the axiom `fittingSubgroup_eq_top_of_layer_eq_bot_of_le_center` together with the
    ported soluble climb (b) to discharge it. All five sub-lemmas it needs are already green.

**To finish the discharge** (next lap): port (b) → repo `soluble_fitting_kernel` (restate with
repo `IsMinimalNormal`); get (a) either from Aristotle `be54b396` (then apply
`layer_ne_bot_of_normal_simple_factor`) or finish the in-repo `MinimalNormal` induction; combine
to prove `layer = ⊥ → all minimal normals abelian`; feed into the axiom
`fittingSubgroup_eq_top_of_layer_eq_bot_of_le_center` to make IT a theorem → Bender's cornerstone
becomes axiom-free down to the standard trio. See `ARISTOTLE-JOB-bender-soluble-kernel.md`.

## ✅ State (verified this session)
- Branch `cfsg-sporadic-order-pin`, HEAD at `1c4dc25` (13 commits this lap). **Full `lake build`
  green at 8291 jobs**
  (and every commit gated green by the pre-commit hook). Repo sorry-free.
- **Top-level `axiom` count: 45** (was 46 at session start; net −1 from the dead-axiom
  removal — the Bender work swapped one axiom for one sharper axiom, so it didn't change the
  count but converted a deep cited axiom into a proved theorem).
- `#print axioms genFittingSubgroup_self_centralizing` →
  `[propext, Classical.choice, fittingSubgroup_eq_top_of_layer_eq_bot_of_le_center, Quot.sound]`.
  The cornerstone is a THEOREM; all 8 new lemmas are axiom-free (standard trio only).
- `#print axioms CFSG` unchanged (the F*/Bender track is Bender's-cornerstone infra, separate
  from CFSG's milestone trail).

Also added this lap: `fittingSubgroup_eq_top_iff_isNilpotent` (`F(G)=⊤ ↔ G` nilpotent, the
basic Fitting characterization mathlib lacks; `058840b`), and a full **job spec**
`ARISTOTLE-JOB-bender-soluble-kernel.md` for attacking the remaining kernel (the decomposition
into layer-structure + soluble climb, two submission framings, verify/port plan — NOT yet
submitted).

## 🎬 Next actions (dealer's choice — all genuine)
1. **The soluble kernel `fittingSubgroup_eq_top_of_layer_eq_bot_of_le_center`** is the lone
   remaining axiom in the Bender trail (classical soluble Fitting self-centralizing, central +
   component-free case). **Ready-to-execute plan in `ARISTOTLE-JOB-bender-soluble-kernel.md`** —
   prefer framing (1): hand Aristotle the pure soluble climp (b) in mathlib terms, leaving the
   layer-structure (a) as a sharper residual. CLI `/usr/local/bin/aristotle`; verify in our
   kernel (`#print axioms`, no `sorryAx`, faithful statement) before porting. Or attack (b)
   in-repo.
2. **Other deep local-theory axioms:** the `FeitThompson/BGsection1/*` lemmas (Bender-Glauberman
   §1) and the family-simplicity axioms. The CFSG milestone axioms (`B_theorem`, `trichotomy`,
   etc.) and bucket-C pin-lag axioms (`alternatingGroup_isSimple` — delete-on-mathlib-bump
   ONLY, do NOT bump mathlib on the shared tree) are intended/permanent.
3. The `fittingSubgroup_characteristic` + map-subtype bricks are reusable for any future
   F(N)/E(N)/F*(N) argument — they're general infrastructure now.

## ⚠️ Gotchas (unchanged, confirmed this session)
- **OOM-thrash under the fleet:** full `lake build` and the pre-commit hook intermittently
  fail "Cannot allocate memory" / a spurious top-module failure even when modules build green.
  **Just retry** — it converges (hit it ~twice this session). Do NOT `pkill lean/lake`.
- **In the lean-yolo-box:** `git` local works (commit freely); **no push / no `gh`** — the host
  pushes. Never offer to push.
- **Shared tree:** other one-repo-per-lane sessions live here; MEMORY.md/KB get rewritten by a
  `/distill` worker. Don't wander to other repos.
- Commits land authored as "Trevor Morris" (hook/config); harmless.

## 📁 Key files touched
- `FiniteSimpleGroups/GeneralizedFitting.lean` — Bender now PROVED (`bender_aux` +
  `genFittingSubgroup_self_centralizing`); kernel = `fittingSubgroup_eq_top_of_layer_eq_bot_of_le_center`;
  `genFittingSubgroup_map_subtype_le`.
- `FiniteSimpleGroups/FittingSubgroup.lean` — `fittingSubgroup_characteristic`,
  `fittingSubgroup_map_subtype_le`.
- `FiniteSimpleGroups/LayerNormal.lean` — `isNormalStep/isSubnormal/isComponent_map_subtype`,
  `layer_map_subtype_le`, `layer_eq_bot_of_le_center`.
- `FiniteSimpleGroups/Wielandt.lean` — dead `IsSubnormal.sup` axiom removed.
- `CFSG_MAP.md` — NOT yet updated for this session's axiom-count/Bender change (do next).

---
**→ Next session: STAY in `finite_simple_groups`. Bender's cornerstone is discharged to its
soluble kernel; the cleanest next move is to attack `fittingSubgroup_eq_top_of_layer_eq_bot_of_le_center`
(in-repo soluble theory or Aristotle). Don't re-derive the monotonicity bricks — they're done
and axiom-free.**
