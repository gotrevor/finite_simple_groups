# Handoff: Bender's cornerstone is FULLY AXIOM-FREE (started the lap as a FALSE axiom)

**Date**: 2026-06-03 ~00:10 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main)

## 🎯 Headline

`genFittingSubgroup_self_centralizing` — **Bender's cornerstone `C_G(F*(G)) ≤ F*(G)`** — is
now a fully machine-checked theorem:
`#print axioms` = `[propext, Classical.choice, Quot.sound]`, **no custom axioms**. The entire
F*/Bender trail (`GeneralizedFitting`, `LayerQuotient`, `PerfectCentralExtension`,
`SolubleFittingKernel`, `Subnormal`) has **zero axioms**.

This lap it went all the way from a **FALSE axiom** → fully proved theorem.

## 🧠 The arc

1. **Soundness fix (critical).** The kernel axiom introduced last lap,
   `fittingSubgroup_eq_top_of_minNormal_abelian`, was **false**: `SL(2,𝔽₅)` satisfies "all
   minimal normals abelian" + `F(G) ≤ Z(G)` yet `F(G) ≠ ⊤`. Aristotle found the counterexample.
   Commit `8042ae5` had weakened the (true) `layer = ⊥` hypothesis to the (insufficient) "minimal
   normals abelian". Reverted, then discharged completely.
2. **Solvable Fitting kernel** (`fittingSubgroup_eq_top_of_isSolvable_of_le_center`,
   `SolubleFittingKernel.lean`): `[IsSolvable G] → F(G)≤Z(G) → F(G)=⊤`. Ported from Aristotle,
   axiom-free.
3. **The induction unlock.** Under `F(G) ≤ Z(G)`, `F(G/Z(G)) = ⊥` (not merely central) —
   `fittingSubgroup_quotient_center_eq_bot`. This kills the apparent circularity; the solvability
   induction `isSolvable_aux` (`GeneralizedFitting.lean`) then closes, with `Ḡ = G/Z(G)` smaller,
   `F(Ḡ)=⊥`, `layer Ḡ=⊥`, solvable by IH, `G` = extension of abelian `Z(G)` by solvable `Ḡ`.
4. **Grün's lemma** (`center_quotient_center_eq_bot_of_perfect`, `PerfectCentralExtension.lean`):
   `Z(K/Z(K)) = ⊥` for perfect `K`, via the three subgroups lemma (`commutator_commutator_eq_bot_of_rotate`).
   Plus `perfect_central_ext_quasisimple` (ported from Aristotle, uses Grün + the third iso
   theorem) and the central-commutator bricks (`commutatorElement_mul_central`,
   `commutator_sup_central_eq`).
5. **The component pullback** (`layer_quotient_center_eq_bot`, `LayerQuotient.lean`): a component
   `Lbar` of `G/Z(G)` pulls back to a subnormal `L = comap π Lbar` that is a perfect central
   extension of the quasisimple `↥Lbar`; its perfect core `⁅L,L⁆` mapped into `G` is a subnormal
   quasisimple subgroup — a component — contradicting `layer G = ⊥`. Built from the subnormal
   pullback bricks `IsSubnormal.comap_top` / `IsSubnormal.of_characteristic_subgroupOf`
   (`Subnormal.lean`), `isQuasisimple_commutator_of_central_quotient`, and the transport lemmas
   `IsQuasisimple.ofMulEquiv` / `Subgroup.equivMapOfInjective`.

**The load-bearing lesson (again):** the repo asserted the kernel was "the one genuinely hard
step the order-induction cannot remove." It WAS removable — the apparent circularity (`F(Ḡ) ≤ Z(Ḡ)`
not inherited) dissolves once you see `F(Ḡ) = ⊥`. And a docstring called an axiom "irreducible"
while it was actually *false*. Verify axioms are even true; don't trust "too deep" labels.

## ✅ State (verified this session)

- Branch `cfsg-sporadic-order-pin`, HEAD `890f1f4` (+ a pending docs commit). **Full `lake build`
  green at 8295 jobs** (every commit gated green; OOM-thrash hit ~4× — just retry).
- `#print axioms genFittingSubgroup_self_centralizing` = `[propext, Classical.choice, Quot.sound]`.
- All new theorems verified axiom-clean individually.
- Remaining repo axioms are all **intended / permanent**: family simplicity (`Alternating` ×1 —
  bucket-C delete-on-mathlib-bump; `LieType` ×4, `Exceptional` ×10, `Sporadics` ×3 — deep) and the
  CFSG milestones (`ProofTree` ×6, `ProofStrategy` ×8 — `B_theorem`, `trichotomy`,
  `Feit_Thompson_odd_order`, `Burnside_paqb`, `aschbacher_dichotomy`, …). See CFSG_MAP.md.

## 🎬 Next actions (a NEW thread is needed — the F* track is done)

The obvious "live" axiom (Bender) is fully discharged. Genuinely attackable remaining targets are
large multi-lap developments, not small discharges:
1. **Burnside's `p^a q^b` theorem** (`Burnside_paqb`, `ProofStrategy.lean`) — a real classical
   theorem (NOT a CFSG-scale axiom), provable via character theory. mathlib v4.29.1 has partial
   representation theory (`RepresentationTheory/`) but not the integrality/vanishing-class-sum
   machinery. Big but legitimate; would need the character-theoretic key lemma first.
2. **Soundness audit** of the remaining axioms' *statements* (the Bender axiom was *false* due to a
   formalization slip). The CFSG-milestone axioms use the custom `IsFSG` typeclass and complex
   conclusions — worth a careful re-read that each is a faithful, true statement. Aristotle is good
   at finding counterexamples (it found `SL(2,5)`).
3. `alternatingGroup_isSimple` stays an axiom until the mathlib pin passes PR #36524 — **do NOT**
   bump mathlib on the shared tree, and do NOT re-derive (repo "one rule").

No Aristotle job is in flight (the Bender backstop `419c2021` was cancelled as redundant — its
target `isQuasisimple_commutator_of_central_quotient` is now proved in-repo). The next lap should
architect a bounded lemma for thread (1) or (2) and submit it.

## ⚠️ Gotchas (confirmed this lap)

- **OOM-thrash:** full `lake build` / pre-commit intermittently "Cannot allocate memory" on an
  unrelated mathlib module. **Just retry the commit** — converges.
- **Element commutator bracket `⁅a,b⁆`** needs `open scoped commutatorElement` to write in a
  statement; subgroup bracket `⁅H,K⁆` does not. Symptom: "failed to synthesize `Bracket G G`".
- **Non-ASCII identifiers** (e.g. `L̄` with a combining macron) are rejected: "expected token". Use
  `Lbar`. (Subscripts like `L₁` are fine; combining marks are not.)
- **`set Z := Subgroup.center G`** shadows the `Z.Normal` instance; pin `haveI : Z.Normal := by
  rw [hZ]; infer_instance`.
- `rw` "motive is not type correct" when rewriting a subgroup inside a `_ ⧸ _` (the `Normal`
  instance depends on it): transport via `QuotientGroup.quotientMulEquivOfEq` instead of `rw`.
- `commutator G` is defeq `⁅⊤,⊤⁆` but `rw` won't match across them; bridge with `commutator_def`
  or a `have : ⁅⊤,⊤⁆ = ⊤ := hperf`.
- Lean local git commits OK; **no push / no `gh`** — the host pushes.

## 📁 Key files (this lap)

- `FiniteSimpleGroups/SolubleFittingKernel.lean` (NEW) — solvable kernel + `F(G/Z(G))=⊥`.
- `FiniteSimpleGroups/PerfectCentralExtension.lean` (NEW) — Grün, `perfect_central_ext_quasisimple`,
  `isQuasisimple_of_perfect_of_central_quotient`, central-commutator bricks.
- `FiniteSimpleGroups/LayerQuotient.lean` (NEW) — `isQuasisimple_commutator_of_central_quotient`,
  `layer_quotient_center_eq_bot`.
- `FiniteSimpleGroups/Subnormal.lean` — `IsSubnormal.comap`/`comap_top`/`of_characteristic_subgroupOf`.
- `FiniteSimpleGroups/GeneralizedFitting.lean` — false axiom removed; `isSolvable_aux`; Bender now
  axiom-free.
- `CFSG_MAP.md`, `ON-LINE-REQUEST.md` — updated.

---
**→ Next session: STAY in `finite_simple_groups`. The F*/Bender milestone is COMPLETE and
axiom-free. Pick a new thread (Burnside `p^a q^b`, or a soundness audit of the remaining
intended axioms). No Aristotle job is currently in flight.**
