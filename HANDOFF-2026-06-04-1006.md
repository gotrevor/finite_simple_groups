# Handoff: PSp(2n,q) simplicity AXIOM-CLEAN for q≥5; only small-field perfectness left

**Date**: 2026-06-04 10:06 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `601ddd7`

## 🎯 What we're doing
Discharging the deep CFSG-family axiom `PSp_isSimpleGroup` via the symplectic Iwasawa criterion.
**This lap took it all the way home for `q ≥ 5`** and reduced the remaining debt to one clean,
well-understood piece: perfectness of `PSp(2n,q)` for `q ∈ {2,3}`.

## ✅ State (8 commits this lap, all green — last full `lake build` = 8309 jobs, pre-commit verified)
- `#print axioms PSpn_isSimpleGroup_of_iwasawa` = `[propext, Classical.choice, Quot.sound]` —
  **the entire symplectic Iwasawa criterion is machine-checked, ZERO custom axioms.**
- `#print axioms PSp_isSimpleGroup` (LieType-level **theorem**, `SpLieType.lean`) =
  `[propext, Classical.choice, Quot.sound, PSp_perfect_small_field]`.
- The chain this lap: (1) primitivity axiom `psp_isTrivialBlock_of_isBlock` → theorem (block
  combinatorics: T1/T2 transitivity + non-orthogonality-graph connectivity + rank-3 bootstrap);
  (2) the residual Δ₀ axiom `sp_stab_transitive_on_perp_lines` → theorem (BEAT Aristotle `91082bf9`,
  cancelled); (3) wired into LieType with `[Fact (Nat.Prime q)]` (composite-q soundness fix), λ=2
  gives `hlam` for `q≥5`; (4) narrowed the `q∈{2,3}` residual from *simplicity* to *perfectness*
  via `PSpn_isSimpleGroup_of_perfect`; (5) machine-checked the symplectic **Steinberg relation**
  (`SpSmallField.lean`).
- **Aristotle `055856d3` (`sp-memb`) RUNNING** — symplectic membership of the root unipotents
  (`1 ± s•rootN1`, `1 ± s•rootN2 ∈ symplecticGroup`). Poll `aristotle list` (one-shot, NOT `show`).

## 🧠 Context to carry forward
- **THE Δ₀ KEY INSIGHT** (don't re-trip): perp-line Witt transitivity `[u]→[u']` in `v^⊥` fixing
  `v` is EASY — ALL transvections centred in the *full* `v^⊥` fix `v` (`ω(v,z)=0`), so
  `sp_transvecFixing_step` (e=v) maps `u→u'` directly: one transvection if `ω(u,u')≠0`, else through
  `w∈v^⊥` non-orth to both. The earlier "needs the unipotent radical / can't change the
  v-coefficient" worry was an artefact of over-constraining by fixing a whole hyperbolic *pair*
  (the `offS` toolkit). Fix only `v`.
- **Perfectness for q∈{2,3} is the sole remaining PSp obligation** and is genuinely the next thread.
  Faithfulness + quasi-primitivity hold over ANY field (proven generally), so `PSp(4,2)≅S₆` being
  non-simple is correctly localized to *perfectness failing*, not primitivity. The engine is the
  type-`Cₙ` Steinberg relation `[1+sN₁, 1+tN₂] = 1 + 2st·M` (`root_steinberg`, machine-checked).
  **Structure constant 2** (`N₁N₂=M`, `N₂N₁=−M` ⟹ `[N₁,N₂]=2M`) is exactly why char 2 (`Sp(4,2)`)
  fails and char 3 works. Do **q=3 first** (2 invertible ⟹ long-root transvections are commutators).
- Projective `mk`-point gotcha: never `rw` under a `Projectivization.mk … hv` nonzero proof — use
  `mk_eq_mk_iff'`. Matrix units are `Matrix.single` now (was `stdBasisMatrix`). `le_or_lt`→`lt_or_ge`.

## 🎬 Next actions
1. **Harvest Aristotle `055856d3`** if COMPLETED: download (`aristotle download <uuid> --destination
   /tmp/x`; it's a gzip-tar, `tar xzf`), verify membership in-kernel + `#print axioms`, port
   `rootN1_mem`/`rootN2_mem` into `SpSmallField.lean`. If it walled, prove locally à la
   `spTransvection_mem` (reduce to `N₁ᵀJ + JN₁ = 0` and `N₁ᵀJN₁ = 0`).
2. **Identify the long root with a repo transvection**: `1 + c•rootM i =
   spTransvection (Pi.single (Sum.inr i) 1) (−c)` — because `J ·ᵥ eᵢ = −fᵢ` gives
   `vecMulVec eᵢ (J·eᵢ) = −rootM i`. (Need a `J ·ᵥ Pi.single (inr i)` value; check SpIwasawa's
   `spForm_single_*`/`J_mulVec_single_*` area, ~lines 440-520.)
3. **Assemble `commutator_Sp_eq_top` for q=3** (`commutator (Sp(2n,ZMod 3)) = ⊤`, n≥2): with
   membership (1) + the transvection-id (2) + `root_steinberg`, the long-root transvection
   `τ_{eᵢ,c}` is a commutator (c = −2st, any c since 2 invertible). Short roots: `n≥3` via
   `[x_{εᵢ-εⱼ}, x_{εⱼ-εₖ}] = x_{εᵢ-εₖ}` (3rd index); `n=2` via `[x_{2εⱼ}, x_{εᵢ-εⱼ}] = x_{εᵢ+εⱼ}`.
   Then `sp_transvec_closure_eq_top` (HAVE) ⇒ `commutator=⊤`; descend (`commutator_PSp` pattern) ⇒
   discharge `PSp_perfect_small_field` for q=3. (q=2, n≥3 is subtler — char-2 short-root-only; do later.)
4. Keep ONE Aristotle job in flight whenever idle (architect a bounded brick, `submit` with SHORT
   positional prompt + `--project-dir`, pre-check elaborates first).

## ⚠️ Gotchas
- **Repo treats warnings as errors** (full build / pre-commit). Trim every unused simp arg / variable
  to ZERO before committing leaf files.
- **Pre-commit OOM is transient** (`resource exhausted (error code 12)` on a mathlib trace read under
  `taskset -c 0-5`). Re-run `lake build` (succeeds) then retry the commit — NOT a code error.
- `lake env lean File.lean` checks but does NOT write oleans — `#print axioms` against a stale olean
  lies. Run a real `lake build` before trusting an axiom check after edits.
- Touching `LieType.lean` rebuilds Classification/SL2/etc. (large). Expect a full rebuild.

## 📁 Key files
- `FiniteSimpleGroups/SpIwasawa.lean` — THE Sp file: Δ₀ theorem (`sp_stab_transitive_on_perp_lines`,
  `exists_dot_both_ne_perp`, `exists_form_both_ne_in_perp`), primitivity (`psp_isTrivialBlock_of_isBlock`
  + T1/T2 `psp_stab_maps_nonperp`/`_perp` + `block_*`), `PSpn_isSimpleGroup_of_perfect`/`_of_iwasawa`.
- `FiniteSimpleGroups/SpLieType.lean` — LieType wiring: `PSp_isSimpleGroup` theorem,
  `PSp_perfect_small_field` axiom (the sole residual), `exists_sq_ne_one`.
- `FiniteSimpleGroups/SpSmallField.lean` — **next-thread home**: root unipotents + 9 relations +
  `unipotent_commutator` + `root_steinberg`. Header lists the remaining steps.
- `PENDING_WORK.md` §F.0 — authoritative perfectness attack plan (q=3 path, char-2 obstruction).
- `/tmp/sp-memb/RootMem.lean` — the submitted membership brick (re-creatable).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context, then start at Next action #1: poll Aristotle `055856d3`; harvest it if done, else prove the
root-unipotent membership locally and push the q=3 perfectness assembly (steps 2–3).**
