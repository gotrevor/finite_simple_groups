# Handoff: PSp(2n,q) simplicity — generation core DISCHARGED (2 axioms → 1)

**Date**: 2026-06-04 08:56 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `a8522ce`

## 🎯 What we're doing

Discharging the deep CFSG-family axiom `PSp_isSimpleGroup` via the symplectic Iwasawa criterion.
The assembled theorem is `SpN.PSpn_isSimpleGroup_of_iwasawa [Nonempty l] (hlam : ∃ λ, λ≠0 ∧ λ²≠1)
: IsSimpleGroup (symplecticGroup l F ⧸ center)`. **This lap took it from 2 disclosed geometric
core axioms to 1** by fully discharging the generation core.

## 🧠 Context to carry forward

- **THE WIN**: `sp_stab_hyperbolic_le` (generation core) is GONE — `sp_transvec_closure_eq_top`
  (symplectic transvections generate `Sp`) is now `#print axioms` = `[propext, Classical.choice,
  Quot.sound]`, fully machine-checked. Verified in-kernel this session. `#print axioms
  PSpn_isSimpleGroup_of_iwasawa` = `[propext, Classical.choice, Quot.sound,
  psp_isTrivialBlock_of_isBlock]` — **ONE axiom left.**
- **The method that worked** (recorded as a KB insight too): the **ambient standard-basis
  induction**. Don't reach for an abstract submodule layer (mathlib has NO symplectic/Witt basis
  infra — checked) or recurse to a subtype `{i//i≠i₀}` (heavy Matrix.reindex/fromBlocks transport).
  Instead: standard pairs `e_i=single(inr i)`, `f_i=single(inl i)` (ω=1 under `J=fromBlocks 0 (-1)
  1 0`); the perp of a `Finset S` is the literal coordinate subspace `offS S := {x : ∀i∈S,
  x(inl i)=0 ∧ x(inr i)=0}`. Relative non-degeneracy becomes DIRECT coordinate algebra;
  "g fixes S-pairs ⟹ preserves offS S" is one `sp_preserves_form`. Strong induction `genAux_le`
  on `Sᶜ.card` peels one pair at a time; base `S=univ` ⟹ g=1; instantiate at `S=∅` ⟹ generation.
- **The old perp toolkit is NOT dead**: I kept the `perpComp`/`exists_perp_transvecGen_maps`
  lemmas (built last lap for the now-removed axiom). They give transitivity WITHIN one
  hyperbolic pair's perp — exactly what the perp-line (Δ₀) transitivity for primitivity will
  reuse. Leave them.
- **Soundness note carried from before**: the `hlam` (∃λ≠0, λ²≠1, i.e. |F|≥4) hypothesis
  correctly EXCLUDES non-simple `PSp(4,2)≅S₆`, but the `λ²≠1` route also MISSES `q∈{2,3}` (where
  PSp(2n,3) IS simple). When wiring to LieType, leave a `q∈{2,3}` residual axiom.

## ✅ State (full `lake build` GREEN this session = 8307 jobs; verified via pre-commit gate)

- `279bc5c` — the discharge: ~340 lines of new machine-checked Lean in `SpIwasawa.lean`
  (`single_inl_eq`/`J_mulVec_single_*`/`spForm_single_*`, `offS`+closure, `offS_form_nondeg`,
  `offS_iff_form`, `FixS`+`FixS_mul`/`FixS_transvecSp`, `offS_preserved`, `offS_form_both_ne`,
  `offS_transvecGen_maps`, `sp_eq_one_of_FixS_univ`, `offS_transvecFixing_step`/`_maps_mate`,
  `offS_transvecGen_maps_pair`, `genAux_le`, new `sp_transvec_closure_eq_top`). Axiom removed.
- `a8522ce` — PENDING_WORK updated with the detailed primitivity attack plan.
- **Aristotle `91082bf9` (`aristotle-perp`) RUNNING** — the Δ₀ perp-line Witt transitivity, a
  self-contained vector-level stub (`/tmp/aristotle-perp/PerpTrans.lean`, prereqs inlined as
  axioms). Poll `aristotle list`. (The old `8522edf3` spstab job was CANCELLED — superseded.)

## 🎬 Next actions

1. **Poll Aristotle `91082bf9`**: if COMPLETED → download, verify in-kernel + `#print axioms`,
   port the Δ₀ perp-line transitivity onto repo defs. If it walled, prove Δ₀ locally via the
   kept `perpComp`/`exists_perp_transvecGen_maps` toolkit (decompose `u = α·v₀ + u_perp` w.r.t.
   a hyperbolic pair `(v₀,w)`; the α-fiber move is the parabolic/unipotent-radical part — the
   genuinely hard bit).
2. **Attack the sole remaining PSp axiom `psp_isTrivialBlock_of_isBlock`** (primitivity). Plan in
   PENDING_WORK §F.2. Reduce via mathlib `IsPreprimitive.of_isTrivialBlock_base a₀`; orbit
   structure `{[v₀]},Δ₀,Δ₁`; **Δ₁ (non-perp) transitivity is ALREADY available** =
   `exists_sp_transvecFixing_maps_mate` (e=v₀,f=w,f'=w', scaled ω=1). **KEY INSIGHT: Case A of
   the bootstrap — a block B∋[v₀] containing ANY non-perp point ⟹ B=univ — needs ONLY Δ₁
   transitivity** (B is invariant under Stab([w']) for every [w']∈B via `IsBlock.smul_eq_of_mem`;
   chain through common-non-perp lines, which exist by `exists_form_both_ne`). Build Case A at the
   projective level first (it's the main case); Case B (B ⊆ ℙ(v₀⊥)) needs Δ₀ + isotropic-flag care.
3. After primitivity lands: WIRE into LieType `PSp_isSimpleGroup` (l:=Fin n, F:=ZMod q, hlam from
   q≥5, q∈{2,3} residual axiom — mirror the PSL n=2/n≥3 split).

## ⚠️ Gotchas

- **This repo treats linter warnings as errors** (full build). Always `omit [Fintype l]
  [DecidableEq l] in` for lemmas not using them, and trim unused simp args — iterate until the
  leaf build shows ZERO warnings before committing.
- Extract a matrix entry `(M *ᵥ single p 1) q = M q p` via `rw [mulVec_single_one]; rfl`.
- `Sum.elim`'s zero function needs an explicit annotation `(0 : l → F)` or it elaborates to `sorry`.
- `Finset.notMem_empty` (not `not_mem_empty`); `(Finset.compl_eq_empty_iff S).mp` takes S explicit.
- Pre-commit hook runs the full `lake build` gate on Lean changes — trust its "OK" as green.
- The projective action idiom: `(QuotientGroup.mk g_Sp) • x = g_Sp • x` (`pspPermHom_mk`),
  `g_Sp • mk F v hv = mk F (g_Sp ·ᵥ v) _` (`Projectivization.smul_mk`), `mk_eq_mk_iff` for [·]=[·].

## 📁 Key files
- `FiniteSimpleGroups/SpIwasawa.lean` — THE file. New generation development (`offS`/`FixS`/
  `genAux_le`) ~lines 412–760; the kept perp toolkit (reusable for Δ₀) ~244–410; the remaining
  `axiom psp_isTrivialBlock_of_isBlock` ~line 918; `psp_isPretransitive`/`pspPreprimitive` follow.
- `PENDING_WORK.md` §F — authoritative status + the primitivity attack plan (§F.2).
- `FiniteSimpleGroups/SpTransvection.lean` — transvection algebra atoms.
- `/tmp/aristotle-perp/PerpTrans.lean` — the submitted Δ₀ brick (re-download via `aristotle`).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait
for instructions, and don't offer other projects from the KB — this doc IS the chosen thread.
Absorb the context, then start at Next action #1: poll Aristotle `91082bf9`, then build the
primitivity Case-A bootstrap (needs only Δ₁ transitivity, which you already have).**
