# Handoff: PSL(2,q) simplicity — FULLY PROVEN, port awaiting one `lake build`

**Date**: 2026-06-04 01:56 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `ef8d462`

## 🎯 What we're doing

Discharging `PSL_isSimpleGroup` for `n = 2` — the repo's *one tractable deep axiom*
(per `PENDING_WORK §D`) — via the Iwasawa criterion on the `PSL(2,q) ↷ ℙ¹(F_q)`
action. **This lap finished it.** `PSL2_isSimpleGroup (q) [Fact prime q] (hq : 4 ≤ q)
: IsSimpleGroup (PSL 2 q)` is **proven and verified `#print axioms`-clean**
(`[propext, Classical.choice, Quot.sound]`) — all six Iwasawa obligations machine-checked.

## 🧠 Context to carry forward

- **The proof is COMPLETE and VERIFIED, but the verification I saw was the standalone
  scratch `scratch/Iwa2.lean` via `lake env lean` (EXIT 0, axiom-clean).** The final
  content is *already written into* `FiniteSimpleGroups/SL2.lean` (108 uncommitted
  insertions — the import, `open scoped Pointwise`, and everything from `Tline_conj`
  through `PSL2_isSimpleGroup`), byte-identical to the scratch body. But that edit
  landed via a command that got interrupted **before the library `lake build` ran**, so
  I have NOT personally seen `lake build FiniteSimpleGroups.SL2` go green with it in
  place. Treat it as "almost certainly green, must confirm."
- The construction (math is done, don't re-derive): the last obligation `IwasawaStructure`
  uses `T x = Tline q x` = image in PSL of the **transvection subgroup along the line**
  `transvecGroup v = {transSL v c}`, `transSL v c = I + c·(v⊗vrot)`, `vrot=(−v₁,v₀)`.
  The crux `trans_mul_comm` / `transSL_conj`: conjugation sends a transvection along `v`
  to one along `g·v` *exactly* (no rescaling), because for `det g = 1`, `gᵀ⁻¹·vrot =
  (g·v)rot`. That makes `is_conj` (`Tline_conj`) clean. `is_comm` is free (range of a
  CommGroup hom). `is_generator` (`Tline_iSup`): `transvecGroup e₁ = upper`s,
  `transvecGroup e₂ = lower`s (via `transSL_e1/e2`), which generate SL (`transvections_generate`)
  hence PSL — proved through the `comap` of `mk'`.
- **Lean gotchas hit (also flushed to memory):** `PSL` is a plain `def`, opaque to
  instance search → `(QuotientGroup.mk g : PSL 2 q) • x` fails `HSMul` synthesis. Fix:
  take `g : PSL 2 q` as a param, `obtain ⟨g_SL, hg⟩ := QuotientGroup.mk_surjective g`,
  and `rw [← hg]` to transform the *existing* smul (never write a fresh quotient-smul).
  `MulAut.conj g • H = H.map (MulAut.conj g)` is `Subgroup.toSubmonoid_inj.mp rfl`. The
  `↑(MulAut.conj g)` monoidHom coe blocks `MulAut.conj_apply` — `change` to the MulEquiv
  form. `rw` left a literal `X = X` unsolved twice (instance mismatch) → used term-mode
  `congrArg`.
- Aristotle jobs `ed2f6bed` (parallel_of_det_zero) and `fa9f507f` (trans_mul_comm) were
  submitted then **beaten locally** — both superseded, ignore/cancel. No job needs harvesting.

## ✅ State (committed up to `ef8d462`; final port uncommitted in working tree)

- Committed this lap (5 commits, `5b8ab16..ef8d462`): `pslFaithful` (FaithfulSMul),
  `card_SL2` (|SL(2,q)|, ported `SL2Card.lean`), `pslQuasiPreprimitive` (2-transitivity),
  transvection groundwork (`trans_mul_comm` … `Tline` + is_comm). All axiom-clean, all
  green via pre-commit `lake build`.
- **Uncommitted in `FiniteSimpleGroups/SL2.lean`** (108 lines): `Tline_conj`,
  `transvecGroup_rep`, `Tline_mk`, `transSL_e1`, `transSL_e2`, `Tline_iSup`, `pslIwasawa`,
  `PSL2_isSimpleGroup`. Verified standalone in `scratch/Iwa2.lean` (`lake env lean` EXIT 0,
  `PSL2_isSimpleGroup` axiom-clean). `git status`: ` M FiniteSimpleGroups/SL2.lean`,
  ` M HANDOFF.md` (stale master, ignore), `?? scratch/`.

## 🎬 Next actions

1. **`lake build FiniteSimpleGroups.SL2`** (the leaf; ~retry on spurious OOM). It should
   be green — the content is the verified scratch body. If a name clash or `variable`
   issue surfaces, diff against `scratch/Iwa2.lean` (the authoritative working copy).
2. Confirm `#print axioms FiniteSimpleGroups.SL2.PSL2_isSimpleGroup` =
   `[propext, Classical.choice, Quot.sound]`.
3. **Commit** (`git add FiniteSimpleGroups/SL2.lean`; pre-commit runs full `lake build`).
   Message: PSL(2,q) simple for prime q≥4 — Iwasawa structure complete, deep axiom
   discharged at n=2.
4. `rm -rf scratch/` (no longer needed once committed). Update top docstring of `SL2.lean`
   (currently says 5/6) and `PENDING_WORK §D` to mark obligation 6 DONE → whole PSL(2,q)
   thread COMPLETE.
5. **New thread:** connect `PSL2_isSimpleGroup` to the actual `axiom PSL_isSimpleGroup`
   (in `LieType.lean`/`Classification.lean`) — can it be discharged for n=2, or its n=2
   uses replaced? Then re-inventory open axioms (`how-to-get-unblocked.md` sweep) and
   architect the next Aristotle brick (keep one in flight — none running now).

## ⚠️ Gotchas

- `lake build` intermittently dies with `Cannot allocate memory` on an unrelated `.olean`
  — **just retry** (spurious, happens ~every few builds).
- The pre-commit hook DOES run a full `lake build` (`taskset -c 0-5`) even though
  `.git/hooks/` looks empty (it's wired via `core.hooksPath` or similar) — so a successful
  commit *is* a green full build. Don't separately fret a 590s timeout on a manual
  `lake build` (default targets incl. FeitThompson exceed 590s cold; the hook finishes).
- `scratch/Iwa2.lean` imports `FiniteSimpleGroups.PSLIwasawa`; the SL2.lean port adds that
  import too (no cycle — PSLIwasawa doesn't import SL2).

## 📁 Key files

- `FiniteSimpleGroups/SL2.lean` — the whole PSL(2,q) thread; now ~900 lines, ends in
  `PSL2_isSimpleGroup`. **Has uncommitted port.**
- `scratch/Iwa2.lean` — authoritative verified copy of the final block (delete after commit).
- `FiniteSimpleGroups/PSLIwasawa.lean` — `PSL2_isSimpleGroup_of_iwasawa` (the 5-obligation
  reduction this consumes).
- `PENDING_WORK.md §D` — obligation tracker (says 5/6; bump to done).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or
wait for instructions, and don't offer other projects from the KB — this doc IS the chosen
thread. Absorb the context, then start at Next action #1: run
`lake build FiniteSimpleGroups.SL2`, confirm axiom-clean, and commit the finished
PSL(2,q)-simplicity proof.**
