# Handoff: PSU(n,q) thread — Step-2 faithfulness MACHINE-CHECKED down to ONE narrow geometry axiom

**Date**: 2026-06-04 ~20:30 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `bfdd1bc`

## 🎯 What we're doing
Discharging the deep CFSG classical-family axiom `PSU_isSimpleGroup` (unitary family) by building a
concrete `PSUConcrete n p = SU_n(F_{p²}) ⧸ center`, proving it simple via the **Iwasawa criterion**
(same machinery as PSL/PSp), then connecting `LieType.PSU`. PSp is the line-by-line template. Last
lap finished Step 1 (transvections) + step-2 number-theory prereqs. **This lap built Step 2 — the
faithful action / `ker = center` crux — and got it almost entirely machine-checked.**

## 🧠 Context to carry forward
- **The key structural difference from PSp** (don't re-derive): PSp acts on the FULL projective
  space and its `ker ⊆ center` is the purely-linear `eq_scalar_of_fixes` (tests standard basis,
  no form). **PSU is NOT transitive on full ℙ** (separate isotropic/anisotropic orbits), so the
  Iwasawa action lives on the **isotropic** points and `ker ⊆ center` becomes a genuine geometric
  theorem: *g ∈ SU fixing every isotropic line ⟹ g scalar*. That theorem is the heart of this lap.
- **The crux proof (machine-checked, `su_fixes_isotropic_imp_scalar`, AXIOM-CLEAN):** form-relation
  `star(λ_v)λ_w·⟨v,w⟩=⟨v,w⟩` + the **hyperbolic-pair / trace-zero sum trick** — for a hyperbolic
  pair ⟨v,w⟩=1 and trace-zero `s`, `v+s·w` is isotropic, and reading the v/w-coordinates of
  `g·(v+s·w)=μ(v+s·w)` pins `λ_v=λ_w` AND `N(λ_v)=1` (no triples needed). Then `N=1` upgrades the
  form relation to `λ_v=λ_w` for non-orthogonal pairs; connectivity + isotropic-span propagate one
  μ everywhere. See memory `unitary-faithfulness-via-norm-one-isotropic`.
- **The crux took geometry facts as HYPOTHESES** (clean separation: verified algebra vs axiomatized
  geometry), then I instantiated for `UnitaryField p` and **discharged 2 of 3** geometry facts:
  - `isotropic_span` ✅ — uniform-in-char proof (two distinct norm-(-1) scalars, `eᵢ+c·eⱼ` minus
    `eᵢ+c'·eⱼ` gives `eⱼ`; no division by 2). New `UnitaryFoundation` lemmas: `exists_star_ne_self`,
    `exists_two_norm_neg_one`.
  - `exists_hyperbolic_partner` ✅ — `w₁=d⁻¹·e_k` then correct to isotropic via `w=w₁+t·z`; the
    correction `t+star t = -⟨w₁,w₁⟩` works because **⟨w₁,w₁⟩ is a NORM hence in range(algebraMap)**,
    so `Algebra.trace_surjective` hits it — sidesteps any fixed-field lemma. New lemmas
    `algebraMap_trace_eq_add_star`, `exists_add_star_eq_neg_norm`. See memory
    `finite-field-trace-norm-as-range-algebramap`.
  - **Connectivity** (`exists_common_nonorth_isotropic`): cases 1–2 machine-checked (`⟨z₂,z₁⟩≠0`:
    u=w₁ or u=w₁+c·z₁); **ONLY the perpendicular case (`z₂⊥z₁`) remains as the narrow axiom
    `common_nonorth_isotropic_perp`**.
- **Why I stopped at the perp case:** its genuine content (`z₂⊥z₁` AND `z₂⊥w₁`, i.e. `z₂∈H^⊥` for
  the hyperbolic plane `H=⟨z₁,w₁⟩`) is **vacuous for n=3** (H^⊥ 1-dim nondegenerate ⟹ anisotropic)
  and for n≥4 needs `H^⊥` orthogonal-complement / partner-within-subspace machinery — a dedicated
  geometry sub-development, not a quick brick. I tried an ad-hoc `u=w₁+b·w₂` construction (w₂ =
  partner of z₂) but it splinters into ⟨w₁,w₂⟩=0 vs ≠0 sub-cases with a p=2 edge (avoiding 2 bad
  b-values in an F_p-line of size p) — messier than the clean subspace route. **Recommend the H^⊥
  decomposition next, not the b-juggling.**
- **Aristotle was UNAVAILABLE all lap** — foreign `aristotle-wmertens2d` held the account (RUNNING
  2h+), one-job limit blocked submission. Nothing to do but work locally (which I did).

## ✅ State (10 commits this lap, every one a green full `lake build` = 8312 jobs; verified this session)
- **`UnitarySimple.lean`** (NEW, ~470 lines): SU action on `n→F` / `ℙ(Fⁿ)`;
  `su_central_fixes_isotropic_line` (center⊆fixes); the algebra engine (`lambda_form_relation`,
  `lambda_eq_norm_one_of_hyperbolic`, `lambda_eq_of_nonorth`, `dotProduct_star_swap`);
  `su_fixes_isotropic_imp_scalar` (AXIOM-CLEAN crux); discharged `isotropic_span`,
  `exists_hyperbolic_partner`, connectivity cases 1–2; `su_center_le_scalar`/`su_scalar_mem_center`/
  `su_mem_center_iff_scalar` (center=scalars, n≥3); `su_fixes_isotropic_imp_central` (ker⊆center
  half); `PSU_nontrivial` (Iwasawa Nontrivial obligation). ONE axiom: `common_nonorth_isotropic_perp`.
- **`UnitaryFoundation.lean`** (+5 lemmas): `exists_star_ne_self`, `exists_two_norm_neg_one`,
  `algebraMap_trace_eq_add_star`, `exists_add_star_eq_neg_norm` (all axiom-clean).
- `#print axioms su_fixes_isotropic_imp_scalar` = `[propext, Classical.choice, Quot.sound]`;
  `su_center_le_scalar` / `PSU_nontrivial` add only `common_nonorth_isotropic_perp`. No sorries.
- `PENDING_WORK.md §G` updated.

## 🎬 Next actions
1. **Re-poll Aristotle** (`aristotle list` one-shot). If the foreign job finally freed, submit a
   bounded brick (e.g. the perp-case `common_nonorth_isotropic_perp`, or the scaling element).
2. **Discharge `common_nonorth_isotropic_perp`** — the LAST PSU-faithfulness axiom. Build the
   minimal `H^⊥` machinery: orthogonal complement of a hyperbolic plane is nondegenerate; an
   isotropic vector in `H^⊥` (only for n≥4) has a hyperbolic partner inside `H^⊥`; then `u=w₁+w₂`.
   For n=3, prove the hypothesis is vacuous (H^⊥ anisotropic). Clean route beats the b-juggling.
3. **Step 2 REMAINING — isotropic-point action + `FaithfulSMul`.** Define the SU-action on the
   isotropic ℙ-points SUBTYPE (isotropy is scale-invariant: `⟨cv,cv⟩=N(c)⟨v,v⟩`), wire
   `su_fixes_isotropic_imp_central` + `su_central_fixes_isotropic_line` into `FaithfulSMul` (mirror
   `SpN.pspFaithful`/`pspPermHom`/`pspAction` in `SpSimple.lean`).
4. **Then** Step 3: quasi-preprimitivity (mirror SpIwasawa primitivity), generation (unitary Witt),
   perfectness assembly (needs the unitary scaling element — now unblocked by hyperbolic partners),
   assemble `IwasawaStructure` ⟹ `IsSimpleGroup`, connect `LieType.PSU`.

## ⚠️ Gotchas
- `Pi.smul_single` / `Pi.single_smul` do NOT exist for plain `Pi.single` — prove `a•single k 1 =
  single k a` by `funext` + `Pi.single_apply` (see `smul_single` inside `isotropic_span`).
  `Pi.single_sub` DOES exist.
- `•` on an unannotated `Pi.single j x` triggers HSMul-synthesis timeout — annotate
  `(Pi.single j x : Fin n → UnitaryField p)`.
- `star_inv₀` (not `star_inv'`) for `star x⁻¹ = (star x)⁻¹` in a field.
- `omit [...] in` must precede the docstring; `Matrix.smul_mulVec` (not `_assoc`) for `(a•A)*ᵥv`.
- Transient box OOM during `lake build` (Qq olean alloc) — just retry, it's memory pressure.
- Repo treats warnings as errors in spirit — keep leaves clean (`omit` unused section vars, trim
  unused simp args). `lake build FiniteSimpleGroups.UnitarySimple` (~4-15s) to iterate.

## 📁 Key files
- `FiniteSimpleGroups/UnitarySimple.lean` — THE step-2 faithfulness file. START HERE.
- `FiniteSimpleGroups/UnitaryFoundation.lean` — UnitaryField + number-theory existence lemmas.
- `FiniteSimpleGroups/SpSimple.lean` — the `FaithfulSMul`/`pspPermHom` template for action wiring.
- `FiniteSimpleGroups/SpIwasawa.lean` — Step-3 Iwasawa-assembly + scaling-element (`spDiag`) template.
- `PENDING_WORK.md §G` — authoritative PSU status.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (PSU).
Absorb the context, then pick up at Next action #1: re-poll Aristotle, then attack the perp-case
axiom `common_nonorth_isotropic_perp` (via H^⊥ machinery) or the isotropic-point `FaithfulSMul`
assembly — both directly extend this lap's machine-checked faithfulness crux.**
