# Handoff: PSp(2n,q) simplicity FULLY AXIOM-CLEAN; PSU thread begun (foundation laid)

**Date**: 2026-06-04 11:08 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `41a0b2d`

## 🎯 What we're doing
Discharging the deep CFSG classical-family simplicity axioms. **This lap closed the entire
symplectic family** (`PSp_isSimpleGroup` is now `[propext, Classical.choice, Quot.sound]` — zero
custom axioms) and **began the unitary family** (`PSU`) by laying its foundation.

## ✅ State (4 commits this lap, all pre-commit-verified green @ 8309–8310 jobs)
- `6397f7e` — `PSp_perfect_small_field` discharged for **q=3** (char≠2 Steinberg): the long-root
  transvection `τ_{eᵢ,c} = ⁅1+s·N₁, 1+N₂⁆` (structure constant 2, invertible). Narrowed the residual.
- `bde31d1` — **PSp FULLY axiom-clean**: the char-2 / n≥3 case discharged by the **rank-3 Steinberg
  engine** (char-FREE). `#print axioms PSp_isSimpleGroup = [propext, Classical.choice, Quot.sound]`.
- `74e3661` — docs: stale comments fixed; `PENDING_WORK §G` scopes PSU.
- `41a0b2d` — **`UnitaryFoundation.lean`: PSU step 0** — `F_{p²}` as a Frobenius `StarRing`, so
  `Matrix.specialUnitaryGroup (Fin n) (UnitaryField p)` (= `SU n p`) and its center-quotient
  (`PSUConcrete`) now elaborate with `Group` instances.

## 🧠 Context to carry forward
- **THE perfectness insight (don't re-trip):** the split for `PSp` small-field perfectness is
  **n≥3 (any field) vs n=2 (char≠2 only)**, NOT q=2 vs q=3. The relation
  `⁅1+s·N₁(εᵢ-εⱼ), 1+a·M_j(2εⱼ)⁆ = (1+sa·N₂(εᵢ+εⱼ))·(1+s²a·M_i(2εᵢ))` produces the long root with
  **structure constant 1** (survives char 2!); combined with short roots being commutators for n≥3
  (rank-3 relation `[N₁(i,k),N₂(k,j)]=N₂(i,j)`, const 1), the n≥3 proof is one uniform char-free
  argument. Only `n=2` (no third index) needs the structure-constant-2 commutator ⟹ char≠2 ⟹ q=3.
  `Sp(4,2)≅S₆` is the genuine exception. (Full note: memory `lean-sp-perfect-rank3-char-free`.)
- **PSU is genuinely multi-lap.** `LieType.PSU` is still `opaque` (NOT a real group). The whole
  thread is: (0) connect the type [DONE this lap, in `UnitaryFoundation`], (1) unitary transvections,
  (2) action on isotropic ℙ-points + faithful/quasi-preprimitive, (3) `MulAction.IwasawaStructure`
  (the SAME mathlib criterion PSL/PSp use) + perfectness. **The entire PSp scaffold
  (`SpIwasawa`/`SpTransvection`/`SpSmallField`) is the line-by-line template to copy.**
- **Why I stopped here, not deeper into PSU:** the foundation is a clean, verified, committed
  checkpoint; step 1 (unitary transvections over the Hermitian/sesquilinear form) is a substantial
  fresh effort better started with full budget + the foundation ready to import. Not a dead end —
  a deliberate seam.
- **Aristotle was unavailable all lap** (a foreign `aristotle-wmertens2d` job held the account; the
  account-wide one-job rate limit meant I couldn't submit without throttling). The next bounded
  brick to submit when free: the unitary transvection membership (mirror `spTransvection_mem`).

## 🎬 Next actions
1. **PSU step 1 — unitary transvections.** In a new `UnitaryTransvection.lean` (template:
   `SpTransvection.lean`), over `UnitaryField p`: the standard Hermitian form is `h(x,y) = x ⬝ᵥ star y`
   (mathlib's `unitaryGroup` is exactly `{M | M Mᴴ = 1}`, `Mᴴ = conjTranspose = star`-entrywise). A
   unitary transvection `τ_{v,a}(x) = x + a·h(x,v)·v` for **isotropic** `v` (`h(v,v)=0`) is unitary
   iff `a + star a = 0` (the trace-zero condition). Prove `τ_{v,a} ∈ unitaryGroup` (the analogue of
   `spTransvection_mem`: `M Mᴴ = 1` reduces to `h(v,v)=0` + `a+ā=0`).
2. Then mirror `SpIwasawa`: transvections generate `SU` (closure), the `PSU ↷ ℙ(isotropics)` action,
   quasi-preprimitivity, perfectness, and assemble `MulAction.IwasawaStructure` → `IsSimpleGroup`.
3. **Connect `LieType.PSU`**: once `PSUConcrete` simplicity is real, replace `opaque PSU` with the
   concrete def (CAUTION: `PSU` is used in `classicalLieTypeCarrier` + `PSU_isSimpleGroup`; check the
   `[Group (PSU n q)]` arg becomes derivable). General `q=p^m` needs `GaloisField p (2m)` +
   `iterateFrobenius`; the prime case `q=p` is `UnitaryField p` already.
4. Keep ONE Aristotle job in flight whenever the account frees (poll `aristotle list`, NOT the TUI).

## ⚠️ Gotchas
- **Pre-commit OOM is transient** (`Cannot allocate memory` reading oleans under `taskset -c 0-5`,
  worse when another heavy job runs). Hit it once this lap — re-run `lake build` (succeeds), retry commit.
- **Repo treats warnings as errors** in full build/pre-commit. Trim every unused simp arg / variable.
- `commutatorElement` `⁅,⁆` notation needs `open scoped commutatorElement` (it's scoped).
- `GaloisField`/`UnitaryField` instances cascade `noncomputable` (Field instance is noncomputable);
  `Fintype` only via `Fintype.ofFinite` (it's `Finite` not `Fintype`). Use type synonyms for `Star`.
- `lake env lean File.lean` checks but writes NO olean — run real `lake build` before `#print axioms`.

## 📁 Key files
- `FiniteSimpleGroups/SpSmallField.lean` — the completed perfectness engine (membership, 3 Steinberg
  relations, `group_comm_first/second_order`, `commutator_PSp_eq_top_char_ne_two`/`_n3`).
- `FiniteSimpleGroups/SpLieType.lean` — `PSp_perfect_small_field` (now a THEOREM), `PSp_isSimpleGroup`.
- `FiniteSimpleGroups/UnitaryFoundation.lean` — **PSU step 0**: `UnitaryField p`, the Frobenius
  `StarRing`, `SU`/`PSUConcrete`. The new-thread home — import it into the next PSU file.
- `FiniteSimpleGroups/SpTransvection.lean` / `SpIwasawa.lean` — the **template** to copy for PSU.
- `PENDING_WORK.md §F` (PSp DONE) and **§G** (PSU plan, with verified mathlib API + the Aristotle brick).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (PSU, the
unitary family). Absorb the context, then start at Next action #1: create `UnitaryTransvection.lean`
importing `UnitaryFoundation`, define the unitary transvection, and prove membership — copying the
`SpTransvection.lean` structure line-by-line.**
