# Handoff: PSU(n,q) thread — transvection toolkit COMPLETE + all step-2 number-theory prereqs done

**Date**: 2026-06-04 ~15:30 UTC · **Branch**: `cfsg-sporadic-order-pin` (NOT main) · **HEAD**: `51d95c6`

## 🎯 What we're doing
Discharging the deep CFSG classical-family axiom `PSU_isSimpleGroup` (the unitary family). PSp is
already fully axiom-clean (closed last lap); PSU is the active thread. `PSU n q` is still `opaque`
in `LieType.lean`, so the axiom is vacuous-ish — the multi-lap job is to build a concrete
`PSUConcrete n p := specialUnitaryGroup (Fin n) (UnitaryField p) ⧸ center`, prove it simple via the
**Iwasawa criterion** (same `MulAction.IwasawaStructure` machinery as PSL/PSp), then connect it to
`LieType.PSU`. The PSp scaffold (`SpTransvection`/`SpIwasawa`/`SpSimple`/`SpSmallField`) is the
line-by-line template.

## 🧠 Context to carry forward
- **This lap (9 commits) finished Step 1 entirely and all Step-2 number-theory prerequisites.** The
  whole transvection toolkit is built and axiom-clean. What remains is the genuinely deep stuff:
  the SU action faithful-mod-center, primitivity, generation (unitary Witt), perfectness assembly.
- **The key simplification for PSU transvections** (don't re-derive — see memory
  `[[unitary-transvection-hermitian-star]]`): `τ_{v,a} = 1 + a·(v ⊗ star v)`. The rank-one core
  `vecMulVec v (star v)` is **Hermitian**, so `star(τ_{v,a}) = τ_{v, star a}` — and membership
  collapses to `τ_{v,a}·τ_{v,star a} = τ_{v,a+star a} = τ_{v,0}=1` (needs `v` isotropic +
  `a` trace-zero `a+star a=0`). This AVOIDS the cross-term matrix algebra the symplectic
  `spTransvection_mem` needed. The **norm** `c·star c` plays the role of the symplectic square `c²`
  everywhere (scaling, commutator).
- **The number-theory prereqs went through mathlib's Galois API** (see memory
  `[[finite-field-norm-trace-via-galois-api]]`): I added `Algebra (ZMod p) (UnitaryField p)`,
  connected `star` (Frobenius) to `Algebra.norm`/`frobeniusAlgHom`, and got `∃ c, c·star c=−1`
  (via `FiniteField.norm_surjective`) and Frobenius-nontrivial (via `orderOf_frobeniusAlgHom =
  finrank = 2`) for free. These give isotropic-vector existence and trace-zero nontriviality.
- **Why I stopped here:** Step 1 + step-2 prereqs is a coherent, complete, verified deliverable.
  The next step — `su_ker_le_center` (faithful action mod center, the Schur-type crux of
  `SpSimple`, ~300 lines to mirror) — is a fresh-budget design effort, not a quick brick. Not a
  dead end; a deliberate seam.
- **Aristotle was UNAVAILABLE the entire lap** — a foreign `aristotle-wmertens2d` job held the
  account (RUNNING ~2h+), and the one-job rate limit blocked submission. The ready brick to submit
  the moment it frees: the **unitary scaling element** (`spDiag` analogue) — see PENDING_WORK §G.
- **CAUTION on perfectness:** like PSp(4,2), small unitary groups are exceptions (the scaling needs
  `N(λ)≠1`, requiring a field-size/dimension hypothesis). Defer the exact exclusion, like PSp did.

## ✅ State (9 commits this lap, every one a green full build = 8311 jobs; all `#print axioms`
clean = `[propext, Classical.choice, Quot.sound]`; last full `lake build` green THIS session)
- **`UnitaryTransvection.lean`** (NEW, 264 lines, general `[CommRing α][StarRing α]`): `uTransvection`,
  `_mulVec`, `_star`, `_mem`/`_det`/`_mem_su`, `_mul`, `u_preserves_form`, `u_isotropic_of_mem`,
  `uTransvection_conjH`/`_conj`, **perfectness engine** (`_smul_vec`, `_inv_eq`, `_commutator`),
  group level (`traceZero` AddSubgroup, `uTransvecHom`, `uRootSubgroup` + `IsMulCommutative`,
  `uTransvecSU_conj`), nontriviality (`uTransvection_ne_one`, `uTransvecSU_ne_one`,
  `uRootSubgroup_ne_bot`), and capstone `exists_isotropic_uRootSubgroup_ne_bot`.
- **`UnitaryFoundation.lean`** (was step-0, +norm section): `Algebra (ZMod p)` instance,
  `algebraMap_norm_eq_mul_star`, `exists_norm_neg_one`, `exists_isotropic` (n≥2, action nonemptiness),
  `exists_traceZero_ne_zero` (root-subgroup nondegeneracy).
- Both files wired into `FiniteSimpleGroups.lean`. `PENDING_WORK.md §G` fully rewritten + current.

## 🎬 Next actions
1. **Poll Aristotle** (`aristotle list` — the one-shot, NOT the TUI). If the foreign job finally
   freed, IMMEDIATELY submit the **unitary scaling-element** brick (PENDING_WORK §G): given `v`
   isotropic and `λ` with `N(λ)=λ·star λ≠1`, build `g∈specialUnitaryGroup` with `g·v=λ·v` (inline
   `uTransvection`/membership as axioms). It's the unitary `spDiag`; combined with
   `uTransvection_commutator` it gives perfectness.
2. **Step 2 proper — the faithful action.** Mirror `SpSimple.lean`: define the SU-action on
   `Projectivization (UnitaryField p) (Fin n → UnitaryField p)` (or on isotropic points), prove
   `su_ker_le_center` (kernel of `MulAction.toPermHom` = center — the Schur-type crux), then
   `psuPermHom`/`psuAction`/`psuFaithful`. This is the biggest single remaining chunk.
3. **Then** quasi-preprimitivity (mirror SpIwasawa primitivity, `IsPreprimitive.isQuasiPreprimitive`),
   generation (unitary Witt — deep core, mirror `sp_stab_hyperbolic_le`/ambient induction), assemble
   `IwasawaStructure` ⟹ `IsSimpleGroup (PSUConcrete n p)`, connect `LieType.PSU`.

## ⚠️ Gotchas
- `omit [...] in` and `open ... in` must go BEFORE the docstring, not between docstring and theorem
  (parse error otherwise). Bit me once each this lap.
- `Matrix.star_eq_inv` is in a `section`, not a namespace — it's `Matrix.star_eq_inv`, NOT
  `Matrix.specialUnitaryGroup.star_eq_inv`. But `coe_star` IS `Matrix.specialUnitaryGroup.coe_star`.
- `add_right_eq_self` was renamed → use `add_eq_left : a + b = a ↔ b = 0`.
- `smul_smul` left-associates scalars: `a•(c•(d•N))` → `((a·c)·d)•N`, fix with `mul_assoc`.
- Repo treats warnings as errors in spirit (trim unused simp args / `omit` unused section vars);
  `lake build` itself tolerates warnings but keep leaves clean. Use `lake build
  FiniteSimpleGroups.UnitaryTransvection` (~4-15s) to iterate; full build for commits.
- Don't preempt the foreign Aristotle job (one-at-a-time rate limit).

## 📁 Key files
- `FiniteSimpleGroups/UnitaryTransvection.lean` — THE step-1 file. START HERE for the toolkit.
- `FiniteSimpleGroups/UnitaryFoundation.lean` — UnitaryField + number-theory existence prereqs.
- `FiniteSimpleGroups/SpSimple.lean` — the faithful-action template to mirror for step 2.
- `FiniteSimpleGroups/SpIwasawa.lean` — the Iwasawa-assembly template for step 3.
- `PENDING_WORK.md §G` — authoritative PSU status + remaining cores + ready Aristotle brick.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (PSU).
Absorb the context, then pick up at Next action #1: poll Aristotle (submit the scaling brick if
free), then attack Step 2 — the SU faithful action mod center, mirroring `SpSimple.lean`.**
