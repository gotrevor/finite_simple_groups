# Handoff: Wielandt join closed (axiom) + order-pq dichotomy proved

**Date**: 2026-05-31 05:30 UTC · **Branch**: `cfsg-wielandt` (HEAD `1f6ffcf`)

## 🎯 What we're doing
Continuing the CFSG scaffold. This session picked up the Wielandt thread from
`HANDOFF-2026-05-31-0407.md`, closed it, then marched into the classification
scaffold to discharge a real sorry. Two commits landed on top of the prior pin.

## 🧠 Context to carry forward
- **The Wielandt full join is an axiom on purpose, not a TODO.** I scouted
  mathlib (action #1 of the prior handoff) and found: mathlib's own
  `Subgroup.IsSubnormal` has `inf` but **no `sup`/join** — nothing to port. And
  the join of two subnormals is **false for arbitrary `[Group G]`** (infinite
  counterexamples); it needs finiteness. The repo runs over bare `[Group G]` and
  nothing downstream consumes the full join (the E(G)/layer/component frontier is
  zero-sorry, using only `inf`/`smul`/`sup_normal`). So grinding the
  three-subgroups proof would have meant a hypothesis change + multi-session work
  on a non-blocker. Trevor chose: record it as an honest `axiom` under
  `[Finite G]`, matching the repo's Bender convention
  (`genFittingSubgroup_self_centralizing`). Done.
- **The order-pq dichotomy was overcautiously sorried.** Its comment said "needs
  Schur-Zassenhaus, out of scope" — false. The *dichotomy* `IsCyclic G ∨ p ∣ q-1`
  is pure Sylow counting: n_q=1 always; n_p ∈ {1,q}; n_p=q ⟹ q≡1 mod p ⟹ p∣q-1;
  n_p=1 ⟹ both Sylows normal ⟹ disjoint (coprime orders) ⟹ generators commute ⟹
  product has order pq=|G| ⟹ cyclic. Proved it; compiled first try, axiom-clean.
- **Two "remaining sorries" I'd flagged are NOT worth proving — verified against
  mathlib history** (Trevor pushed on both, rightly):
  - `Alternating.lean` (240/255/277): the three `case*_witness` leaves are a
    **pin-lag artifact**. General Aₙ simplicity (`alternatingGroup.isSimpleGroup`)
    landed in mathlib **PR #36524, 2026-04-28** — 11 days *after* our v4.29.1 pin
    (commit `5e932f97`, 2026-04-17). Our pin has only `isSimpleGroup_five`. So the
    fix is **not** to prove them: on a host-side `lake update` past #36524, delete
    the leaves and collapse `alternatingGroup_isSimple` to the one-liner
    `alternatingGroup.isSimpleGroup (n := Fin n) (by simpa using hn)` (the repo's
    own TODO at Alternating.lean:481-487). Needs network → host-only.
  - `SmallOrders.lean:104` (simple, order<60 ⟹ prime): **genuinely absent** from
    mathlib (only `IsSimpleGroup.prime_card` for `CommGroup`/abelian exists). No
    upstream shortcut. Elementary (Sylow-level) but a real multi-order grind
    (12,24,30,36,40,48,56 + the A₅ boundary). Optional scaffold completeness.
- Verified all the above offline using the **full git clone** inside
  `.lake/packages/mathlib` (it's not shallow — `merge-base --is-ancestor`,
  `tag --contains`, `log --all` all work). See flushed memory below.

## ✅ State (verified this session)
- `cfsg-wielandt` HEAD `1f6ffcf`, tree clean (only untracked `.githooks/` +
  handoff docs). Three commits this thread:
  - `3e2eceb` Wielandt warm-up `IsSubnormal.sup_normal` (prior session)
  - `96e245f` Wielandt full join `IsSubnormal.sup` — axiom, `[Finite G]`
  - `1f6ffcf` order-pq dichotomy proved — `PrimeMul.lean` now sorry-free
- **Full `lake build` green: "Build completed successfully (8282 jobs)"** —
  observed twice, including the pre-commit hook running it on `1f6ffcf`.
- `card_eq_prime_mul_prime_classification` `#print axioms` → `[propext,
  Classical.choice, Quot.sound]` only. Warm-up `sup_normal` still axiom-clean.
- Remaining real sorries repo-wide: `SmallOrders:104`, `Alternating:240/255/277`
  (both characterized above), plus the lone pre-existing `SmallOrders` build
  warning. Everything else green.

## 🎬 Next actions
1. **Default: this is a clean checkpoint — the box-side work is done.** Ask Trevor
   whether to (a) take on `SmallOrders` order<60 (the only genuine hand-proof
   left, multi-session), or (b) stop and let the host handle pushes + the mathlib
   bump. Don't auto-start a grind; the tractable bricks are exhausted.
2. **Host-side only** (no egress in the box — `gh`/`ssh`/`fetch` hang on the
   firewall): push `cfsg-wielandt` (3 commits) + the branch stack below it, open
   PR(s). Then bump mathlib past #36524 + `lake exe cache get` → collapse
   `alternatingGroup_isSimple` to the one-liner, delete the 3 Alternating leaves.

## ⚠️ Gotchas
- **Verify new/edited modules with `lake build <Module>` + olean existence**, not
  bare `lake build` (a module not imported by a default target is skipped → false
  green). The pre-commit hook DID run a real build on both commits this session.
- **Relay scrambled `rg` output mid-session** (lemma names collapsed to `n`). When
  it does, stop trusting grep: confirm lemma names via `lake env lean` `#check`
  (the elaborator's "unknown constant" is ground truth), and read build results
  from a `> /tmp/x.log` file.
- `.githooks/` is **untracked** (local-only `ls a b` fix from the prior session);
  re-apply `git config core.hooksPath .githooks` on a fresh clone. (`hooksPath`
  was already set this session — the hook fired.)
- **Don't touch the master `HANDOFF.md`** — this repo uses dated session docs +
  a post-merge master. Refresh the master only after the branch merges (host-side).

## 📁 Key files
- `FiniteSimpleGroups/Wielandt.lean` — warm-up (proved) + full join (axiom).
- `FiniteSimpleGroups/Adjacent/PrimeMul.lean` — this session's proof; sorry-free.
- `FiniteSimpleGroups/Alternating.lean` — pin-lag sorries; one-liner TODO at L481.
- `FiniteSimpleGroups/SmallOrders.lean` — the one genuine remaining hand-proof.
- `lake-manifest.json` — mathlib pin `5e932f97` (v4.29.1).

---
**→ Next session: this is your starting point. Don't summarize this doc back to
Trevor or wait for instructions, and don't offer other projects from the KB — this
doc IS the chosen thread. The box-side work is at a clean checkpoint: do NOT start
proving `SmallOrders` blind. Open with Next action #1 — ask Trevor whether to grind
`SmallOrders` or hand off to the host for push + mathlib bump.**
